CHANGELOG
Zasady

Po każdej większej zmianie dopisz kilka krótkich punktów opisujących, co zostało zmienione wraz z datą.


21.07.2026
1. Poprawka: cel zarządu "nie spadnij z ligi" możliwy do wylosowania w VII lidze (najniższej)
- Zgłoszenie gracza: w VII lidze (najniższej dostępnej) zarząd mógł wygenerować cel "nie spadnij z ligi", mimo że z tej ligi nie ma gdzie spadać.
- Diagnoza (Zasada #7, raport przed kodem): `systems/board-goals.js`, kaskada doboru puli celów głównych. Gałąź `lvl>=6` (obejmująca V/VI/VII ligę) była jedynym miejscem w całej kaskadzie, które nie wykluczało `ALL_MAIN.stay` dla `lvl===8` — dwie pozostałe gałęzie (`streak>=3` i domyślna `else`) już to poprawnie robiły (`lvl===8?ALL_MAIN.mid:ALL_MAIN.stay`), przeoczenie wyglądało na pominięcie przy kopiowaniu wzorca. Dodatkowo cel przy `lvl===8` był mechanicznie trywialny, nie tylko narracyjnie bez sensu — `check()` (`(G.myLeague||8)<=lvl`) był tam zawsze prawdziwy, bo `myLeague` nigdy nie przekracza 8, więc gracz dostawał darmową nagrodę bez realnego ryzyka porażki.
- Naprawione (jednowierszowa poprawka, zatwierdzona przed wdrożeniem): `pool=[ALL_MAIN.mid,ALL_MAIN.top3,lvl===8?ALL_MAIN.top8:ALL_MAIN.stay,ALL_MAIN.cup_final]` — `stay` zastąpiony przy `lvl===8` już istniejącym, łatwym celem pozycyjnym `top8`, wzorem dwóch pozostałych gałęzi.
- Zweryfikowane: `node --check`.

2. Nowa funkcja: Akademia — faza treningu wychowanka w akademii przed promocją do kadry + rozbudowany łuk fabularny (Wariant B)
- Zlecenie gracza: rekrutacja od 15/16 roku życia z indywidualnym treningiem w akademii zanim wychowanek trafi do kadry seniorskiej, prosty łuk fabularny generujący wpisy w Kronice (nie tylko surowe OVR/potencjał), oraz samo szkolenie jako angażujący mechanizm z powtarzalnymi decyzjami gracza, nie bierne czekanie na wzrost OVR.
- Diagnoza (prompt #8 z `PROMPTY_ROZWOJOWE.md`, Zasada #7 — raport przed kodem): zaakceptowany prospekt trafiał dotąd od razu do `G.players` (`acceptProspect()`) — nie istniał żaden pośredni etap "junior w akademii, jeszcze nie w kadrze". Trening był wyłącznie zbiorowy (`training-stadium.js`, jeden `G.trainFocus` na całą drużynę). Kronika miała już 20 gotowych eventów akademii (`ac01`-`ac20`, `engine/kronika-eventy-academy.js`) pokrywających wyłącznie fazę PO wejściu do kadry. Potwierdzony też znany dług: pola `h.ovr/h.m/h.g/h.a/h.club` w `G.academy.hist` (zakładka "Historia") nigdy nie były ustawiane przez żadne miejsce, które tam pushuje — mimo to działający, równoległy odpowiednik (`p.history[]`, już poprawnie zasilane przez `kronika.js`/`week-progress.js`/`season-summary.js`) istniał od dawna i był używany gdzie indziej (`renderAcadHistoryTab()` na karcie zawodnika).
- Przedstawione i wybrane przez gracza 3 warianty rozwiązania (lekki automat / częściowa kontrola / pełne indywidualne plany z wypożyczeniami) — wybrany **Wariant B** (częściowa kontrola): nowy stan pośredni + sezonowy wybór fokusu, bez tygodniowego mikrozarządzania i bez wypożyczeń (świadomie poza zakresem, kandydat na osobne zlecenie). Ustalone z graczem: wiek startowy zostaje 16-17 (bez zmiany globalnego pasma wieku świata), naprawić przy okazji martwe pola `G.academy.hist`, pełne pokrycie Kroniki dla nowej fazy.
- Kluczowa decyzja architektoniczna (zgodna z zasadą zamkniętego świata zawodników z `CLAUDE.md`): trainee to ten sam obiekt zawodnika co dziś tworzy `acceptProspect()` (prawdziwe `id`, prawdziwa tożsamość), tylko zamiast trafić do `G.players`, ląduje w nowym `G.academy.trainees[]`. Przy promocji ten sam obiekt (nie kopia) trafia do `G.players`, dokładnie tym samym idiomem co dzisiejszy transfer wchodzący (`kronika.js:200-206`). Dzięki temu trainee jest całkowicie niewidoczny dla `myPl()`/silnika meczowego/składu, dopóki nie zostanie promowany, a naprawa "Historii" sprowadza się do przełączenia źródła danych na już działające `p.history[]`.
- Wdrożone w `systems/academy.js`: `acceptProspect()` przekierowany do `G.academy.trainees` (bez numeru koszulki/wpisu `history` — te powstają dopiero przy promocji); nowa `setTraineeFocus()` (sezonowy wybór fokusu ATK/POL/OBR/zbalansowany, reużywa matematykę istniejącego `sendIndCamp()` z `training-stadium.js`); `applyTraineeFocusGains()` (rozliczenie raz na koniec sezonu — świadomy kompromis pod "sesje krótkie/płynne", nie tydzień po tygodniu); `promoteTrainee(pid, early)` (promocja z karą ucięcia potencjału przy wcześniactwie); `releaseTrainee()` (zwolnienie nietrafionego trainee). Naprawiony w tej samej funkcji realny bug wykryty dopiero przy weryfikacji: trainee siedzący w `G.academy.trainees` nie był łapany przez żadną z dwóch istniejących pętli starzenia (`season-summary.js` dla klubu gracza, `match-post.js` dla AI, obie operują tylko na `G.players`) — bez jawnego `p.age++` w `applyTraineeFocusGains()` wychowanek utknąłby na 16-17 lat na zawsze i nigdy nie osiągnąłby progu automatycznej promocji.
- Nowa podzakładka "W akademii" w `renderAcadWychowankowie()` (karty trainee z wyborem fokusu i przyciskami promocji/zwolnienia) oraz naprawiona podzakładka "Historia" (źródło danych przełączone z martwych pól `G.academy.hist` na `p.history[]`, wzorem `renderAcadHistoryTab()`).
- 13 nowych eventów Kroniki (`ac21`-`ac33`, `engine/kronika-eventy-academy.js`, ten sam wzorzec co `ac01-ac20`: 3 wybory kosztowny-pewny/bierny-darmowy/ryzykowny-losowy) pokrywających fazę treningową: przyjęcie do akademii, wybór fokusu, debiut w rezerwach, nagły skok rozwoju, kryzys motywacji (realna kara `trainRate` przy przegranej), uraz na treningu, pokusa wcześniejszej promocji (realnie wywołuje `promoteTrainee(pid,true)`), sama promocja (`ac28`, dodany do `KRON_TIMELINE_WORTHY`), przetrzymywanie zbyt długo (realnie może wywołać `releaseTrainee()`), duma z "fabryki talentów", łańcuch międzysezonowy obserwacji skautów rywala (`ac31`/`ac32`, wzorem istniejącego `ac18`/`ac19`, stan w `G.flags.traineeWatch`), presja rodziców. Razem z `ac01-ac20` (faza po promocji) daje to pełne pokrycie łuku fabularnego wychowanka. Pełne treści T.pl + T.en dla wszystkich nowych kluczy `kron_ac21_*`...`kron_ac33_*` i `acad_trainee_*`.
- Zweryfikowane: `node --check` na wszystkich zmienionych plikach, parytet kluczy i18n PL/EN sprawdzony programowo. Brak dostępu do prawdziwej przeglądarki w tym środowisku (Chrome extension i chromium-cli niedostępne) — zamiast tego napisany i uruchomiony pełny test funkcjonalny w Node (`vm`, realny kod gry z zaślepionym DOM/UI): 67 asercji, wszystkie przeszły — tworzenie trainee i jego izolacja od `G.players`, wybór fokusu, przyrost atrybutów capowany potencjałem, starzenie, promocja automatyczna i wczesna (z karą), zwolnienie, wszystkich 13 nowych eventów (weight/body/choices bez błędów pod realnymi warunkami wyzwalającymi), łańcuch `ac31`/`ac32` między sezonami, renderowanie HTML wszystkich 4 podzakładek Akademii — w tym potwierdzenie, że naprawiona zakładka "Historia" faktycznie pokazuje realne dane sezonowe zamiast pustych pól.

3. Oznaczenie wychowanków akademii (🎓) w Składzie, Meczu, Taktyce i Treningu
- Zlecenie gracza: rozszerzyć istniejące oznaczenie wychowanka o zakładkę Meczową Składu, ekran meczu i trening — zweryfikować gdzie już działa i zaproponować rozwiązanie tam, gdzie brakuje.
- Diagnoza: fioletowa odznaka 🎓 (`#9c27b0`, wzorzec `(p.fromAcademy?' <span style="color:#9c27b0;font-size:9px">🎓</span>':'')`) już działała w Składzie → Statystyki/Zdrowie/Kontrakty (`ui/navigation-squad.js`). Brakowała w 4 miejscach: Skład → Meczowy (`fillTacSquad()`), widget Boisko w Taktyce (`fillPitch()`), karty graczy na boisku podczas meczu live i w podsumowaniu (`renderRatingsPitch()`, współdzielona funkcja dla obu), oraz w Treningu — liście wyboru do obozu indywidualnego i tabeli postępu (`fillCampPanel()`/`fillProgressPanel()`).
- Zaprezentowane w Artifact (rekonstrukcja realnych komponentów gry na tokenach z `css/style.css`) i zaakceptowane przez gracza: dla wierszy tabel — dokładnie ten sam wzorzec inline co już działa; dla kart `.pp` na boisku (mecz + widget Taktyki) — **nowy, narożny znaczek** zamiast tekstu w `.pp-name`, bo ten element ma `max-width:58px;overflow:hidden`, a nazwisko jest już ucięte do 8 znaków — dopisanie emoji ryzykowałoby jego obcięcie.
- Wdrożone: `ui/tactics-playercard.js` (`fillTacSquad()` — inline; `fillPitch()` — narożny znaczek), `engine/match-post.js` (`renderRatingsPitch()` — narożny znaczek, obsługuje jednocześnie mecz live i podsumowanie), `systems/training-stadium.js` (`fillCampPanel()` i `fillProgressPanel()` — inline). Nowa reguła `.pp-acad-badge` w `css/style.css` (mały krążek `#9c27b0` w rogu karty `.pp`, wymagało dodania `position:relative` do `.pp`).
- Zweryfikowane: `node --check` na wszystkich zmienionych plikach JS.


20.07.2026
1. Flow "Nowa Kariera" — nawigacja/reset kroku Talent/Wiek/Skład, brakujące tłumaczenia EN, tekst podsumowania zależny od jakości składu
- Zgłoszenie gracza (sesja w trybie czysto diagnostycznym, Zasada #7 — raport i pytania przed jakimkolwiek kodem): (1) przycisk "wstecz" po wejściu w krok Talent/Wiek/Skład wracał do złego ekranu zamiast do nazwy drużyny, (2) restart wyboru składu nie zaczynał od zera, tylko zachowywał stan poprzedniej próby, (3) wersja EN nadal pokazywała polskie "najsilniejszy"/"najsłabszy" (i inne twarde stringi) w kroku wyboru składu, (4) tekst modala "Prasa przed sezonem" po wylosowaniu składu nie różnicował się wg jakości wylosowanej drużyny.
- Diagnoza (1)+(2), wspólna przyczyna: `G` nigdzie w całym projekcie nie było zerowane poza deklaracją w `core/state.js:228`. Przycisk potwierdzenia modala "Niezapisana gra" (`ng-confirm`, `index.html`) wywoływał `_startNewGameFlow()` bez czyszczenia `G`, więc `_ensureCareerInitialized()` (`js/ui/save-setup-misc.js`) pomijał `initGame()` przy drugim podejściu do flow — Talent/Wiek/Losowanie operowały na starym składzie z poprzedniej kariery, a stan widoczności kroków (`setup-step0`/`panel-philosophy`) i zmienne modułowe (`_philTalent`/`_philAge`/`_philChosenIds`/`_squadRolled`) też nigdy nie wracały do stanu początkowego.
- Wdrożone: `index.html` (`ng-confirm` zeruje `G=null` przed `_startNewGameFlow()`); nowa `_resetPhilosophyStep()` w `js/ui/save-setup-misc.js` (wołana z `_startNewGameFlow()`) przywraca ekran nazwy drużyny, odblokowuje karty Talent/Wiek i przyciski Losuj/Rozpocznij, zeruje stan modułowy.
- i18n (`js/core/i18n.js` + `index.html`): dodane `id` i wiring w `applyLang()` dla "najsłabszy"/"najsilniejszy" (nowe klucze `setup_roll_weakest`/`setup_roll_strongest`) i przycisku losowania (podłączony istniejący, dotąd nieużywany klucz `setup_roll_btn`); nowe klucze PL+EN dla twardych stringów budowanych w JS: `setup_preview_mgr_prefix` ("trener:"), `setup_roll_avg_prefix` ("śr."), `press_squad_avg_label`/`press_squad_avg_vs_league` (linia średniej OVR w modalu prasowym).
- Tekst podsumowania wg jakości składu: `showPressModal()` liczy `delta = squadAvg − _leagueAvgOvr(myLeague)` (już istniejąca funkcja, oparta na realnym paśmie `LEAGUE_OVR`) i dobiera tekst z nowych puli `press_tier_weak/mid/strong_1-10` — próg ±3 OVR ustalony wspólnie z graczem jako 1/3 rzeczywistego rozstępu ligi VII (dół tabeli ~14 OVR, szczyt ~35 OVR wg `mkLeaguePlayers()`/`LEAGUE_OVR[8]`). Zastąpiono nim linię losowaną wcześniej z `_squadRollZone` (mierzyła szczęście rzutu Naturalnego Szumu, nie realną siłę składu względem ligi) — `_squadRollZone` i 60 kluczy `press_roll_low/mid/high_1-10` usunięte, treść przeniesiona 1:1 do nowych kluczy `press_tier_*` (bez pisania nowej treści od zera).
- Follow-up gracza po wdrożeniu: dopisany wiek zawodnika obok OVR na liście wzmocnionych zawodników w modalu prasowym, reużywając istniejącego wzorca `p.age+t('tr_age_suffix')` już używanego w `systems/transfers.js`/`ui/tactics-playercard.js` — bez nowych kluczy i18n.
- Zweryfikowane: `node --check` na `js/core/i18n.js` i `js/ui/save-setup-misc.js`, parytet kluczy PL/EN sprawdzony grepem. Bez testu w prawdziwej przeglądarce w tym środowisku.
- Poprawka po zgłoszeniu gracza: na wykresie słupkowym w kroku Losowania Składu etykiety "najsłabszy"/"najsilniejszy" pod paskami były zamienione miejscami względem realnych słupków. Przyczyna: `_renderSquadRollChart()` (`js/ui/save-setup-misc.js`) sortowała skład malejąco po OVR (`ovr(b)-ovr(a)`, najsilniejszy z lewej), a stałe etykiety w `index.html` w kolejności HTML zakładały odwrotnie (najsłabszy z lewej). Doprecyzowane z graczem: docelowy układ to najsilniejszy z lewej, najsłabszy z prawej — sortowanie w `_renderSquadRollChart()` zostaje malejące (`ovr(b)-ovr(a)`) bez zmian, naprawiona wyłącznie kolejność dwóch `<span>` z etykietami w `index.html` (id/klucze i18n bez zmian, zamieniona tylko pozycja w wierszu). Zweryfikowane: `node --check`.

2. Poprawka: niespójne ułożenie drużyn (gospodarz/gość vs Ty/rywal) między analizą przedmeczową, meczem live i podsumowaniem
- Zgłoszenie gracza: ułożenie drużyn (u siebie po lewej, na wyjeździe po prawej) musi być identyczne na każdym ekranie meczowym — zweryfikować i naprawić.
- Audyt (fork): reguła „gospodarz zawsze po lewej" była już poprawnie stosowana w nagłówku analizy przedmeczowej, na scorebarze live i w zakładce BOISKO. Znalezione dwie niespójności na jej tle: (1) pasek szans WYGRANA/REMIS/PRZEGRANA w analizie przedmeczowej zawsze stawiał usera po lewej, niezależnie od `isHome`; (2) cały ekran podsumowania meczu (`v-match-summary`) zawsze stawiał drużynę gracza po lewej, niezależnie od `isHome`.
- Naprawione w `js/ui/match-ui.js::fillMatch()` (pozycja segmentów paska szans podąża teraz za `isHome`, kolor/etykieta nadal opisują usera) oraz w `js/ui/match-ui.js::openMatchSummary()` + `js/engine/match-post.js::postMatch()` (dodana flaga `isHome` do `window._lastMatchSummary`; lewa strona ekranu podsumowania = gospodarz, prawa = gość, zgodnie z resztą ekranów).
- Zweryfikowane: `node --check` na obu plikach. Bez testu w prawdziwej przeglądarce w tym środowisku.

3. Analiza przedmeczowa — redesign układu (3 mockupy → połączenie wybranych wariantów → wdrożenie)
- Zlecenie gracza: pokazać wizualnie 3 warianty analizy przedmeczowej, możliwie przejrzyste bez nadmiaru danych, ale z wyczerpującą informacją — 3 mockupy w Artifact (belka porównawcza / karty klubów / wniosek + szczegóły na żądanie), oparte na realnych tokenach kolorów i skali fontów z `css/style.css`, żeby wyglądały jak faktyczny ekran gry.
- Doprecyzowanie gracza: połączyć wariant „belka porównawcza" z wariantem „karty klubów", bez powielania danych. Z połączonego mockupu usunięte: kropki formy (zdublowane z liczbą „Forma NN%"), pigułki GOSPODARZ/REMIS/GOŚĆ (zdublowane z paska szans), symetryczny wiersz „Skład OK" pokazywany dla obu drużyn naraz. Kolejne tury poprawek na mockupie: przywrócone kropki formy w oryginalnej kolorystyce, belka ATK/MID/DEF przebudowana z układu wyglądającego na 3-częściowy (dwa paski „doczepione" do środka z pustą szarą strefą pomiędzy) na czysty podział na 2 segmenty, usunięte plakietki „⚠ DEF"/„✓ 15/15", dopisane nazwy drużyn nad paskiem szans (żeby było wiadomo, którego zespołu dotyczy dany koniec paska).
- Wdrożone w `js/ui/match-ui.js::fillMatch()`: nowa `statRow2()` (jeden pasek na statystykę, 2 segmenty — gospodarz|gość) zastępuje `cardStatRow()`/`cardStatRowOpp()` i dwie osobne karty TY/RYWAL; nagłówek przebudowany na karty klubów (OVR, forma, pozycja w tabeli); plakietki formacji/stylu bez dawnych linii Skład/Słabość — baner „uzupełnij skład" nad ekranem nadal pokazuje krytyczne ostrzeżenie o niekompletnym składzie, więc nic nie zniknęło funkcjonalnie. Usunięty martwy kod, który stał się zbędny po zmianie (`BMAX`, `miniBar()`, `cardStatRow()`, `cardStatRowOpp()`, `squadOk`/`stCount`/`req2`), `styleIcon` uogólniony do `_styleIcon(s)` używanej po obu stronach.
- Na życzenie gracza: liczba OVR w karcie klubu najpierw wyraźnie powiększona (24px, waga 800), następnie zmniejszona do rozmiaru nazwy drużyny (`var(--fs-body)`) po doprecyzowaniu, z zachowaniem wytłuszczenia.
- Prawdziwy herb zamiast wymyślonego monogramu: zamiast placeholdera z inicjałami klubu użyty istniejący, wcześniej nieużywany w tym miejscu generator pikselartowych herbów `pxCrest()` (`js/engine/pixelart.js`) — ten sam, którego już używają karta klubu (`club-modal.js`) i tabela ligowa (`season-summary.js`), deterministyczny względem `clubId`. Hydratacja przez placeholdery `.pm-crest-slot`/`data-cid` po ustawieniu `innerHTML`, tym samym wzorcem co w `club-modal.js`/`season-summary.js`.
- Zweryfikowane: `node --check` po każdej turze zmian. Bez testu w prawdziwej przeglądarce w tym środowisku.

4. Mecz pucharowy — pełna analiza przedmeczowa zamiast uproszczonego ekranu
- Zgłoszenie gracza: po meczu ligowym, a przed pucharowym, też powinna być pełna analiza przedmeczowa (wcześniej mecz pucharowy całkowicie omijał `#m-prematch`, pokazując tylko baner i pusty scorebar).
- Zbadane (fork) przed zmianą, czy dane rywala pucharowego są dostępne w tym samym kształcie co dla przeciwnika ligowego: `G.players` jest globalne dla wszystkich klubów w grze (bezpieczne liczenie ATK/MID/DEF/OVR/formy dla dowolnego rywala pucharowego, z dowolnej dywizji), `G.clubTactics` ma już bezpieczny fallback dla klubów spoza bieżącej ligi — ale `ALL_CLUBS` zawiera tylko 16 klubów AKTUALNEJ ligi gracza, więc tożsamość (nazwa/id) rywala pucharowego spoza tej puli nie może być rozwiązywana przez `ALL_CLUBS.find()`, tylko przez dane wbudowane w sam mecz pucharowy.
- Wdrożone w `js/ui/match-ui.js::fillMatch()`: usunięta osobna, uproszczona ścieżka dla `G._cupMatchActive` (wcześniej całkowicie ukrywała `#m-prematch`) — `nextMatch()` już wcześniej normalizował aktywny mecz pucharowy do tego samego kształtu `{h,a,rnd,done}` co mecz ligowy, więc mecz pucharowy przechodzi teraz przez dokładnie tę samą analizę przedmeczową co liga. Dopisane wyłącznie realne różnice: bezpieczny fallback tożsamości klubu (`G._cupMatchActive.match.h/.a.name`, gdy klub spoza `ALL_CLUBS`), pucharowy tekst rundy (`#m-info2`) i etykieta przycisku gry, sterowane przez `G._cupMatchActive`. Baner pucharowy (`#m-cup-banner`) miał już gotową, ale nieosiągalną gałąź w głównej ścieżce — teraz działa. Osobny przycisk `cup-tac-btn` ukrywany tak jak dla ligi (analiza przedmeczowa ma już własny przycisk „ZMIEŃ TAKTYKĘ").
- Świadoma zmiana zachowania (konsekwencja ujednolicenia, nie osobna decyzja): mecz pucharowy podlega teraz tej samej blokadzie „uzupełnij skład" co ligowy — wcześniej puchar całkowicie ignorował kompletność składu.
- Przy okazji naprawiony niezwiązany, preexistujący błąd zauważony podczas pracy w tej samej funkcji: w gałęzi „brak kolejnego meczu" kod odwoływał się do `_scbar` przed jego deklaracją `const` (temporal dead zone) — realny crash w scenariuszu bez kolejnego meczu (np. koniec sezonu). Deklaracja `_scbar` przeniesiona przed pierwsze użycie, usunięty zbędny duplikat niżej w funkcji.
- Zweryfikowane: `node --check` na `js/ui/match-ui.js`, `js/engine/match-post.js`, `js/engine/cup-engine.js`, `js/engine/match-engine.js`. Bez testu w prawdziwej przeglądarce w tym środowisku.


19.07.2026
1. Kapitan drużyny — stały panel wyboru nad listą składu, z widoczną mentalnością
- Zgłoszenie gracza: kapitan gubił się w liście składu (mała ikonka 🎖 doklejona na końcu wiersza, łatwa do przeoczenia). Zaproponowane 3 mockupy (Artifact) ujęcia wizualnego — wybrany wariant C: stały panel nad tabelą składu zamiast szukania w wierszach.
- Doprecyzowanie gracza po wyborze wariantu: atrybut decydujący o jakości kapitana (mentalność, `p.men`) musi być widoczny również przy wyborze kapitana, nie tylko na karcie zawodnika.
- Wdrożone w `js/ui/tactics-playercard.js`: nowy `#sq-captain-panel` (`index.html`, między licznikiem SKŁAD a tabelą), renderowany przez `renderCaptainPanel()` — pokazuje aktualnego kapitana (nazwisko, pozycja, OVR, mentalność) i przycisk „ZMIEŃ" rozwijający listę wszystkich starterów posortowaną malejąco po mentalności, każdy z widoczną wartością — świadomy wybór zamiast klikania w ciemno. Logika doboru domyślnego kapitana (`ensureCaptainValid()`) i `setCaptain()` bez zmian.
- Stara klikalna ikonka 🎖 w wierszu tabeli zastąpiona małym, statycznym znaczkiem „C" widocznym wyłącznie przy aktualnym kapitanie (bez możliwości kliknięcia — wybór odbywa się teraz wyłącznie przez panel). Usunięty nieużywany po tej zmianie klucz i18n `tac_captain_tooltip`.
- Zweryfikowane: `node --check`. Bez testu w prawdziwej przeglądarce w tym środowisku.

2. Nowa funkcja: Drzewo dziedzictwa (Historia klubu → Zawodnicy) — Najlepsza XI, rekordy, zawodnicy którzy odeszli
- Zlecenie gracza: rozszerzyć istniejące odznaki legend (`G.legends`/DATA→LEGENDY) o retrospektywny widok w zakładce ZAWODNICY. Sesja zaczęta zgodnie z Zasadą #7 w trybie czysto diagnostycznym (zero kodu) — sprawdzone grepem, że gra już zachowuje wszystko potrzebne: `G.allTimeStats.players` nigdy nie jest przycinane, `p.history[]` to gotowy per-sezonowy log (OVR/gole/asysty/czyste konta/klub), `p.formerClubs` daje pełną trasę klubową, a istniejąca `legScore()`/`legTrophies()` (`core/data.js`) już liczy „jakość legendy" — zero nowej struktury w `G`, wyłącznie warstwa renderująca licząca na żywo przy każdym otwarciu zakładki.
- Trzy tury mockupów (Artifact) przed kodem: 3 warianty ujęcia → wybór połączenia „boisko + gałęzie osi czasu" → doprecyzowanie z graczem kryteriów kwalifikacji, kształtu formacji i puli kandydatów do listy odejść.
- Wdrożone w `js/systems/traits-history.js` (`renderHistZawodnicy()`), reużywając istniejących klas CSS zamiast nowych — `.pitch-wrap`/`.pp`/`.pp-name`/`.pp-rat` z ekranu Taktyki, `.dyn-item`/`.dyn-dot`/`.dyn-line` z zakładki Dynastia:
  - **Najlepsza XI wszech czasów** — boisko w stałej formacji 4-4-2 (GK1/OBR4/POL4/NAP2, `LEGACY_XI_MIN_MATCHES`, niezależnej od bieżącej taktyki gracza — retrospektywa nie powinna zależeć od dzisiejszych ustawień), kandydaci z ≥30 meczami w klubie, ranking przez istniejącą `legScore()`. Slot pokazuje tylko nazwisko i zakres sezonów (np. „S1–S4") — na życzenie gracza usunięta z boiska sama liczba punktów rankingowych i etykieta „LEG".
  - **Rekordy** — najwięcej goli/asyst/meczów/czystych kont w karierze klubowej wśród wszystkich zawodników (≥10 meczów), bez wymuszania „ta pozycja = ten rekord" — dowolna pozycja może trzymać dowolny rekord; czyste konta i tak w praktyce trafiają tylko do bramkarzy, bo `cs`/`ga` liczone są w tej grze wyłącznie dla GK (`match-engine.js:822-828`), nie z filtra narzuconego w tym kodzie. Sekcja nazwana po prostu „REKORDY" (wcześniej „REKORDY PER POZYCJA").
  - **Zawodnicy, którzy odeszli** — każdy zawodnik (wychowanek lub kupiony, bez rozróżnienia) z ≥100 meczami w klubie, dziś już poza nim, z zakresem sezonów z `p.history` i statusem (aktywny gdzie indziej / emerytura od S{n} / wolny agent), wykluczający zawodników już pokazanych na boisku wyżej.
- Ochrona danych: `protectedRetireeIds()` (`core/data.js`) rozszerzona o nową stałą `VETERAN_MATCHES_THRESHOLD=100` — bez tego zawodnik spełniający próg listy „odeszli" mógłby zostać wycięty z `G.retiredPlayers` przy limicie 200 najnowszych emerytów (100 meczów bez goli/tytułów daje tylko 25/200 pkt istniejącego progu `legScore()`).
- Nowe klucze i18n `ht_legacy_*` (PL+EN, `core/i18n.js`).
- Zweryfikowane: `node --check` na wszystkich zmienionych plikach, ręczne prześledzenie ścieżek brzegowych (pusty zapis, brak rekordu na danej statystyce, nierozwiązywalny zawodnik pominięty z okna emerytów). Bez testu w prawdziwej przeglądarce w tym środowisku.

3. Poprawka: karta zawodnika po przejściu na emeryturę pokazywała wyzerowane gole/asysty
- Zgłoszenie gracza: dla zawodnika, który właśnie przeszedł na emeryturę, panel „BIEŻĄCY SEZON" na karcie zawodnika pokazywał 0 goli i 0 asyst, mimo że w tabeli historii niżej te same liczby były poprawne.
- Przyczyna: `ui/season-summary.js` resetuje `p.st`/`p.cupSt`/`p.seasonRatings` dla WSZYSTKICH zawodników na nowy sezon (~linia 501) ZANIM (~linia 559) zawodnicy oznaczeni `p.retiring` trafiają do `G.retiredPlayers` — realne dane ostatniego sezonu przeżywają poprawnie w `p.history[]` (zapisane wcześniej, przy końcu sezonu), ale `renderPlayerHistory()` (`ui/tactics-playercard.js`) budowała panel „bieżący sezon" bezwarunkowo z `p.st`, które dla emeryta jest już trwale wyzerowane.
- Naprawione w warstwie wyświetlania, bez ruszania logiki zmiany sezonu (ryzykowne dla emerytur AI, dzielących ten sam reset): dla `p.status==='retired'` panel budowany jest z ostatniego realnego wpisu `p.history` (mecze/gole/asysty/kartki/śr. ocena/puchar) zamiast z `p.st`. Nagłówek zmienia się z „BIEŻĄCY SEZON" na „OSTATNI SEZON" dla emerytów (nowy klucz i18n `plr_hist_last_season_label`).
- Sprawdzone, że inne miejsca pokazujące `p.st.g`/`p.st.a` (`club-modal.js`, `navigation-squad.js`, `season-summary.js`) operują wyłącznie na żywych składach klubowych, nigdy na emerytach — nie były dotknięte tym błędem.
- Zweryfikowane: `node --check`. Bez testu w prawdziwej przeglądarce w tym środowisku.

4. Poprawka: własny bramkarz nigdy nie dostawał czystych kont/straconych goli z rozegranych meczów
- Zgłoszenie gracza: po 3 sezonach rekord „Najwięcej czystych kont" w Drzewie dziedzictwa (patrz pkt 2) pokazywał „Brak jeszcze rekordzisty", mimo realnie rozegranych meczów.
- Przyczyna: `engine/match-engine.js::next()` — komentarz w kodzie mówił „GK clean sheets for both teams", ale blok liczył `st.cs`/`st.ga` wyłącznie dla bramkarza PRZECIWNIKA (`oppGK`), nigdy dla własnego. Ten sam brak powielony w `ui/dev-mode.js::devSimMyMatch()` (osobna, równoległa kopia tej logiki na potrzeby trybu „Symuluj sezony") — tam akurat mecze pucharowe liczyły to poprawnie, ale ligowe nie.
- Naprawione w obu miejscach: dodany brakujący, symetryczny blok liczący `st.cs`/`st.ga` dla własnego bramkarza (`myPl().find(p=>p.pos==='GK'&&p.starter)`), analogiczny do już istniejącego dla przeciwnika. Naprawia dane od teraz — mecze z minionych sezonów nie są przeliczane retroaktywnie.
- Zweryfikowane: `node --check` na obu plikach. Bez testu w prawdziwej przeglądarce w tym środowisku.

5. Nowa funkcja: Cele zarządu — eskalacja narracyjna („zarząd pamięta")
- Zlecenie gracza: kolejne cele zarządu mają odwoływać się do wcześniejszych decyzji/obietnic gracza (np. "3 sezony temu obiecałeś awans..."). Sesja zaczęta zgodnie z Zasadą #7 w trybie czysto diagnostycznym — sprawdzone grepem trzech obszarów (`systems/board-goals.js`, `ui/world-board-render.js`, `engine/kronika.js`/`core/state.js`): strukturalny ślad "obietnicy" już istnieje jako `G.board.goalsHistory[]` (season/mainGoalId/mainDone/optGoal/optDone, nigdy nie przycinane) — nie trzeba nowego `G.boardGoalsHistory`, tylko warstwy odczytującej. Poza celami zarządu żadna ogólna historia decyzji Kroniki nie istnieje (tylko garstka z góry zaprojektowanych par flag `G.flags.*` dla konkretnych łańcuchów) — świadomie poza zakresem tej funkcji.
- Zaproponowany i zaakceptowany mechanizm doboru (bez losowości bez sensu): przywołanie tylko gdy stary cel jest **tematycznie powiązany** z pulą oferowaną w bieżącym sezonie (te same "rodziny" celów: awans/tytuł/TOP/puchar/utrzymanie) i miał wagę ≥3★; porażki ranked wyżej niż sukcesy, powtarzające się porażki tego samego celu (eskalacja) wyżej niż pojedyncze; każde wspomnienie użyte co najwyżej raz (`_referenced` na wpisie historii). Gracz doprecyzował kanał dostarczenia: komunikat ma się pokazywać w Aktualnościach, nie w bannerze panelu Zarządu.
- Wdrożone w `systems/board-goals.js`: `checkBoardGoals()` dopisuje `stars` do każdego wpisu `goalsHistory` (potrzebne do oceny wagi, wpisy sprzed zmiany nie kwalifikują się — brak zmyślania wartości wstecz). Nowa `checkBoardMemory()` skanuje historię (pomijając dopiero co zakończony sezon), dobiera najlepszego kandydata wg reguł wyżej i wywołuje `addNews()` ze zrekonstruowaną, poprawnie zlokalizowaną nazwą celu (`t('board_'+mainGoalId+'_label')`, niezależnie od zamrożonego tekstu z chwili ustawienia celu). Podłączone w `ui/season-summary.js::startNewSeason()` zaraz po `genBoardGoals()` — celowo NIE podłączone przy regeneracji obiektów celu po wczytaniu zapisu (`news-bootstrap.js`), żeby wczytanie tego samego save'a nie generowało fałszywego przypomnienia za każdym razem. 5 nowych kluczy i18n `board_memory_*` (PL+EN, w tym poprawna polska odmiana "sezony"/"sezonów").
- Zweryfikowane w sandboxie Node (prawdziwe `core/i18n.js`+`systems/board-goals.js`, 10 scenariuszy: pusta historia, tylko ostatni sezon, porażka tematycznie powiązana, cel zbyt niskiej wagi, brak powiązania rodzinowego, eskalacja przy powtórzonej porażce, już przywołane wspomnienie, pozytywne przywołanie spełnionego celu, poprawność odmiany "2 sezony"/"7 sezonów") — 0 błędów.
- Zweryfikowane w prawdziwej przeglądarce (Playwright + Chromium, lokalny serwer statyczny): nowa kariera rozegrana od ekranu startowego (wybór języka → menu → nowa gra → start), wstrzyknięta historia z niespełnionym 4★ celem "Awans" sprzed 4 sezonów, wywołana ta sama sekwencja co produkcyjnie (`genBoardGoals()`+`checkBoardMemory()`) — komunikat pojawił się na szczycie realnego `#news-list` w zakładce KLUB z poprawną polską odmianą ("4 sezony"), druga próba nie zdublowała wpisu (`_referenced` zadziałał), zero błędów konsoli.

6. Kronika Klubu — audyt wpływu na rozgrywkę, trwała blokada powtórek, losowa kolejność wyborów
- Zlecenie gracza: każda decyzja w Kronice powinna mieć realny wpływ na rozgrywkę (finanse/reputacja/OVR); eventy nie powinny się powtarzać w obrębie rozgrywki poza łańcuchami; układ wyborów (pozytywny/neutralny/negatywny) nie powinien być stały wg pozycji, tylko losowy.
- Audyt (fork, czyste odczytanie 11 plików Kroniki — silnik + 10 kategorii, ~158 eventów / 474 wyborów): **13% wyborów (62, zawsze pozycja B — środkowa) nie miało żadnego efektu mechanicznego** — czysto pusta `effect:function(){}`. Dodatkowo: mutacja realnego atrybutu OVR (tec/pas/sht/def/phy/men) występowała w całej Kronice zaledwie 2 razy (i to przy podpisywaniu nowego wychowanka, nie zmianie istniejącego zawodnika) — "poprawa gracza" szła zawsze przez `.form` (tymczasowe), nigdy przez trwałe atrybuty. Potwierdzony też systemowy szablon autorski (nie przypadek): A=kosztowna+pewna nagroda, B=darmowa+bierna (często pusta), C=darmowa+losowa — zweryfikowane na próbce 20 eventów, 100% zgodności. Silnik dodatkowo koloruje przyciski na sztywno wg pozycji (`kronika.js`), wzmacniając wzorzec niezależnie od treści.
- Powtarzalność: potwierdzone, że blokada (`kron.usedThisSeason`) resetuje się co sezon — ten sam event mógł wracać w nieskończoność. Jedyny znaleziony celowo cykliczny wyjątek: `rv02_rivalry_milestone` (co 5. mecz z rywalem, własny licznik `lastMilestone`).
- Zaakceptowany przez gracza mechanizm i wdrożone w `engine/kronika.js` + `core/data.js` (pośrednio) + `ui/season-summary.js`/`ui/news-bootstrap.js`: nowe trwałe `G.kronika.usedEver[]` (nigdy nie resetowane, w odróżnieniu od `usedThisSeason`) blokuje dany event na całą rozgrywkę, chyba że ma jawną flagę `repeatable:true`. Tą flagą oznaczono 11 eventów o warunkach opartych na bieżącym, powtarzalnym stanie (serie zwycięstw/porażek, presja zarządu, seria kontuzji) — na wyraźną prośbę gracza, żeby sytuacyjne eventy mogły wracać, gdy sytuacja się powtórzy: `rv02_rivalry_milestone`, `s19_superstition_ritual`, `fn01_board_pressure_streak`, `cr04_board_ultimatum`, `k01_star_cup`, `k02_injury_streak`, `s04_bench_protest`, `sp13_win_streak_pressure`, `sp14_lose_streak_media_panic`, `sp17_trait_spotlight`, `sp18_formation_superstition`. Migracja starych zapisów: lazy-init `usedEver` w trzech miejscach (nowa gra, wczytanie, reset sezonowy).
- Losowa kolejność wyborów: `kronShowModal()` tasuje teraz kolejność WYŚWIETLANIA (`ev._displayOrder`, Fisher-Yates przez istniejący `shuffled()`), nie samą tablicę `ev.choices` — `KRON_IGNORED_WORTHY` (2 eventy zakładające na sztywno, że pozycja C to "zignoruj kryzys") nadal poprawnie rozpoznaje oryginalny indeks wyboru, niezależnie od losowego slotu na ekranie. Kolejność liczy się raz na pokazanie eventu i jest cache'owana na obiekcie — powrót do tego samego modala po kliknięciu linku zawodnika w treści nie przetasowuje go drugi raz.
- Uzupełnione wszystkie 62 puste wybory B, każdy dopasowany treściowo do konkretnego eventu (nie kopiuj-wklej): przeważnie mały, gwarantowany, darmowy efekt (pomiędzy kosztownym A a losowym C) — zmiana budżetu/reputacji/formy zawodnika, w kilku przypadkach świadomie negatywny, gdy oryginalny tekst outcome już sugerował gorszy scenariusz (np. `hl01_fatigue_warning`: "gra dalej na przetrzymanie" → realny wzrost zmęczenia). Humorystyczne dopiski PL+EN dograne do istniejącego tonu Kroniki (sucha, biurokratyczno-lokalna ironia). Rozłożone po plikach: academy ×6, club ×6, crisis/cup ×2, finance ×6, health ×6, history ×5, locker ×7, sporting ×10, transfery ×7, core ×2 (`sp01`, `sp06`) — plus 4 przykłady zatwierdzone wcześniej (`ac04`, `s18`, `sp19`, `t08`).
- Zweryfikowane: `node --check` na 15 zmienionych plikach — czysto. W prawdziwej przeglądarce (Playwright): (1) 40× `kronTrigger()` z zerowanym cooldownem — 18 realnych trafień, wszystkie unikalne id, `usedEver` poprawnie narosło, zero nielegalnych powtórek; (2) 12× pokazanie syntetycznego eventu — 5 różnych układów wyborów na 6 możliwych permutacji, stabilna kolejność przy powrocie do tego samego obiektu, `KRON_IGNORED_WORTHY` poprawnie rozpoznaje oryginalny wybór niezależnie od slotu; (3) wszystkie 58 naprawionych wyborów z plików danych wywołane bezpośrednio przez `buildKron*Events()` — poprawne działanie, zero rzuconych wyjątków, zero nierozwiązanych tokenów `{xxx}` w tekście; (4) 2 naprawione eventy core (`sp01`, `sp06`) wywołane przez realny `kronTrigger()` — poprawne działanie. Zero błędów konsoli w całej sesji testowej.

7. Poprawka: trwała blokada powtórek Kroniki (pkt 6) nie skalowała się do bardzo długich rozgrywek
- Zgłoszenie gracza: czy trwała blokada `usedEver` z poprzedniego punktu jest zweryfikowana pod kątem rozgrywki na ok. 100 sezonów, skoro eventów jest tylko 156?
- Zweryfikowane symulacją (158 eventów realnych id, 11 `repeatable`, ~3 eventy/sezon — dokładnie parametry z produkcyjnego kodu): **problem realny, ale nie taki jak podejrzewane "całkowite milczenie"**. System nigdy nie milczy, bo 11 eventów `repeatable` samo w sobie zawsze wypełnia 3 sloty/sezon — ale to właśnie maskowało degradację: po ok. sezonie 49 (147 jednorazowych ÷ 3/sezon) cała reszta 100-sezonowej gry pokazywałaby w kółko WYŁĄCZNIE tych 11 eventów (12/90 unikalnych id w ostatnich 30 sezonach, wobec 88/90 na starcie gry). Pierwszy pomysł na fix ("recykluj gdy pula pusta") w tej symulacji **nigdy by się nie uruchomił** — pula nigdy nie jest pusta właśnie przez `repeatable`.
- Rozwiązanie: `G.kronika.usedEver` (trwała, nieodwracalna blokada) zastąpione przez `G.kronika.lastUsedSeason` (mapa id→sezon ostatniego użycia) z 30-sezonowym cooldownem odnowienia — event jednorazowy wraca do puli po 30 sezonach zamiast nigdy. Eventy `repeatable:true` bez zmian (własny warunek sytuacyjny, bez cooldownu). Przetestowane cooldowny 20/30/35/40 sezonów — wybrane 30 jako środek: przez całe 100 sezonów **zero powtórek w obrębie 30-sezonowego okna** (dokładnie realizuje pierwotną prośbę "nie powtarzać"), a różnorodność w ostatnich 30 sezonach wraca do 83/90 unikalnych id.
- Wdrożone w `engine/kronika.js` (filtr + oznaczanie jako użyty, nowa stała `KRON_REVIVAL_COOLDOWN_SEASONS=30`), `ui/news-bootstrap.js` i `ui/season-summary.js` (lazy-init/migracja `lastUsedSeason`, NIE czyszczone przy resecie sezonowym — w odróżnieniu od `usedThisSeason`). `usedEver` (punkt 6) nigdy nie trafiło do żadnego realnego zapisu poza tą sesją, więc migracja to tylko pusta mapa dla starych zapisów (jednorazowy, zaakceptowany koszt: kilka eventów mogłoby wrócić wcześniej niż po pełnym cooldownie przy pierwszym wczytaniu).
- Zweryfikowane w prawdziwej przeglądarce (Playwright): regresja 40× `kronTrigger()` — te same 18/18 unikalnych trafień co przed refaktorem, zero błędów; `G.kronika.lastUsedSeason` poprawnie zapisuje `{id: sezon}` dla każdego trafienia; test granicy cooldownu — event użyty w sezonie 1 dalej zablokowany w sezonie 11 (10 sezonów odstępu, wewnątrz okna), odblokowany dokładnie w sezonie 31 (30 sezonów odstępu). Zero błędów konsoli.


18.07.2026
1. Poprawka: „Nadchodzące wydarzenia" ogłaszały zbliżanie się do rekordu goli/asyst/występów już w 1. sezonie
- Zgłoszenie gracza: news „Łukasz Książek zbliża się do klubowego rekordu goli — brakuje 1 trafień (rekord: 2)" — absurdalny w 1. sezonie, bo rekord w `G.allTimeStats.players` jest wtedy z natury śladowy (dopiero rozegrane pierwsze mecze).
- Naprawione w `engine/milestones.js` (`checkUpcomingEvents()`, sekcja B): trzy wywołania `_ueCheckRecordProximity` (goals/assists/matches) opakowane w `if(G.season>=2)` — te zapowiedzi odpalają się dopiero od 2. sezonu. Rocznice (staż, awans, rekordowy transfer, stadion) — bez zmian.
- Zweryfikowane: `node --check`.

2. Nowa funkcja: Kronika Klubu — „Rozdział sezonu" + „List do kibiców" w Podsumowaniu Sezonu
- Zlecenie gracza: zamknięcie ekranu podsumowania sezonu (`ui/season-summary.js`) w formie stylizowanej na rozdział kroniki, z jednym zdaniem podsumowującym sezon zbudowanym z faktycznych wydarzeń (nie losowym tekstem), plus krótki list do kibiców z perspektywy menedżera. Sesja zaczęta w trybie diagnostycznym (zero kodu) — sprawdzone, że `G.timeline` jest realnym, ale zbyt rzadkim źródłem sygnału (tylko ~7 typów zdarzeń pushowanych bezpośrednio + eventy Kroniki oznaczone `KRON_TIMELINE_WORTHY`, losowe wyzwolenie), natomiast dane już liczone w `showSeasonSummary()` (pozycja/awans/spadek, top strzelec, transfery, cel zarządu) są bogatsze i gwarantowane co sezon — to one stały się głównym źródłem faktów, nie `G.timeline`. Zdiagnozowane też, że oba życzenia gracza (rozdział + list) to jeden spójny mechanizm zbierania faktów z dwoma niezależnymi konsumentami tekstu, nie dwa osobne zadania.
- Mockup (Artifact, chrom modala 1:1 z realnym `#modal-season-summary`) zatwierdzony po iteracji: 3 scenariusze porównawcze (mistrzostwo/kryzys utrzymania/sezon cichy), przełącznik wariantów i języka PL/EN, pełna biblioteka treści do przeglądu — dopiero po akceptacji ruszył kod produkcyjny.
- Ustalone z graczem przed kodem: próg „utrzymania" — zdejmij z tabeli wynik ostatniej kolejki, jeśli bez niego klub by spadł, a z nim jest bezpieczny, to fakt „utrzymanie" (nie neutralny); w cichym sezonie (niska waga najlepszego faktu) rozdział i tak bierze pojedynczy skromny fakt, bez sklejania dwóch w zdanie złożone; warianty tekstowe humorystyczne (ton ironicznego menedżera, jak istniejąca Kronika), od razu PL+EN.
- Wdrożone: nowa `buildSeasonFacts()` (`ui/season-summary.js`) zbiera fakty sezonu z już liczonych `pos`/`sorted`/`myS`/`lastGoals`/`topScorer` + `G.mHist` (ostatni mecz ligowy, `_isCup` pomijane) + `G.allTimeStats` (rekord = realnie **przebite** maksimum, nie tylko „blisko" jak w `milestones.js`) — zero nowych trwałych pól w `G`. 10 kategorii faktów z ustaloną hierarchią wag (mistrzostwo>awans>spadek>utrzymanie>rekord>kryzys celu zarządu>transfer rekordowy>cel zarządu spełniony>top strzelec>neutralny fallback, ten ostatni zawsze obecny). Rozdział = pojedynczy najwyższy fakt (numer sezonu rzymską liczbą + zdanie), list = najlepszy fakt sportowy + najlepszy klubowy (z fallbackiem, gdy klubowego brak). 133 nowe klucze i18n × PL+EN (`core/i18n.js`): `mss_ch_*` (10×10 wariantów rozdziału) i `mss_letter_*` (10×3 warianty listu + 3 fallback) — dobór wariantu przez istniejący `pick()`/`r()`, ten sam wzorzec co `fanMemoryTrigger()` (`engine/kronika.js`). `index.html`: nowe pasmo rozdziału (`mss-chapter-num`/`mss-chapter-title`) między nagłówkiem a zakładkami modala.
- Poprawka względem mockupu: usunięte odniesienia do baraży/rzutów karnych w kategorii „awans" — gra ma wyłącznie automatyczny awans top-2/spadek bottom-2 (`ui/season-summary.js::startNewSeason()`), bez systemu play-off.
- Zweryfikowane w sandboxie Node (prawdziwy `core/i18n.js` + wyekstrahowany kod `buildSeasonFacts()`): 5 syntetycznych sezonów (mistrzostwo, utrzymanie zdecydowane ostatnią kolejką, sezon cichy z top strzelcem, sezon całkiem pusty, realny spadek) — poprawna hierarchia priorytetów w każdym, `record` poprawnie nie odpala się fałszywie przy realistycznej historii klubu (rozróżnienie „nowy rekord" od „już dawno pobity"), zero nierozwiniętych placeholderów w żadnym z wariantów PL/EN. `node --check` czysty na `core/i18n.js` i `ui/season-summary.js`.

3. Kronika Klubu — diagnostyka i plan rozbudowy o kilkaset wydarzeń (Sesja 0)
- Zlecenie gracza: masowa rozbudowa treści fabularnej Kroniki Klubu (`engine/kronika.js`) o docelowo kilkaset nowych wydarzeń — wyraźnie zastrzeżone jako zadanie wielosesyjne, pierwsza sesja wyłącznie diagnostyczna/planistyczna, zero kodu.
- Przeanalizowany dokładnie post-mortem usuniętego martwego eventu `a05_prospect_burnout` (CHANGELOG, wcześniejszy wpis) — warunek odwoływał się do pola (`p.seasonsInAcademy`), które generujący kod nigdy nie ustawiał, i do stanu (prospekt żyjący >1 sezon), który mechanika akademii w ogóle nie dopuszcza. Wyprowadzona zasada obowiązująca przy projektowaniu każdego nowego eventu: zanim `weight()` odwoła się do pola encji, zweryfikować grepem, że pole jest realnie ustawiane i że encja/stan przeżywa wystarczająco długo, by warunek mógł się kiedykolwiek spełnić. Przy okazji znaleziony analogiczny, odwrotny przypadek wciąż obecny w kodzie: `G.flags.firstTitleSeason`/`hallOfFame`/`trainingUpgrade*`/`dataSecure*` były rzetelnie zapisywane przez istniejące eventy, ale nigdzie w repo nieodczytywane — gotowy, bezpieczny materiał na przyszłe rocznice.
- Zinwentaryzowane: 30 istniejących eventów w 9 kategoriach (nierówny rozkład — `locker`/`transfers`/`sporting` przeładowane, `history`/`club`/`academy` prawie puste), 13 typów warunków wyzwalania już używanych, oraz „menu składników" — pola `G` realnie dostępne, ale jeszcze niewykorzystane (`G.cHist`, `G.records`, `G.legends`, `G.cupHistory`, `TRAITS`, `p.fatigue`, `p.suspension` i in.).
- Zaprojektowany szkielet: taksonomia (eventy reaktywne / rocznice / łańcuchy wieloetapowe × 10 kategorii tematycznych), propozycja rozszerzenia istniejącego `fanMemoryTrigger()` zamiast budowy równoległego silnika rocznic, ocena ryzyka (wydajność runtime: niskie; utrzymywalność jednego pliku przy setkach eventów: realne, stąd propozycja podziału na pliki danych) oraz plan 10 sesji tematycznych + 2 sesje inżynieryjne (rocznice, łańcuchy). Zapisany w `PLAN_KRONIKA_ROZBUDOWA.txt` (nowy plik w korzeniu repo) do mojej akceptacji.
- Decyzje zatwierdzone po diagnostyce: podział na pliki danych bez zmiany silnika wyzwalania (opcja B), `i18n.js` zostaje jednym plikiem, próg rocznic 5 sezonów, łańcuchy wieloetapowe rozszerzone międzysezonowo (stan wyłącznie w `G.flags`, nigdy w `G.kronika.flags` — to drugie zeruje się co sezon w `season-summary.js`), cel: 150 eventów łącznie.
- Fundament (Sesja 0): `flags:{}` dopisane do literału `G` w `initGame()` (`ui/news-bootstrap.js`) + migracja starych zapisów (`if(!G.flags)G.flags={};`), pełna lista prefiksów id per kategoria ustalona z góry, żeby uniknąć kolizji między sesjami.

2. Kronika Klubu — 150 nowych wydarzeń w 10 kategoriach tematycznych, humor w stylu Hattricka (Sesje 1-10)
- Po jednej sesji na kategorię, każda w osobnym nowym pliku ładowanym przed `engine/kronika.js`: `kronika-eventy-history.js` (+16), `-academy.js` (+20, pierwsza treść tej kategorii — było 0), `-sporting.js` (+15), `-locker.js` (+13), `-transfery.js` (+12), `-finance.js` (+12, nowy prefiks `fn`), `-club.js` (+9, nowy prefiks `cl`), `-crisis.js` (+8, wyższa stawka niż flavor — realne kryzysy budżetowe/dyscyplinarne/kontuzyjne w skali istniejących `k04`/`x02`), `-health.js` (+8, pierwsza sesja sięgająca po `p.fatigue` — zweryfikowane, dotąd nieużywane pole), `-cup.js` (+7). Razem dokładnie 30+120=150, potwierdzone grepem po unikalnych `id:` w całym `engine/kronika.js` + wszystkich plikach danych.
- Ważna poprawka architektoniczna wykryta przy pierwszej sesji: eventy w plikach danych nie mogą być statyczną tablicą budowaną raz przy starcie strony — `title`/`category` to gotowe stringi z `t()`, a jedyny powód, dla którego dzisiejszy silnik bezpiecznie przełącza język w locie, to fakt, że cała tablica `KRON_EVENTS` jest budowana od nowa co tydzień. Ustalony i utrzymany przez wszystkie 10 sesji wzorzec: każdy plik eksportuje funkcję budującą (`buildKron<Kategoria>Events()`), wołaną świeżo przy każdym `kronTrigger()` — `engine/kronika.js` dogrywa jej wynik do `KRON_EVENTS` jedną linią na plik, zero zmian w logice wyzwalania/losowania.
- Eventy rocznicowe (próg 5 sezonów, ok. 15 sztuk łącznie) oparte wyłącznie na polach `G`, które realnie przechowują sezon zdarzenia — m.in. `G.records.bestWin.season`, `G.stadium.hist[].season`, `G.cupHistory[].winner`, `G.flags.firstTitleSeason` — świadomie NIE zbudowano żadnej rocznicy na `G.records.maxWinStreak` i pokrewnych, bo te pola nie mają towarzyszącego sezonu (dokładnie ten sam błąd klasy co `a05`, tym razem uniknięty).
- Każdy event: warunek wyzwalania oparty na zweryfikowanym, realnie istniejącym polu `G` (żadnej wymyślonej liczby bez uzasadnienia), pełne tłumaczenie PL+EN od razu, kategoria/klucze i18n spójne z konwencją `kron_*`, parametr lambda nigdy nazwany `t` (zawsze `tr`/inne, żeby nie przesłaniać globalnej funkcji tłumaczeń). Kilkanaście eventów-„jednorazowych osiągnięć" (założenie fanklubu, budowa muzeum klubowego, pomnik) z trwałą flagą w `G.flags`, żeby się nie powtarzały w nieskończoność.
- Zweryfikowane po każdej sesji: `node --check` na wszystkich plikach Kroniki oraz sandbox Node (prawdziwe `core/i18n.js`+`core/state.js`+dany plik danych, w kilku sesjach też prawdziwy `engine/kronika.js` dla dostępu do helperów zamkniętego świata typu `kronFindSurplusPlayer`/`kronTransferIn`) na 4-10 syntetycznych stanach gry na sesję — każdy nowy event wyzwolony i przećwiczony na wszystkich 3 wyborach co najmniej raz, 0 błędów. Po drodze złapane i naprawione własne błędy testowe (nie w kodzie gry): zduplikowane id klubu w syntetycznej tabeli ligowej maskujące poprawne działanie warunków opartych na pozycji w tabeli, oraz brak klonowania stanu gry między wyborami w jednym przebiegu testowym, przez co realna mutacja z jednego eventu przeciekała do warunku kolejnego.
- Weryfikacja w prawdziwej przeglądarce/grze niewykonana w tym środowisku dla żadnej z 10 sesji.

3. Kronika Klubu — mechanizm rocznic: fanMemoryTrigger() świadomy typu wpisu (Sesja 11)
- Zamiast uczyć `fanMemoryTrigger()` czytać `G.cHist`/`G.flags` jako drugie, równoległe źródło danych (co pierwotny plan sam odradzał), zrealizowany rekomendowany wariant: `G.timeline` zostaje jedynym źródłem, a `tr01_training_accident`/`dc01_data_breach` (które ustawiają trwałe `G.flags.*`, czytane niezależnie przez dedykowane eventy rocznicowe z Sesji 1) dodane do `KRON_TIMELINE_WORTHY` — istniejący, scentralizowany mechanizm w `kronShowModal()` (zero nowego kodu) sam zaczyna wypychać ich wpisy na oś czasu.
- `fanMemoryTrigger()` (`engine/kronika.js`) rozpoznaje teraz 7 kategorii typu wpisu na osi czasu (`kronika_ignored_*`, `kronika_*` — cała seria ~23 eventów Kroniki na `KRON_TIMELINE_WORTHY`, `academy_debut`, `record_bought`, `record_sold`, `fan_favourite_sold`, `stadium_expand`) z osobnymi, dwuwariantowymi szablonami tekstowymi (14 nowych kluczy `tl_recall_*` w PL i EN) zamiast jednego uniwersalnego zdania dla wszystkiego. Każdy typ spoza tej listy (w tym każdy przyszły, jeszcze nienazwany) trafia bez zmian w dokładnie to samo zachowanie sentymentowe co przed tą sesją — zero regresji.
- Zweryfikowane w sandboxie Node z `Math.random` spinowanym na 0 (deterministyczne wymuszenie triggera) na 10 scenariuszach: każda z 7 nowych kategorii renderuje właściwy, kontekstowy szablon bez nierozwiniętych placeholderów, a 3 scenariusze z nieznanym typem poprawnie spadają do starego zachowania.

4. Kronika Klubu — łańcuchy międzysezonowe: utrwalenie wzorca + 3 nowe przykłady (Sesja 12, ostatnia)
- Audyt jedynego dotąd łańcucha (`ac18`/`ac19`, Sesja 2) potwierdził wzorzec jako gotowy do powielenia; spisana kanoniczna checklista w `PLAN_KRONIKA_ROZBUDOWA.txt` (stan wyłącznie w jednym kluczu `G.flags.<nazwa>`, stage 1 z wzajemnym wykluczeniem i wszystkimi 3 wyborami kontynuującymi łańcuch, stage 2 bez górnego limitu czasowego i z czyszczeniem flagi we wszystkich 3 wyborach — jedyna naprawdę krytyczna zasada bezpieczeństwa, odpowiednik klasy błędu `a05`: zapomniane czyszczenie = łańcuch nie do zabicia).
- Przy okazji znaleziony i udokumentowany nowy, prostszy wariant wzorca: zamiast śledzić konkretnego zawodnika przez zamknięty świat (`G.players.concat(G.retiredPlayers)`), rozstrzygnięcie może porównywać abstrakcyjny stan gry zapisany w chwili startu łańcucha z jego stanem bieżącym (poziom ligi, pojemność stadionu) — deterministyczne, zero ryzyka „zawodnik nie istnieje".
- 3 nowe przykładowe łańcuchy (6 nowych id, świadomie ponad pierwotny cel 150): `t20`/`t21` (`kronika-eventy-transfery.js`) — kapitan publicznie deklaruje lojalność, rok+ później sprawdzenie czy dotrzymał (wariant ze śledzeniem zawodnika); `cr09`/`cr10` (`kronika-eventy-crisis.js`) — rywal przepowiada kryzys, rozstrzygnięcie przez porównanie poziomu ligi wtedy i teraz; `cl10`/`cl11` (`kronika-eventy-club.js`) — miasto obiecuje pomoc przy rozbudowie stadionu bez terminu, rozstrzygnięcie przez porównanie pojemności stadionu.
- Zweryfikowane w sandboxie Node dla każdego z 3 łańcuchów: start stage'u 1 i poprawny zapis stanu przez wszystkie 3 wybory, poprawne wzajemne wykluczenie (stage 1 nie odpala się ponownie, gdy łańcuch aktywny), stage 2 rok później ze wszystkimi gałęziami rozstrzygnięcia, czyszczenie flagi we wszystkich wyborach stage 2 — 0 błędów po naprawieniu jednego błędu we własnym teście (zbyt niskie statystyki syntetycznego zawodnika).
- Finalny stan całego planu: 156 unikalnych eventów w `KRON_EVENTS` (150 z Sesji 1-10 + 6 z Sesji 12), scalanych z `engine/kronika.js` przez 10 plików danych + rozszerzony mechanizm rocznic. Pełna weryfikacja w przeglądarce całej serii Sesji 1-12 naraz oraz dalsza rozbudowa łańcuchów w pozostałych kategoriach zostawione jako zalecenia na przyszłość w `PLAN_KRONIKA_ROZBUDOWA.txt`.

5. Poprawka: pozycja w tabeli w Aktualnościach ignorowała bilans bramkowy
- Zgłoszenie gracza: po meczu wpis w Aktualnościach („🏆 Wygrana... Miejsce X") pokazywał lepszą pozycję niż realna tabela przy remisie punktowym z gorszym bilansem bramkowym — sama tabela (`ui`) liczyła poprawnie, błąd tylko w newsie meczowym.
- Przyczyna: `engine/match-engine.js` liczył pozycję do newsa przez `.sort((a,b)=>b.pts-a.pts)` bez dogrywki na bilans bramkowy — przy remisie punktowym sort JS (stabilny) zostawiał kolejność wg insercji, nie wg realnej tabeli, która wszędzie indziej w kodzie (~30 miejsc) sortuje `b.pts-a.pts||(b.gf-b.ga)-(a.gf-a.ga)`.
- Naprawione w dwóch miejscach tego samego pliku: obliczenie `_myPos` do newsa meczowego oraz analogiczne `_oppPos`/`_myPos2` używane do detekcji derbów — oba dostały brakującą dogrywkę na bilans bramkowy. Sprawdzone: `cup-engine.js` (pole `pts` to tam zupełnie co innego — średnie OVR do siewu pucharowego, nie punkty ligowe) i `match-ui.js` (pozycja przedmeczowa już liczona poprawnie) — celowo bez zmian.
- Zweryfikowane: `node --check` oraz syntetyczny skrypt Node odtwarzający remis punktowy w tabeli — stary sort dawał miejsce 1, nowy poprawne miejsce 3.

6. Nowa funkcja: „Nadchodzące wydarzenia" — zapowiedzi rocznic i rekordów klubowych w Aktualnościach
- Zlecenie gracza: dać graczowi konkretny, przewidywalny powód do powrotu do gry (np. zbliżająca się rocznica klubu, zawodnik bliski rekordu goli) — sesja zaczęta w trybie wyłącznie diagnostycznym (zero kodu), zakończona dwiema turami projektowymi z mockupami (osobna sekcja w Gabinecie vs. hybryda vs. tylko Aktualności) i jawną decyzją gracza: wyłącznie wpisy w istniejących Aktualnościach, bez nowej sekcji w UI.
- Diagnostyka przed kodem: potwierdzone grepem, że w `G` nie istnieje żadna „data założenia klubu" (staż liczony wyłącznie przez `G.season`, jak już w istniejących eventach rocznicowych `hs05`-`hs08`), natomiast `G.allTimeStats.players` (trwałe, nigdy nie przycinane) jest solidną podstawą pod rekordy goli/asyst/meczów, a emerytura zawodnika jest z natury losowa (dwie rozjeżdżające się tabele `retireChance` w `season-summary.js` i `match-post.js` — odnotowane, nienaprawiane w tej sesji), więc świadomie pominięta jako niemożliwa do pokazania jako pewna data.
- Nowy `engine/milestones.js` (`checkUpcomingEvents()`, wołane co tydzień z `advWeek()` w `engine/week-progress.js`, obok `kronTrigger()`/`fanMemoryTrigger()`): zapowiedzi rocznicowe (staż menedżera co 5 lat, pierwszy awans, najgorszy sezon, rekordowy transfer, wpis do Galerii Sław) na 3/2/1 sezon przed wydarzeniem, zbliżanie się do klubowego rekordu goli/asyst/występów (luka ≤10, przeliczane co tydzień), oraz rozbudowa stadionu przekraczająca okrągły próg pojemności w ostatnich 3 tygodniach budowy.
- Cztery helpery liczące rocznice (`hsFirstPromotionSeason` i in.), dotąd lokalne wewnątrz `buildKronHistoryEvents()` w `engine/kronika-eventy-history.js`, wyniesione na poziom pliku (czysty refaktor, zero zmiany zachowania), żeby mogły być reużyte przez nowy moduł zamiast pisać odpowiedniki od nowa.
- Zabezpieczenie przed zalaniem Aktualności powtórkami: `G.flags.upcomingAnnounced` (leniwie tworzone jak istniejące `G.flags.hallOfFame`, bez zmian w `initGame()`) pilnuje, żeby każdy próg (rocznica × liczba sezonów, zawodnik × typ rekordu, próg pojemności stadionu) ogłosić dokładnie raz.
- Zweryfikowane w sandboxie Node (`core/i18n.js`+`core/state.js`+prawdziwe pliki modułu): poprawna progresja „za 3/2/1 sezon" między sezonami, brak powtórnego ogłoszenia przy wielokrotnym wywołaniu w tym samym tygodniu/sezonie, poprawna luka rekordu (10) z wykluczeniem samego rekordzisty, próg pojemności stadionu — 0 błędów.

7. Nowa funkcja: „Odwieczny rywal" — automatyczne wykrywanie rywala z historii meczów + kronika rywalizacji (Sesja 13)
- Zlecenie gracza: automatyczne oznaczanie „odwiecznego rywala" na podstawie historii meczów (najwięcej pojedynków, najbardziej dramatyczne wyniki) i osobna mini-kronika tej rywalizacji, w pełni lokalnie w ramach save'a.
- Diagnostyka przed kodem (Zasada #7): potwierdzone grepem, że żadna trwała struktura head-to-head per klub dotąd nie istniała — `G.mHist` (mecze klubu gracza) i `G._mHistAI` (mecze AI-AI) są czyszczone przy starcie każdego nowego sezonu (`ui/season-summary.js`), a `_mHistAI` dodatkowo jest jawnie pomijane w `saveGame()` (`ui/news-bootstrap.js`, `SKIP`). Ustalone też ważne rozróżnienie: istniejące `G.rival` to rywal DERBOWY, losowany od nowa co sezon przez `assignDerbyPairs()` (`ui/world-board-render.js`, wprost skomentowane jako „arbitralne, brak danych geograficznych") — zupełnie inny, ortogonalny mechanizm, świadomie nietknięty.
- Zaproponowana i zaakceptowana definicja rywala — ścisłe sortowanie leksykograficzne bez wag wymyślonych na miejscu: 1) najwięcej rozegranych meczów (dosłowna treść słowa „odwieczny"), 2) przy remisie: najwięcej „dramatycznych" wyników, 3) przy kolejnym remisie: najbardziej wyrównany bilans (najmniejsze |W-P|). Próg min. 3 spotkań. „Dramatyczny" mecz = remis wyrównany w doliczonym czasie (ostatni gol w min≥90) LUB odwrócenie wyniku (strona, która w dowolnym momencie meczu prowadziła, kończy go bez zwycięstwa) — obie reguły policzalne z minutowego przebiegu goli już dostępnego w `allEvts` w chwili końca meczu.
- Wdrożone: nowe trwałe pole `G.h2hHistory` (obiekt keyowany po id klubu przeciwnika: `matches/w/d/l/gf/ga/dramatic/lastSeason/lastRnd/lastMyG/lastOppG`), aktualizowane w `engine/match-engine.js` dokładnie tam, gdzie liczony jest już `G.records.bestWin` (ten sam wzorzec leniwej inicjalizacji, ten sam brak resetu międzysezonowego) — obejmuje też mecze pucharowe, bo to wciąż realne spotkania z przeciwnikiem. Dwie nowe funkcje pomocnicze w `core/state.js`: `getNemesisClub()` (wybór rywala wg powyższych kryteriów) i `h2hIsDramaticMatch()` (detekcja dramatyzmu). Nowy plik `engine/kronika-eventy-rivalry.js` (kategoria `kron_cat_rivalry`, prefiks id `rv`) z dwoma eventami: `rv01_nemesis_named` (jednorazowe ogłoszenie tożsamości rywala — dodane też do `KRON_TIMELINE_WORTHY`) i `rv02_rivalry_milestone` (cykliczna rocznica co 5. spotkanie z rywalem) — stan „ogłoszono?"/„ostatnia rocznica" trzymany bezpośrednio na wpisie `G.h2hHistory`, celowo NIE w `G.kronika.flags` (to drugie zeruje się co sezon). Podłączony w `index.html` i scalany w `engine/kronika.js` tym samym wzorcem `buildKron*Events()` co pozostałe 10 kategorii.
- Przy okazji naprawiona realna niespójność w `ui/club-modal.js`: sekcja „BEZPOŚREDNIE STARCIA" w karcie klubu liczyła bilans wyłącznie z bieżącego sezonu (z `G.mHist`, dopasowanie po nazwie klubu) i zerowała się niezauważenie po każdej zmianie sezonu — teraz czyta trwałe `G.h2hHistory` po id klubu, plus dostała odznakę „⚔️ ODWIECZNY RYWAL" gdy karta dotyczy aktualnego nemesis.
- Zweryfikowane w sandboxie Node (`core/i18n.js`+`core/state.js`+`engine/kronika-eventy-rivalry.js` w jednym kontekście vm, żeby uniknąć realnych zapisów w grze): `h2hIsDramaticMatch()` na 5 scenariuszach (0-0 bez goli, utrzymana przewaga, comeback na wygraną, wyrównanie w doliczonym czasie, jednostronna wygrana bez zmiany prowadzenia), `getNemesisClub()` z progiem 3 meczów i tie-breakiem po liczbie dramatycznych wyników, oba eventy Kroniki (waga/body/wybory) na dwóch symulowanych meczach z narastającym `G.h2hHistory` — 0 błędów. `node --check` na wszystkich 6 zmienionych/nowych plików.
- Błąd złapany dopiero przy weryfikacji w przeglądarce (nie w sandboxie): `devSimMyMatch()` (`ui/dev-mode.js`, tryb deweloperski „symuluj sezony") ma własną, równoległą kopię logiki `G.records` zamiast dzielić kod z `match-engine.js::next()` — mój hak na `G.h2hHistory` siedział tylko w tym drugim miejscu, więc szybkie symulowanie sezonów w ogóle nie zasilałoby bilansu h2h. Naprawione dopisaniem lustrzanego bloku w `devSimMyMatch()` (ten sam wzorzec, jakim wcześniej zdublowano tam `G.records`), liczy się też mecz pucharowy, tak jak w prawdziwym meczu.
- Zweryfikowane w prawdziwej przeglądarce (Playwright + Chromium, lokalny serwer statyczny, zero zaślepek — realny przebieg gry): nowa kariera → 4 sezony rozegrane przez tryb dev → `G.h2hHistory` narosło organicznie do 20+ przeciwników (niektórzy z 8 meczami w tej samej lidze) → `getNemesisClub()` poprawnie wybrał klub z najwyższą liczbą meczów, rozstrzygając remis (kilku przeciwników po 8 meczów) liczbą wyników „dramatycznych" → karta klubu poprawnie pokazała odznakę „⚔️ ODWIECZNY RYWAL" tylko przy właściwym przeciwniku (inny, niedawno poznany klub — bez odznaki) → oba eventy Kroniki wywołane przez ten sam `kronShowModal()` co produkcyjnie, wszystkie 3 wybory sprawdzone (efekt reputacji +5, wpis w Aktualnościach z `kron_rv01_c3_news`, flagi `announced`/`lastMilestone` ustawione poprawnie) — zero błędów konsoli w całej sesji.
1. Kalendarz i Puchar → Drzewko: linki do kart klubów i do wyników rozegranych meczów
- Życzenie gracza: w Kalendarzu link do rozegranych meczów, w zakładce Puchar → Drzewko linki do drużyn i do wyników meczów.
- Diagnoza: „karta drużyny"/„wynik meczu" w grze to nie osobne strony/route, tylko modale — `openClubModal(clubId)` (`ui/club-modal.js`) i `showMatchDetail(mHistIdx,src)` (`systems/traits-history.js`), czytający `G.mHist` (mecze gracza) / `G._mHistAI` (mecze AI-AI). Problem: mecze pucharowe NIGDY tam nie trafiały — mecz gracza w pucharze pomijał `G.mHist.push(...)` (`engine/match-engine.js`, warunek `!G._cupMatchActive`), a mecze AI-AI w pucharze (`simCupRound()`, `engine/cup-engine.js`) liczyły tylko zbiorczy wynik, bez strzelców/kartek/ocen. Zapytany o zakres, gracz wybrał pełny wariant: dogenerować kompletne dane meczu pucharowego (jak w lidze), nie tylko goły link do drużyny.
- Wdrożone: mecz pucharowy gracza (`match-engine.js`) dopisuje wpis do `G.mHist` z KOŃCOWYM wynikiem (po ew. dogrywce/karnych z `resolveCupMyMatch()`, czytanym przez referencję do obiektu meczu, bo `resolveCupMyMatch()` zeruje `G._cupMatchActive` zanim wynik jest znany wywołującemu), oznaczony `_isCup`. Symulacja AI-AI w pucharze (`simCupRound()`, `cup-engine.js`) generuje teraz strzelców/asystentów/kartki/oceny tym samym „lekkim" wzorcem co `simOthers()` dla ligi i zapisuje je do `G._mHistAI`. Oba miejsca zapamiętują `mHistIdx`/`mHistSrc` bezpośrednio na obiekcie meczu w `G.cup.rounds[]`, więc `ui/gabinet.js` (`_kalBuildRows`/`_kalRowHtml` — Kalendarz) i `cup-engine.js` (`renderCupDrzewko()` — Puchar→Drzewko) mogą linkować każdy rozegrany wiersz do `showMatchDetail()` bez własnego wyszukiwania. Nazwa przeciwnika w Kalendarzu i nazwy obu drużyn w Drzewku linkują do `openClubModal()` niezależnie od tego, czy mecz już się odbył. `systems/traits-history.js` (`showMatchDetail()`) pokazuje nazwę rundy pucharu (np. „Finał") zamiast surowego numeru kolejki — nowy klucz i18n `ht_cup_round_season` (PL/EN, `core/i18n.js`).
- Link do wyniku działa tylko dla bieżącego sezonu (`cur.src==='live'` w Drzewku) — `G.mHist`/`G._mHistAI` są czyszczone na starcie nowego sezonu, więc w archiwum Pucharu (`G.cupHistory`) zostają same linki do drużyn, spójnie z tym samym ograniczeniem już istniejącym dla kart klubów.
- Przy okazji naprawiona realna niespójność, na jaką trafiono podczas wdrożenia: wyszukiwanie wyniku meczu ligowego po numerze kolejki w `ui/season-summary.js` (ekran podsumowania sezonu) mogło przypadkowo trafić na mecz pucharowy z tego samego tygodnia (obie rundy bywają oznaczone tym samym numerem — `CUP_WEEKS` pokrywa się z numeracją kolejek ligowych) i pokazać zły mecz po kliknięciu; dopisany warunek `!x._isCup` w obu wyszukiwaniach. Analogicznie zabezpieczona rekonstrukcja `G.cHist` ze starych zapisów w `traits-history.js` (`fillHistory()`), żeby nie liczyła goli/punktów meczu pucharowego jako ligowych.
- Zweryfikowane w Playwright (prawdziwe funkcje UI, nie ominięcia): pełny sezon, setki meczów AI, rozegrany mecz ligowy i mecz pucharowy gracza (przegrana 1:2) — oba poprawnie klikalne w Kalendarzu i w Drzewku, `showMatchDetail()` pokazuje właściwy wynik i etykietę rundy, zero błędów konsoli na całej ścieżce.

2. Ikonka „zagraj mecz" (FAB na pasku dolnym) — 5 zaproponowanych wariantów, wdrożony trójkąt play
- Życzenie gracza: zaproponować 5 wzorów ikonki meczu, trochę powiększonej na tle pozostałych ikon paska.
- Zaproponowane w interaktywnym podglądzie (Artifact, prawdziwe barwy/skala paska): piłka z wybitymi plamkami po pięciokątach, gwizdek sędziowski, dwie strzałki „na starcie", boisko z góry (linia środkowa + koło + pola bramkowe), trójkąt play. Gracz wybrał trójkąt play.
- Wdrożone (`index.html`, `.nav-fab`): ikona podmieniona na `<polygon>` — czysty trójkąt zamiast dotychczasowego pikselowego kształtu; rozmiar SVG podniesiony z 22px do 24px (FAB pozostaje 48px, ikony sąsiednie 20px — bez zmian w CSS).
- Zweryfikowane zrzutem ekranu w Playwright na realnym pasku po starcie kariery: trójkąt wyśrodkowany, wyraźnie większy od ikon KALENDARZ/SKŁAD/TABELA/WIĘCEJ, zero błędów konsoli.

3. Zakładka Więcej — etykiety kafelków ujednolicone z paskiem dolnym
- Życzenie gracza: w zakładce Więcej ten sam rozmiar i kolor czcionki etykiet co na dolnym pasku ekranu głównego.
- Zweryfikowane różnice: `.tile-lbl` (kafelki Więcej) miało `font-size:var(--fs-h3)` (11px) i `color:var(--gb)` (jaskrawa zieleń), a `.nav-btn span:last-child` (etykiety paska) ma `var(--fs-micro)` (10px) i `color:var(--gr)` (przygaszona zieleń-szarość, dziedziczony z `.nav-btn`). Klasa `.tile-lbl` (`css/style.css`) używana jest wyłącznie w zakładce Więcej (dawny `.tile-grid` na dashboardzie jest martwy, zastąpiony paskiem — Wariant B, wpis z 16.07), więc zmiana bezpieczna bez skutków ubocznych gdzie indziej.
- Naprawione: `.tile-lbl` — `font-size` na `var(--fs-micro)`, `color` na `var(--gr)`.
- Zweryfikowane zrzutem ekranu w Playwright: etykiety TRANSFERY/FINANSE/AKADEMIA/… wizualnie spójne z KALENDARZ/SKŁAD/TABELA/WIĘCEJ na pasku, zero błędów konsoli.

4. Zakładka Świat → Aktywne kluby — dane nie odświeżały się od razu po wejściu
- Zgłoszenie gracza: dane w zakładkach Świat/Historia powinny się aktualizować od razu po wejściu do panelu, nie dopiero po przejściu między pod-zakładkami.
- Diagnoza: `fillHistory()` (`systems/traits-history.js`) poprawnie sprawdza aktywną pod-zakładkę i renderuje właściwe dane — Historia nie miała tego buga. `fillWorld()` (`ui/world-board-render.js`) zawsze renderowała wyłącznie „Rekordowe transfery" (`renderWorldTransfers()`), niezależnie od tego, która pod-zakładka (Transfery/Aktywne kluby) była aktywna przy poprzednim wejściu — stan zakładek nie resetuje się przy zamknięciu panelu (`closePanel()` tylko zdejmuje klasę `.open`).
- Naprawione: `fillWorld()` sprawdza aktywny przycisk (`#world-tab-clubs`) i woła `renderWorldClubs()` albo `renderWorldTransfers()` odpowiednio, analogicznie do już poprawnej logiki `fillHistory()`.

5. Zakładka Więcej — nowa kolejność kafelków wg grup tematycznych
- Życzenie gracza: 3 propozycje kolejności kafelków w bottom sheet „Więcej"; wybrany wariant grupowania tematycznego.
- Wdrożone (`index.html`): TRANSFERY/FINANSE/TRENING → AKADEMIA/STADION/PUCHAR → ŚWIAT/HISTORIA/MENU (kadra i budżet → rozwój klubu i rozgrywki → treści przeglądowe/meta). Zmieniona wyłącznie kolejność istniejących kafelków — te same `onclick`/ikony/etykiety.

6. Audyt cech (traits) zawodnika — aktywne vs martwe w silniku meczowym + usunięcie „profesjonalista"
- Życzenie gracza: zweryfikować, które z 16 cech (`systems/traits-history.js`, `TRAITS`) realnie wpływają na wynik symulacji meczu, a które są kosmetyczne, i wskazać dokładny mechanizm dla każdej.
- Diagnoza: 7/16 cech w pełni aktywnych i symetrycznych gracz/AI (sprinter/artysta/snajper/mur — bonus atrybutu w `playerStr()`; lider/nerwowy/zimna_krew — bonus/kara formy post-mecz). 4/16 częściowo martwe: wytrzymały (tylko żywy mecz gracza, brak systemu kontuzji meczowych poza tym), szybki_start/słaby_start (pętla `myPl()` — tylko klub gracza), pewny_siebie (`G.winStreak` istnieje tylko dla klubu gracza). 1/16 w 100% martwa: „profesjonalista" — jedyne odwołanie to nigdy niewołana `getTraitEffect()` (`traits-history.js`). 4/16 poza silnikiem meczowym z zamysłu (pojęty/lojalny/twardy/żądny kasy — aktywne w innych systemach: trening/kontrakty/finanse).
- Wdrożone na życzenie gracza: cecha „profesjonalista" usunięta z gry — definicja i wpisy w pulach losowania (`traits-history.js`), klucze i18n (`core/i18n.js`), migracja w `loadGame()` (`ui/news-bootstrap.js`) zamieniająca tę cechę na nową losową u zawodników ze starych zapisów, którzy już ją mieli (żeby nie zostać z 2 cechami zamiast 3).

7. Symetria cech AI — Etap 1: szybki_start / słaby_start / pewny_siebie
- Wdrożone: `week-progress.js` — pętla bonusu/kary formy w pierwszych 5 kolejkach rozszerzona z `myPl()` na `G.players` (wszystkie kluby, nie tylko gracza). `match-post.js` — `pewny_siebie` dla AI wykorzystuje już istniejący per-klubowy licznik serii `cai._streak` (próg `>=3`, ten sam co `G.winStreak` gracza) zamiast tworzenia nowego pola.

8. Symetria cech AI — Etap 2: wytrzymały w trybie dev
- Wdrożone (`ui/dev-mode.js`, `devSimMyMatch()`): dodany rzut kontuzją pomeczową, ten sam wzór co `next()` w `match-engine.js`, dla obu drużyn meczu gracza — tryb dev do tej pory w ogóle nie miał systemu kontuzji meczowych.

9. Symetria cech AI — Etap 3: wytrzymały dla meczów AI-AI + bezpiecznik kadrowy
- Wdrożone (`match-post.js`, `simOthers()`): ten sam rzut kontuzją dla wszystkich ~63 meczów AI-AI symulowanych na kolejkę (8 lig × 16 klubów). Bezpiecznik na życzenie gracza: klub z mniej niż 16 zdrowymi (niekontuzjowanymi) zawodnikami jest pomijany w losowaniu — zapobiega powtórce znanego wcześniej problemu klubów AI zostających bez obsady na pozycji (`ensureClubGoalkeepers()`).
- Koszt wydajnościowy: pomijalny (~1400 dodatkowych `Math.random()` na tydzień) — `_buildMatchLite()` pozostaje jednym przybliżeniem Poissona, zmiana nie wymagała przejścia AI-AI na pełny silnik zdarzeniowy.

10. Nowa funkcja: Kapitan drużyny
- Życzenie gracza: wybór kapitana + bonus wynikający z roli. Sprawdzone pod kątem istniejących pól w `G` — jedyne odwołania do „kapitana" to lokalna, jednorazowa flaga narracyjna w dwóch zdarzeniach Kroniki (`kron.flags._s02captainId`, `engine/kronika.js`), nie trwały stan gry.
- Zaproponowane i zatwierdzone przez gracza: domyślny wybór kapitana = najwyższe MEN wśród starterów; bonus = odporność na spadek formy po porażce drużyny.
- Wdrożone: `ui/tactics-playercard.js` — `ensureCaptainValid()` (domyślny wybór/samonaprawa, gdy dotychczasowy kapitan wypadnie ze składu) i `setCaptain(id)` (ręczna zmiana); odznaka 🎖 w tabeli Skład meczowy (klikalna, tylko przy starterach) i w karcie zawodnika. `match-engine.js`/`match-post.js` — kapitan (gracz: `G.captainId`; AI: starter z najwyższym MEN, liczony na bieżąco, bez trwałego pola) dostaje +2 formy po porażce drużyny — ten sam mechanizm co cecha `zimna_krew`. `initGame()`/`loadGame()` (`ui/news-bootstrap.js`) inicjalizują/migrują `G.captainId`. Nowe klucze i18n PL/EN: `tac_captain_tooltip`, `tac_captain_set_notif`, `plr_captain_badge`. Bez nowych tokenów `--fs-*` — UI reużywa istniejący system typograficzny (`--fs-dense`/`--fs-h3`).
- Zweryfikowane (pkt. 4-10): `node --check` na wszystkich zmienionych plikach — zero błędów składni. Bez testu w przeglądarce/Playwright w tej sesji.

11. Karta klubu gracza — usunięta losowa „filozofia" AI (typ drużyny)
- Zgłoszenie gracza: w karcie klubu drużyny gracza nie powinien być widoczny typ drużyny (np. „grający wychowankami"/„drużyna transferowa") — klubem gracza zarządza sam gracz, więc etykieta filozofii AI nie ma sensu.
- Diagnoza: `ensureClubsHaveAI()` (`engine/match-post.js`) losuje każdemu klubowi, łącznie z klubem gracza, `club.ai.type` z `AI_TYPES` (akademia/sprzedający/bogaty/stabilny) — pole napędzające automatyczne decyzje transferowe AI, poprawnie pomijane przez całą logikę AI dla `G.myClubId`, ale mimo to ustawione i widoczne w UI. Wyciekało w dwóch miejscach: (1) `ui/world-board-render.js` (`renderWorldClubs()`) pokazywał ten losowy typ jako „filozofię" nawet w wierszu gracza (oznaczonym „TWÓJ”); (2) `ui/club-modal.js` — `openClubModal()` dla własnego klubu w ogóle nie wołał `_renderClubCard()` (wchodził od razu w zakładkę SKŁAD), więc zakładka KARTA KLUBU po ręcznym kliknięciu pokazywała pustą/nieaktualną zawartość pozostałą po ostatnio oglądanym klubie AI — niespójne z już istniejącym wzorcem dla zakładki „historia”, która dla własnego klubu świadomie przekierowuje do panelu gracza (`_cmGoToPlayerHistory()`).
- Zaproponowane i zatwierdzone przez gracza: w wierszu klubu gracza (Świat → Kluby) i w karcie klubu — zamiast typu AI stała etykieta „Zarządzany przez Ciebie”; poza tym karta własnego klubu ma pokazywać te same dane co karta klubu AI (statystyki składu, reputacja, historia transferów).
- Wdrożone: nowy klucz i18n `cm_managed_by_you` (PL/EN, `core/i18n.js`). `world-board-render.js` — `philo` dla wiersza `isMe` używa `cm_managed_by_you` zamiast `philoMap[ai.type]`. `club-modal.js` — `openClubModal()` dla własnego klubu teraz też woła `_renderClubCard()` z podmienionym `def` (`cm_managed_by_you` zamiast opisu AI) i reputacją z `G.reputation` (nie z „widmowego”, nigdy realnie nieaktualizowanego `ai.reputation` klubu gracza) — domyślna zakładka po otwarciu nadal SKŁAD, bez zmian. Nowy helper `_myClubTransferLog()` mapuje `G.fin.transfers` gracza do formatu `ai.transferLog` klubów AI (zakupy bez znanego klubu źródłowego pokazują „← Rynek”, sprzedaże pokazują realny klub docelowy), żeby zakładka KARTA KLUBU mogła reużyć istniejącego renderowania `_renderClubCard()` bez duplikowania kodu. Sekcje „cel zarządu” i „bilans H2H” (właściwe tylko dla AI) pozostają poprawnie ukryte dla własnego klubu — to już istniejąca logika (`isMyClub`).
- Zweryfikowane: `node --check` na wszystkich trzech zmienionych plikach — zero błędów składni. Bez przeglądarki (brak `chromium-cli`/Playwright w tym środowisku) — zamiast tego uruchomiony prawdziwy `openClubModal()` z `club-modal.js` w Node `vm` na sfingowanych danych gry: filozofia poprawnie pokazuje „Zarządzany przez Ciebie” (bez zdublowanej ikony), reputacja czyta `G.reputation` a nie `ai.reputation`, statystyki składu i historia transferów (kupno z „Rynek”, sprzedaż z realnym klubem) renderują się identycznie jak u klubu AI, zero wyjątków.


16.07.2026
1. Etap B — linkowanie klubu/zawodnika w newsach, Kronice, historii reputacji i na osi czasu
- Życzenie gracza: każda wzmianka drużyny lub zawodnika w tekście gry musi mieć link do jej/jego karty. Audyt: ~193 wywołań `addNews()` + 112+ podstawień `{name}`/`{rival}` w `engine/kronika.js` (plus dziesiątki w `systems/transfers.js`, `engine/week-progress.js`, `ui/tactics-playercard.js`) — za dużo miejsc do ręcznej edycji. Ustalono, że jedyne ujście tekstu bez obsługi HTML to `notif()` (pasek powiadomień, renderuje przez `textContent`) — wszystkie pozostałe (lista newsów, newsy ligowe, treść Kroniki, historia reputacji, oś czasu) renderują przez `innerHTML`.
- Wdrożone: nowy generyczny `linkifyNames(msg)` (`core/data.js`) zamienia każde wystąpienie nazwy klubu i pełnego imienia+nazwiska zawodnika na klikalny link do karty (`openClubModal`/`showById`) — uogólnienie dawnego `_linkifyClubNames` (był tylko dla klubów, tylko w newsach ligowych) o zawodników. Wołany WYŁĄCZNIE przy renderze, nie przy zapisie — surowy tekst w `G.news`/`outcome`/timeline zostaje czystym stringiem, więc `notif()` nigdy nie dostaje HTML-a i nie potrzebuje osobnego „odlinkowania". Dzięki temu zero zmian w ~193+11 miejscach, które budują newsy.
- Podłączony w: `ui/news-bootstrap.js` (`_newsItemHtml` — lista newsów gracza, `_repHistoryItemHtml` — historia reputacji), `ui/world-board-render.js` (`_worldNewsItemHtml` — newsy ligowe), `engine/kronika.js` (treść dialogu Kroniki), `systems/traits-history.js` (etykiety na osi czasu klubu).
- Usunięty martwy kod po podmianie: `newsClickable()` (news-bootstrap.js, kruchy prefix-match tylko dla zawodników), stary `_clubLink()`/`_linkifyClubNames()` (world-board-render.js, przeniesione i uogólnione do core/data.js).
- Przy okazji, na wyraźną prośbę: sprzątnięty niezwiązany, wcześniej istniejący martwy kod w `ui/navigation-squad.js` — dwie definicje `function advWeekPrep(){}` jedna po drugiej; pierwsza (odwołania do nieistniejącej zmiennej `n` i do usuniętej `newsClickable`) była całkowicie nieosiągalna przez hoisting deklaracji funkcji.

2. Naprawa: link zawodnika w Kronice otwierał kartę z opóźnieniem (dopiero po zamknięciu Kroniki)
- Zgłoszenie gracza po wdrożeniu pkt. 1: kliknięcie linku zawodnika w treści Kroniki nie pokazywało karty od razu, tylko po zamknięciu Kroniki.
- Przyczyna: karta zawodnika (`#p-player`) to panel z `z-index:200`, a modal Kroniki ma `z-index:9995` — karta faktycznie otwierała się natychmiast, tylko chowała się pod wciąż widocznym modalem Kroniki. Ten sam błąd czekał na historię reputacji (`modal-reputation`, z-index 900), bo tam też doszły linki w pkt. 1. Linki do klubu nie miały tego problemu — `openClubModal()` sam ustawia sobie `z-index:9999`, więc zawsze wychodzi na wierzch niezależnie skąd kliknięty.
- Naprawione tym samym wzorcem „return point", jakiego silnik już używał dla `modal-season-summary`/`modal-club-ai`: `engine/kronika.js` zapamiętuje bieżący event Kroniki (`window._kronCurrentEv`/`_kronCurrentBody`, czyszczone po wybraniu opcji), `ui/news-bootstrap.js` (`showById()`) wymusza zamknięcie `modal-kronika`/`modal-reputation`, żeby karta była widoczna od razu, `ui/tactics-playercard.js` (`_captureReturnPoint()`) rozpoznaje oba modale jako punkt powrotu, a `ui/navigation-squad.js` (`closePanel('p-player')`) po zamknięciu karty odtwarza dokładnie ten sam event Kroniki (bez ponownego rozwiązywania wyboru) albo ponownie otwiera historię reputacji.
- Zweryfikowane: `node --check` na wszystkich zmienionych plikach (core/data.js, ui/world-board-render.js, ui/news-bootstrap.js, engine/kronika.js, systems/traits-history.js, ui/tactics-playercard.js, ui/navigation-squad.js) — zero błędów składni.

3. Wariant B — nowa nawigacja główna (pasek dolny + Gabinet) i pxIcon()
- Zlecenie: diagnoza (bez zmian w kodzie), a po akceptacji wdrożenie nowego interfejsu z dwóch zatwierdzonych mockupów — pasek dolny 5 pozycji (Gabinet/Skład/Mecz jako FAB/Tabela/Więcej) zamiast dzisiejszego dashboardu z kafelkami, plus zestaw 13 zatwierdzonych ikon UI.
- **Część B**: `engine/pixelart.js` — `ICON_BITMAPS` (bitmapy 12×12) + `window.pxIcon(key,sc,color)`, ten sam wzorzec co `pxFace`/`pxCrest`/`pxKit`.
- **Część A**: nowy plik `ui/gabinet.js` — ekran Gabinet (skrót celu zarządu + `renderNews()` bez zmian w samym mechanizmie newsów), pasek dolny widoczny na dashboardzie (przykrywany przez panele dokładnie jak dawny tile-grid), bottom sheet „Więcej" z 8 pozycjami nieobecnymi na pasku (Transfery/Finanse/Akademia/Stadion/Świat/Puchar/Trening/Historia — Kronika i Zarząd świadomie NIE są tam pozycjami, zostają jak dziś: Kronika jako automatyczny popup, Zarząd wyłącznie w zakładce Finansów). Herb klubu w nagłówku (`pxCrest`) klikalny → `openClubModal()`. Scalono `p-squad`+`p-tactics`: Skład dostał 4. podzakładkę „Skład meczowy" (przeniesioną z dawnej wewnętrznej zakładki Taktyki), Taktyka spłaszczona do jednego widoku bez własnego przełącznika — oba panele zostały osobnymi, w pełni niezależnymi `.panel` (zero zmian w match-locku/i18n hook/przyciskach taktyki z ekranu meczu), segmentowany przełącznik Skład/Taktyka to tylko `closePanel()+openPanel()` między nimi. Usunięty martwy kod ujawniony scaleniem: `tacTab()`, klucze i18n `tac_tab_squad`/`tac_tab_settings`.
- **Poprawki po pierwszym wdrożeniu** (zgłoszenia gracza): ikonki paska/„Więcej" wróciły do dawnej, wielokolorowej konwencji inline SVG (kafelkowej) zamiast jednokolorowych `pxIcon` — `pxIcon()` zostaje tylko w segmencie Skład/Taktyka. Usunięty zdublowany duży przycisk „następny mecz" nad newsami (funkcja przejęta przez FAB) — w jego miejsce wąski, wciąż klikalny pasek tydzień/kolejka/rywal (`.gab-nextweek`, też same ID co dawny przycisk — zero zmian w `updateHdr()`).
- **Naprawa (3 warstwy tego samego problemu — pasek/arkusz "Więcej" rozjeżdżały się na desktopie / zostawały widoczne po zamknięciu)**: (1) `.navbar`/`.navbar-sheet`/`.navbar-sheet-scrim` miały `position:fixed` (liczone względem całego okna przeglądarki) zamiast `position:absolute` — na szerokim desktopie rozciągały się na cały ekran zamiast trzymać się ramki `#app-frame` (480px, wyśrodkowana) tak jak reszta gry. (2) `#v-game` miał tylko odziedziczone `.view{min-height:100vh}` bez górnego ograniczenia — długa lista newsów rozciągała cały widok, więc zamiast przewijać się wewnątrz Gabinetu przewijała się cała strona; naprawione `#v-game{height:100dvh;overflow:hidden}`. (3) najsubtelniejsza: `#v-game` wciąż nie miał `position:relative`, więc `overflow:hidden` z (2) i tak niczego nie przycinał — pasek/arkusz (position:absolute) pozycjonowały się względem `#app-frame` (dziadka, jedynego wcześniejszego przodka z ustawionym `position`), nie względem `#v-game`; przy stronie dłuższej niż jeden ekran "zamknięty" arkusz wyciekał widoczny poniżej paska. Naprawione dopisaniem `position:relative` do `#v-game`. Zweryfikowane w Playwright z celowo wymuszonym przepełnieniem strony (krótkie okno przeglądarki) — przed poprawką arkusz zostawał widoczny po zamknięciu, po poprawce poprawnie chowa się niezależnie od wysokości okna i długości listy newsów.

4. Wariant B — Kalendarz w miejsce Gabinetu na pasku dolnym + spójność ikony Skład
- Życzenie gracza po przeglądzie makiety: skoro aktualności są i tak domyślnym widokiem (bez potrzeby osobnego przycisku), zwolnione miejsce na pasku dostaje **Kalendarz** — czego dotąd nie było jako osobnego ekranu. Zaproponowane i zwizualizowane w interaktywnej makiecie (Artifact) przed wdrożeniem, z pytaniami o kształt ikony, brak podświetlenia aktywnej pozycji na starcie i zakres (ligowe + pucharowe) — wszystkie zatwierdzone.
- Wdrożone: nowy panel `p-kalendarz` (`index.html`) — sekcja „Najbliższy mecz" (wyróżniona, klikalna → `handleNextWeek()`) + pełny „Terminarz". `fillKalendarz()` (`ui/gabinet.js`) łączy `G.schedule` (mecze ligowe, `{h,a,rnd,done,hg,ag}`) z `G.cup.rounds` (mecze pucharowe — `h`/`a` to obiekty `{cid,name}`, inny kształt niż w schedule) w jedną listę posortowaną chronologicznie: runda ligowa 1 zawsze wypada w tygodniu 3 (`G.round` rośnie od tygodnia 3, `advWeek()` w `week-progress.js`) więc `week=rnd+2`, tygodnie rund pucharowych są stałe (`CUP_WEEKS` w `cup-engine.js`). Wynik kolorowany (zielony/szary/czerwony) z perspektywy własnego klubu, nie surowo home/away. Mecze pucharowe oznaczone tagiem PUCHAR.
- Przycisk GABINET usunięty z paska (nowa ikona kalendarza w tej samej, kolorowej konwencji SVG). Usunięty stały się martwym kodem po zniknięciu przycisku: `fillGabinet()`, `goNav('gabinet')`, klucz i18n `nav_gabinet` — skasowane.
- Dopisane klucze i18n (PL+EN): `nav_kalendarz`, `kal_title`, `kal_next_label`, `kal_schedule_label`, `kal_none`, `kal_none_next`, `kal_cup_tag`.
- Przy okazji naprawiona realna niespójność zgłoszona przez gracza: ikonka SKŁAD w segmencie Skład/Taktyka (wewnątrz panelu) renderowała się inaczej niż na pasku dolnym (`pxIcon()` jednokolorowy canvas vs kolorowe inline SVG na pasku od poprzedniej poprawki). Ujednolicone na statyczne inline SVG w obu miejscach (`index.html`, 4 wystąpienia w `p-squad`/`p-tactics`); usunięta stająca się zbędną funkcja `_initUiIcons()` z `ui/gabinet.js`. `pxIcon()`/`ICON_BITMAPS` zostają w `engine/pixelart.js` jako gotowa infrastruktura (Część B), obecnie nieużywana nigdzie w realnym kodzie.
- Przy wdrożeniu znaleziona i naprawiona literówka: budowanie HTML wiersza terminarza (`_kalRowHtml`) dawało nadmiarowy cudzysłów w atrybucie `class` (`<div class="kal-row"">`) — niepoprawny, choć nieszkodliwy wizualnie HTML; poprawione na jawne budowanie atrybutów `class`/`onclick`.
- Zweryfikowane w Playwright (prawdziwe kliknięcia, nie wywołania JS z pominięciem UI): panel otwiera się i zamyka poprawnie, pasek znika/wraca tak jak inne panele, 30 meczów ligowych i wstrzyknięty testowo mecz pucharowy renderują się poprawnie z właściwym tagiem i sortowaniem, zero błędów konsoli na całej ścieżce.

5. Naprawa: komunikat po akcji z aktualności nie zawsze trafiał na samą górę listy
- Zgłoszenie gracza: po wykonaniu jakiegoś działania z aktualności (np. wybór w Kronice, sprzedaż zawodnika, ustawienie planu treningowego) potwierdzający komunikat powinien pojawić się na samej górze listy, ale czasem lądował niżej.
- Przyczyna: `addNews()` (`ui/news-bootstrap.js`) dopisuje nowy wpis przez `G.news.unshift(...)`, więc sama tablica jest już chronologiczna (najnowszy pierwszy) — ale `renderNews()` resortowało newsy bieżącego tygodnia wg sztywnej tabeli priorytetu typu (`err`/`inj`:0 … `contract`:8), nie wg kolejności dodania. Świeżo dodany komunikat typu np. `ok`/`club`/`train`/`info` lądował pod starszym newsem tego samego tygodnia o „ważniejszym" typie (np. `err`/`budget`), zamiast na górze.
- Naprawione: usunięte sortowanie wg priorytetu dla listy bieżącego tygodnia (`curFiltered` w `renderNews()`) — newsy renderują się teraz w naturalnej kolejności chronologicznej z `G.news`. Kolor paska/ikona (`barColor`/`icons` w `_newsItemHtml`) nadal wizualnie odróżniają wagę wpisu. Sekcja „poprzednie tygodnie" (`_renderPrevList`) została bez zmian — tam priorytet w ramach zamkniętego historycznego tygodnia nadal ma sens. Przy okazji usunięta stająca się martwą zmienna `prio` w `renderNews()`.
- Zweryfikowane: `node --check` na `ui/news-bootstrap.js` — zero błędów składni.


15.07.2026
1. Karta klubu — historia od sezonu 1 zamiast tylko ostatnich sezonów, suwak sezonów na wykresach
- Zgłoszenie gracza: w zakładce Historia karty klubu tabela sezonów pokazywała tylko 10 najnowszych (odwrócona kolejność), wykres punktów per sezon tylko ostatnie 12, a dolna granica osi wykresu OVR była sztywno ograniczona do 20. Przy wielu sezonach (np. 40) nie dało się łatwo wrócić do sezonu 1.
- Naprawione (`ui/club-modal.js`): tabela sezonów pokazuje teraz wszystkie sezony rosnąco od S1 (bez limitu 10 i bez odwracania kolejności); dolna granica osi OVR obniżona z 20 do 10.
- Dodany suwak sezonów (`<input type="range">`, okno 12 sezonów) sterujący wspólnie trzema wykresami zakładki (OVR w czasie, Punkty per sezon, Transfery per sezon) — domyślnie pokazuje najnowsze sezony, przesunięcie w lewo cofa aż do S1. Skala osi (min/maks OVR, maks punkty, maks kwota transferu) liczona z pełnej historii, nie tylko z widocznego okna, żeby się nie „skakała" przy przesuwaniu.

2. To samo ściskanie wykresów w zakładce DATA i w tabeli historii ligi — audyt i naprawa
- Na wyraźną prośbę: sprawdzone, czy podobny problem (wykresy/tabele rosnące poziomo z liczbą sezonów) występuje gdzie indziej w grze. Znaleziony analogiczny, w praktyce gorszy przypadek: `ui/data-center.js` (zakładka HISTORIA → DATA) renderuje 5 wykresów (`dcBars()`, CSS `.dc-bars{display:flex}` + `.dc-bar-col{flex:1}`) bez żadnego `overflow-x:auto` — słupki nie są przewijane, tylko ściskane do szerokości ekranu; `G.cHist` jest przycinane do 30 sezonów przy zapisie gry, więc problem realny już po dłuższej rozgrywce, nie tylko teoretyczny. Tabela historii ligi (macierz drużyna×sezon, `ui/season-summary.js`, zakładka Historia→Miejsca) rosła poziomo podobnie, choć tam `overflow-x:auto` przynajmniej działał (przewijanie bez ściskania).
- Bezpieczne bez zmian: tabela kariery zawodnika (`ui/tactics-playercard.js`) — wiersze to sezony (układ pionowy), nie ma ściskania poziomego, suwak byłby zbędny.
- Wdrożone (na życzenie, dla wszystkich znalezionych miejsc): ten sam mechanizm okna+suwaka co w karcie klubu. `data-center.js` — wspólny suwak dla 4 wykresów `dcRenderWzrost()` (Reputacja, Pojemność stadionu, Budżet, Akademia) + osobny suwak dla wykresu Wartości kadry w `dcRenderKadra()`, okno 12 sezonów. `season-summary.js` — suwak dla tabeli drużyna×sezon (funkcje `_lgHistSeasonHdrs`/`_lgHistTeamRows`/`_lgHistSlide`, przy okazji usunięta martwa, nieużywana zmienna `lg2`).
- Nowy wspólny klucz i18n `cm_season_slider_label` (PL/EN) używany przez wszystkie trzy pliki.
- Zweryfikowane w headless Chromium (Playwright) z syntetyczną historią 20-25 sezonów: suwaki poprawnie przesuwają widoczne okno na wszystkich wykresach/tabelach jednocześnie, zero błędów konsoli.

3. Naprawa nakładających się etykiet na wykresach DATA przy oknie 12 sezonów
- Zgłoszenie gracza po weryfikacji pkt. 2: etykiety kwot (Budżet, Wartość kadry) nachodziły na siebie przy szerokości kolumny ~29-32px. Zmierzone w przeglądarce: pełny format `fmt()` ("1 870 000 €") → etykieta 86px, skrócony `fmtVal()` ("1.7 mln €") → 70px — oba nakładają się na wszystkich 11 sąsiednich parach z 12 słupków. Zmniejszanie okna nie rozwiązałoby problemu (potrzebowałoby ~4-5 słupków, żeby taki tekst się zmieścił).
- Naprawione (`ui/data-center.js`): nowy krótki format `_dcShortVal()` bez symbolu waluty (np. „1.7M", „700k" — jednostka jest już w nagłówku wykresu) użyty w etykietach słupków Budżetu i Wartości kadry. Zmierzona szerokość 31px → mieści się bez nakładania w tym samym oknie 12 sezonów. Dopisana jednostka „[€]" do nagłówka wykresu Wartości kadry (Budżet już ją miał).
- Przy okazji znaleziony i naprawiony drugi, mniejszy problem tego samego rodzaju: dopisek „⏳" do etykiety trwającego sezonu poszerzał ją o ~18px, co powodowało pojedyncze nałożenie na ostatnim słupku na wszystkich 5 wykresach (nie tylko kwotowych). Naprawione: `dcBars()` dostał nowy parametr `cur` — trwający sezon oznaczany teraz stylem słupka (obniżona przezroczystość + przerywana ramka) zamiast tekstem w etykiecie, zero kosztu szerokości.
- Zweryfikowane w przeglądarce z 25-sezonową historią: 0 nakładających się etykiet na wszystkich sprawdzonych wykresach (wcześniej 11/11 dla Budżetu i Wartości kadry).

4. Karta zawodnika — fikcyjne wpisy „Wolny agent" przed wejściem zawodnika do świata, historia nie od sezonu debiutu
- Zgłoszenie gracza: w karcie zawodnika pojawiał się „wolny agent" mimo że zasada zamkniętego świata (CLAUDE.md) mówi, że zawodnik wchodzi do gry wyłącznie jako junior albo w puli startowej — więc skąd wpis sprzed debiutu? Historia powinna zaczynać się od sezonu, w którym zawodnik faktycznie wszedł do świata (np. junior od S10 → historia od S10), nie od S1.
- Przyczyna (zweryfikowana w headless Chromium na prawdziwym kodzie gry): `fillHistoryGaps()` (`engine/match-post.js`, wołana po każdym zakończonym sezonie) uzupełniała brakujące wpisy historii pętlą od sezonu 1 do bieżącego, NIEZALEŻNIE od tego, kiedy zawodnik faktycznie powstał — dla juniora z debiutem w S10 dokładała 9 fikcyjnych wpisów „Wolny agent" (clubId:0) dla sezonów sprzed jego istnienia. Odtworzone empirycznie: zawodnik z realną historią S10-S20 dostawał tabelę z 24 wierszami (9 fałszywych + 11 prawdziwych + nagłówki) zamiast 14.
- Naprawione: pętla uzupełniania zaczyna się teraz od `entrySeason` — najwcześniejszego PRAWDZIWEGO (nie-placeholder) wpisu zawodnika, czyli jego faktycznego sezonu wejścia do świata (junior albo pula startowa `initGame()`). Zweryfikowane, że legalne uzupełnianie luk W TRAKCIE kariery (np. sezon bez zapisanych statystyk u gracza obecnego od S1) nadal działa poprawnie — zmieniony tylko punkt startowy pętli, nie sama logika wypełniania.
- Przy okazji: etykieta placeholdera była na sztywno po polsku („Wolny agent") niezależnie od języka gry — zamieniona na `t('plr_free_agent')`.
- Zgłoszenie „brak podsumowania mecze/gole/asysty przy 21 sezonach" (`ui/tactics-playercard.js`): zweryfikowane, że wiersz sumy „Σ TOTAL" w tabeli historii karty zawodnika już istnieje i liczy poprawnie (potwierdzone w teście: 308 M / 110 G / 44 A) — nie był usunięty, tylko trudny do zauważenia, bo powyższy błąd sztucznie wydłużał tabelę o kilkanaście-kilkadziesiąt fałszywych, zerowych wierszy „Wolny agent". Po naprawie tabela ma tylko realne sezony i wiersz sumy jest bezpośrednio widoczny na dole — nie było potrzeby budować nowego mechanizmu.

5. Karta zawodnika — zamrożona migawka zawodnika z zakładki Akademia, nieaktualny OVR bieżącego sezonu w historii
- Zgłoszenie gracza: po symulacji sezonów w trybie deweloperskim (dev-mode) i starcie nowej kariery (sezon 1, bez przeładowania strony) karta zawodnika potrafiła pokazać sezon z poprzedniej, dawno skończonej gry (np. S21) oraz inny OVR niż w nagłówku karty. Zweryfikowane na dwóch osobnych, potwierdzonych przyczynach (headless Chromium, prawdziwy kod gry) — nie na domysłach.
- **Przyczyna 1 („sezon 21 mimo sezonu 1")**: `systems/academy.js`, zakładka Akademia → Wychowankowie — dwie karty zawodników otwierały profil przez `onclick="showPlayer(JSON.stringify(p)...)"`, czyli zamrożoną migawkę zapisaną wprost w HTML w chwili renderowania panelu, zamiast świeżego wyszukania po ID (`showById()`, wzorzec używany wszędzie indziej w kodzie). Jeśli panel Akademii nie zostanie odświeżony po starcie nowej kariery, kliknięcie starej, wciąż obecnej w DOM karty pokazywało dane sprzed wielu sezonów. Odtworzone dokładnie w zgłoszonym scenariuszu: wyrenderowana karta w „starej" grze (S21) → podmiana `G` na nową karierę (S1, ten sam ID zawodnika) → kliknięcie tego samego, nieodświeżonego elementu DOM. Naprawione: oba miejsca używają teraz `showById(p.id)`/`showById(cur.id)` — zawsze świeże dane z bieżącego `G`, niezależnie od tego, kiedy panel był ostatnio renderowany.
- **Przyczyna 2 („inny OVR w karcie niż w historii")**: `ui/tactics-playercard.js`, `renderPlayerHistory()` — wiersz bieżącego sezonu (`_current`, tworzony przy transferze/debiucie) pokazywał zamrożony `ovr` z chwili utworzenia wpisu, podczas gdy nagłówek karty zawsze liczy OVR na żywo (`ovr(p)`) — w trakcie sezonu zawodnik trenuje i te dwie wartości się rozjeżdżały. Zmierzone: nagłówek 58 vs historia 49 przed poprawką. Naprawione: wiersz bieżącego sezonu liczy teraz OVR na żywo tak samo jak nagłówek — po poprawce 58 vs 58.
- Obie poprawki syntaktycznie i funkcjonalnie zweryfikowane w headless Chromium, zero błędów konsoli.

6. Likwidacja „kryzysu kadrowego" — prewencyjne minimum na pozycję przy sprzedaży + awaryjne przekwalifikowanie zamiast rynku ratunkowego
- Życzenie gracza: dla klubu gracza zamiast reaktywnego „kryzysu kadrowego" (blokada wejścia na mecz + wymuszony rynek awaryjny od AI) powinno być prewencyjne minimum liczby zawodników na pozycję, egzekwowane przy sprzedaży — sprzedaż poniżej minimum ma być po prostu zablokowana. Opcję kryzysu kadrowego usunąć całkowicie.
- Analiza: `ui/tactics-playercard.js` (`openSellModal()`) miał już częściową, niespójną ochronę („min 22 zawodników", „min 2 bramkarzy" na sztywno, bez pozostałych pozycji). Kluby AI mają od dawna właściwy mechanizm — `POS_QUOTA` (`core/data.js`: GK≥2, OBR≥6, POL≥6, NAP≥4), egzekwowany w `aiEvaluateSale()`. Ustalone z graczem (pytanie doprecyzowujące): jeśli mimo pilnowanego minimum kontuzje/zawieszenia jednocześnie wyłączą zbyt wielu zawodników na pozycji przed meczem, zamiast blokować mecz lub otwierać rynek — automatyczne przekwalifikowanie zdrowego zawodnika z innej pozycji na czas tego kryzysu.
- **Wdrożone**:
  1. `tactics-playercard.js`, `openSellModal()`: ad hoc limity zastąpione `POS_QUOTA[pos].min`, ta sama reguła co dla AI. Nowy klucz i18n `plr_notif_min_pos` (zastąpił `plr_notif_min_squad`/`plr_notif_min_gk`).
  2. `navigation-squad.js`: usunięte `checkSquadCrisis()`, `openFACrisis()`, `_faCrisisMissingHtml()`, `signFreeAgent()`, `closeFACrisis()` oraz panel `p-freeagents` (index.html) i cały powiązany rynek awaryjny.
  3. `autoFillSquadFromBench()` rozbudowana o krok 3: gdy uzupełnienie z ławki tej samej pozycji nie wystarcza, wstawia najlepszego zdrowego zawodnika z innej pozycji, z tymczasową zmianą `p.pos` (`p._tempPosOverride` pamięta oryginał) — cofane automatycznie na początku kolejnego przygotowania do meczu, gdy przestaje być potrzebne. Naturalny spadek jakości wynika z niedopasowanych atrybutów (`playerStr()` liczy wg `p.pos`) — bez ingerencji w silnik meczu (`match-engine.js` filtruje zawodników po `p.pos` wszędzie, więc tymczasowa zmiana automatycznie „po prostu działa").
  4. Posprzątane referencje: przycisk „Wolni Agenci" w banerze niekompletnego składu (`match-ui.js`), hook odświeżający panel kryzysu w `applyLang()` (`i18n.js`), martwe klucze i18n (`nav_crisis_*`, `fa_*`, `match_free_agents_btn`, `nav_notif_fa_signed`, `news_wa_signed`), dokumentacja modułu w `ARCHITECTURE.md`, oraz już wcześniej martwy (błędne id) blok czyszczący panel kryzysu w `fillPanel()`.
- Zweryfikowane w headless Chromium na prawdziwej grze (`initGame()`, nie atrapa): sprzedaż obrońców do dokładnie 6 (minimum) przechodzi, próba sprzedaży 7. — zablokowana (skład bez zmian, modal się nie otwiera). Scenariusz „2 zdrowych z 8 obrońców, formacja 4-4-2 wymaga 4" poprawnie przekwalifikował 2 zawodników z innych pozycji (POL→OBR, NAP→OBR) bez otwierania jakiegokolwiek rynku; po wyzdrowieniu kontuzjowanych kolejne wywołanie automatycznie cofnęło oba przekwalifikowania do zera. Zero błędów konsoli.

7. Karta klubu — zakładka Historia: bilans meczów, oś czasu trofeów, podzakładki Przegląd/Sezony, deduplikacja dla własnego klubu
- Życzenie gracza: trofea w karcie klubu były nieczytelne (płaski worek jednakowych chipów, bez kolejności sezonowej i bez rozróżnienia kolorem liga/puchar) — poproszone o 3 warianty wizualne, wybrany Wariant 2 (oś czasu). Doproszono też o bilans wygrane/remisy/przegrane. Przy przeglądzie osobnego panelu Historii gracza (`systems/traits-history.js`: Sezony/Rekordy/Zawodnicy/Dynastia) okazało się, że duża część tych danych (trofea, bilans meczów, tytuły/awanse/spadki, ranking najlepszych zawodników) już tam istnieje w bogatszej formie — dla własnego klubu dublowała się z kartą klubu. Uzgodnione dwuetapowe rozwiązanie (2 warianty pokazane, wybrany pierwszy, potem doprecyzowany drugi).
- **Wdrożone — Rozwiązanie 1 (`ui/club-modal.js`)**:
  1. Wiersz statystyk bez „Najlepsza/najgorsza pozycja" (nieużywane nigdzie indziej, klucze i18n `cm_stat_best_pos`/`cm_stat_worst_pos` usunięte) — zostają Tytuły ligowe/Puchary/Awanse/Spadki, w jednym rzędzie 4 kafli.
  2. Nowy blok „Bilans meczów" — wygrane/remisy/przegrane liczbowo i procentowo (tylko liga, spójnie z tabelą sezonów niżej) + pasek proporcji.
  3. Trofea jako chronologiczna oś czasu (najnowsze u góry) zamiast płaskiego worka chipów — sezon z tytułem ligowym i pucharem naraz łączy się w jedną kartę z plakietką „DUBLET", luki bez trofeów opisane tekstem („Sezon X: bez trofeum" / „Sezony X–Y: bez trofeów").
  4. Zakładka podzielona na dwie podzakładki: 🏆 Przegląd (staty, bilans, trofea, legendy) i 📅 Sezony (suwak + wykresy OVR/punkty/transfery + tabela sezonów), wzorem istniejących podzakładek w Zarządzie (`finZarzadInner`, `finance.js`).
- **Wdrożone — Rozwiązanie 2, po doprecyzowaniu przez gracza**: kliknięcie „Historia” na karcie WŁASNEGO klubu nie renderuje już nic w modalu — od razu zamyka modal i przenosi (`openPanel('p-history')`) do panelu Historii gracza, zakładka Dynastia. Wykresy OVR w czasie i punkty/sezon (wcześniej tylko w karcie klubu) przeniesione na górę zakładki Sezony w `traits-history.js`. Zakładka Dynastia dostała na górze dokładnie ten sam blok staty+bilans+trofea co Przegląd w karcie klubu AI, bez legend (mają własną zakładkę Zawodnicy) — istniejąca gablota trofeów i oś czasu z kamieniami milowymi (stadion/akademia/transfery/Kronika) zostały bez zmian, poniżej (decyzja gracza: dołożyć, nie zastępować).
- Techniczne: komponenty (`_cmSecHead`, `_cmStatsRowHtml`, `_cmRecordHtml`, `_cmTrophyTimelineHtml`, `_cmSquadOvrForSeason`, `_cmSeasonSliderHtml`) wydzielone jako funkcje globalne w `club-modal.js` i wołane też z `traits-history.js` — jedna implementacja zamiast dwóch kopii do pilnowania. Nowe klucze i18n (PL/EN): `cm_tab_overview`, `cm_record_title`, `cm_record_total`, `cm_trophy_double`, `cm_trophy_gap_single`/`cm_trophy_gap_range`.
- Zweryfikowane: `node --check` na obu plikach oraz uruchomienie właściwej logiki (`_renderClubHistory`, `renderHistSezony`, `renderHistDynastia`) w Node na zmockowanej 13-sezonowej historii z dubletem w S11 — poprawne grupowanie trofeów, procenty bilansu, przekierowanie własnego klubu bez renderowania zawartości, kolejność bloków w Dynastii (nowy Przegląd przed starą gablotą). Środowisko nie miało dostępnej przeglądarki/dev-servera do pełnej wizualnej weryfikacji w samej grze — zalecane doraźne sprawdzenie w przeglądarce.
- Dopisane po uwadze gracza: przekierowanie z karty własnego klubu do panelu Historii gracza nie wracało z powrotem do karty klubu po kliknięciu „◀ WRÓĆ" (lądowało na dashboardzie), łamiąc istniejącą w grze zasadę „wracamy do karty, z której weszliśmy w zakładkę" (ten sam wzorzec co powrót z karty zawodnika do modalu klubu, `window._clubModalReturn`/`_playerReturnTo`). Naprawione tym samym wzorcem: `_cmGoToPlayerHistory()` (`club-modal.js`) zapisuje punkt powrotu `window._historyPanelReturn={clubId,tab:'historia'}`; `closePanel('p-history')` (`navigation-squad.js`) go konsumuje i otwiera z powrotem modal klubu na zakładce Historia; `fillHistory()` (`traits-history.js`) czyści ten punkt na starcie każdego ŚWIEŻEGO otwarcia panelu, żeby normalne wejście w Historię z menu głównego nie zaczęło nieoczekiwanie wracać do karty klubu, także gdy panel zostanie po drodze zamknięty z pominięciem przycisku WRÓĆ (np. przejściem na inną zakładkę menu). Zweryfikowane w Node (trzy pliki załadowane razem w jednym kontekście `vm`) na 3 scenariuszach: pełny cykl karta→Historia→WRÓĆ→karta (wraca poprawnie), normalne wejście z menu i WRÓĆ (nie wraca do żadnej karty), „porzucone” przekierowanie zamknięte z pominięciem WRÓĆ + kolejne zwykłe otwarcie Historii (poprawnie samo-naprawia się, bez nieoczekiwanego powrotu).
- **Regresja znaleziona przez gracza zaraz po powyższym**: „z Dynastii WRÓĆ nigdzie nie wraca". Przyczyna: punkt powrotu wskazywał zakładkę `'historia'`, a ta zakładka dla własnego klubu nie renderuje już nic — tylko wywołuje `_cmGoToPlayerHistory()` ponownie. Efekt: `closePanel('p-history')` → `openClubModal(1)` → `cmTab('historia')` → `_renderClubHistory(1)` → znowu `_cmGoToPlayerHistory()` → `closeClubModal()`+`openPanel('p-history')`, czyli panel zamykał się i w tej samej chwili otwierał ponownie. Odtworzone wprost w Node przed poprawką (licznik wywołań `_cmGoToPlayerHistory` rósł z 1 do 2 po kliknięciu WRÓĆ, `p-history` zostawało otwarte zamiast się zamknąć). Naprawione: punkt powrotu wskazuje teraz zakładkę `'karta'` zamiast `'historia'` — jedyna sensowna zmiana, bo „historia" u własnego klubu nigdy nie jest realnym miejscem docelowym, zawsze tylko przekierowuje dalej. Po poprawce licznik zostaje na 1, panel Historii poprawnie się zamyka, modal klubu poprawnie wraca (zweryfikowane w Node z PRAWDZIWYMI, nie atrapowanymi, `openClubModal`/`cmTab` — poprzedni test tego nie złapał właśnie dlatego, że te dwie funkcje były tam atrapowane).

8. Legendy/rekordziści klubu znikali z listy emerytów (limit 200) — link do ich karty przestawał działać
- Zgłoszenie gracza: pytanie o sens limitu 200 emerytów (`G.retiredPlayers`) — postulat, żeby zawodnik z rekordem (najlepszy strzelec itd.) albo status legendy klubu był zawsze dostępny przez link, niezależnie od limitu.
- Zweryfikowane, błąd potwierdzony: `G.retiredPlayers` jest przycinany czysto wg świeżości — co sezon do 200 najnowszych (`engine/match-post.js`, `aiRenewContracts()`), a przy zapisie gry jeszcze mocniej: do 80 najnowszych (tylko ci, co grali u gracza), w trybie awaryjnym L1 do 40, w L2 do zera. Statystyki („najlepszy strzelec"/legenda) żyją osobno w `G.allTimeStats.players`, które NIGDY nie jest przycinane — więc rekord/tytuł legendy przeżywał zawsze, ale sam obiekt zawodnika (potrzebny do otwarcia karty) znikał wraz z resztą starych emerytów. Efekt: `ui/data-center.js` (lista Legend) renderowała link do karty niezależnie od tego, czy zawodnik dalej istnieje w danych — po wyrzuceniu z puli klik nie robił nic (`showById()` po cichu zwraca, gdy nie znajdzie zawodnika).
- **Wdrożone**: nowa funkcja `protectedRetireeIds()` (`core/data.js`, wczytywany przed `news-bootstrap.js`/`match-post.js`) zwraca zbiór id zawodników, którzy nie mogą zniknąć: legendy klubu (`legScore()`≥`LEG_THRESHOLD`, ta sama reguła co lista Legend) oraz rekordziści (top 5 strzelców/asystentów/liczby meczów + najdroższy sprzedany/kupiony, ta sama pula co „Zawodnicy" w Historii gracza). `LEG_THRESHOLD`/`legScore()`/`legTrophies()` przeniesione z `data-center.js` do `core/data.js`, żeby `protectedRetireeIds()` mogła z nich korzystać (zostają w globalnym zasięgu, `data-center.js` używa ich bez zmian). Wyjątek podłączony we wszystkich czterech miejscach przycinania: sezonowy limit 200 (`match-post.js`), zapis gry limit 80 (`news-bootstrap.js`), tryb awaryjny L1 limit 40, tryb awaryjny L2 (wcześniej pełne zerowanie — teraz zostają chronieni, zbiór z reguły mały, więc nie zagraża limitowi miejsca zapisu).
- Zweryfikowane w Node (sandbox `vm`, prawdziwy `core/data.js`): 250 emerytów, jeden „stary" (id poza oknem 200 najnowszych) ustawiony jako rekordzista wszech czasów — po przycięciu do 200 zostaje 202 (200 najnowszych + chroniony), chroniony obecny, zwykły „stary" spoza okna poprawnie usunięty. `node --check` bez błędów na wszystkich czterech zmienionych plikach. Pełna weryfikacja w przeglądarce (klik na legendę po wielu symulowanych sezonach) niewykonana w tym środowisku — zalecane doraźne sprawdzenie w grze.

9. Wydarzenie „Rywal buduje dynastię" (odpowiedź: inwestycja w akademię) — nowy prospekt nazywał się „30 30", niewidoczny w Akademii
- Zgłoszenie gracza: po wyborze odpowiedzi „Inwestujesz w akademię" w kronice X-04 news pokazał „Akademia wzmocniona! Reputacja: 88 (+10). 30 30 to nowy prospect." — imię „30 30" wygląda na błąd (opisowa treść zdarzenia — akademia gracza rośnie jako jego własna, wybrana odpowiedź na dominację rywala — jest zamierzona i poprawna, problem dotyczy wyłącznie zepsutego imienia nowego prospekta).
- Przyczyna (potwierdzona w kodzie, `engine/kronika.js`, zdarzenie `x04_dynasty_threat`, wybór C2): imię nowego prospekta budowane było jako `tStr('first')+' '+tStr('last')`. `tStr()` to NIE funkcja tłumaczeń (tę reprezentuje `t()`) — to zupełnie inna funkcja z `news-bootstrap.js`, licząca średni OVR składu wyjściowego danego klubu po `clubId`, z fallbackiem **30**, gdy nie znajdzie żadnego zawodnika danego klubu. Wywołana z literałami `'first'`/`'last'` (nie istniejący `clubId`) zawsze trafiała w fallback — stąd zawsze dokładnie „30 30", niezależnie od gracza czy sezonu.
- Drugi, poważniejszy błąd znaleziony przy tej samej okazji: obiekt nowego prospekta nie miał pól wymaganych przez resztę systemu Akademii (`systems/academy.js`) — brakowało `ovr`, `trainRate` (potrzebne przez `acceptProspect()` przy przyjęciu do składu — bez nich zawodnik dostałby atrybuty `NaN`) oraz, co ważniejsze, `status:'pending'` — lista Akademia → Przegląd pokazuje tylko prospektów z `status==='pending'` (`renderAcadPrzeglad()`), więc ten „nowy prospekt" nigdy nie pojawiał się na liście i nie dało się go ani zaakceptować, ani odrzucić przez UI. Trwał cicho w `G.academy.prospects` (licząc się do limitu 6 miejsc) bez żadnej możliwości interakcji.
- **Wdrożone**: obiekt nowego prospekta zbudowany teraz dokładnie tym samym wzorcem co `generateProspects()` w `academy.js` — imię przez `getUniqueName()` (ten sam generator co reszta gry), `id` przez współdzielony licznik `pid++` (`core/state.js`, ta sama pula id co wszyscy inni zawodnicy — zgodnie z zasadą zamkniętego świata z `CLAUDE.md`) zamiast `Date.now()+losowa`, dodane `ovr`, `trainRate` (ten sam rozkład losowania co w normalnej generacji) i `status:'pending'`. Usunięte nieużywane nigdzie indziej pola (`stars`, `fromAcademy` — ustawiane dopiero przy akceptacji, `_dynastyResponse` — nigdzie nieodczytywane), `seasonsInAcademy:0` też usunięte (patrz niżej).
- Przy okazji znalezione: zdarzenie `a05_prospect_burnout` wyzwala się warunkiem `p.seasonsInAcademy>=2`, ale żaden realny prospekt z `generateProspects()` nigdy nie dostaje pola `seasonsInAcademy`, a co ważniejsze — `generateProspects()` (wołane co sezon z `season-summary.js`) i tak kasuje WSZYSTKICH nieprzyjętych „pending" prospektów przy każdym nowym sezonie, zanim doda nową pulę. Prospekt fizycznie nie może przeżyć dłużej niż jeden sezon w obecnym modelu — warunek zdarzenia zakłada mechanikę, która w grze nie istnieje, więc `a05` nigdy się nie uruchamiał.
- **Poprawka wymagała decyzji projektowej** (dwie ścieżki: albo zmienić `academy.js` tak, żeby prospekci mogli przeżyć 2-3 sezony zanim wygasną — realna zmiana mechaniki w innym module, albo usunąć martwe zdarzenie skoro model „jeden sezon na decyzję" jest zamierzony). Zapytany wprost — wybrana: **usunięcie zdarzenia**. Wdrożone: cały blok `a05_prospect_burnout` (waga/tytuł/treść/3 wybory) usunięty z `KRON_EVENTS` w `engine/kronika.js`, wraz z powiązanymi kluczami i18n (`kron_a05_*`, `kron_note_a05_prospect_burnout`) w obu językach (PL/EN) w `core/i18n.js`. Zostawiony bez zmian klucz kategorii `kron_cat_academy` („🎓 AKADEMIA") — to ogólna, wielokrotnego użytku etykieta kategorii (jak `kron_cat_finance`/`kron_cat_crisis`), nie treść jednego zdarzenia, więc naturalnie się przyda, jeśli w przyszłości pojawi się inne zdarzenie akademii.
- Zweryfikowane: `node --check` bez błędów na `kronika.js` i `i18n.js`; wyszukiwanie `a05`/`seasonsInAcademy`/`kron_a05` w całym repo po zmianie nie zwraca już żadnych odwołań do usuniętego zdarzenia poza samym changelogiem.
- Zweryfikowane w Node (sandbox `vm`, prawdziwe `core/data.js`+`core/state.js`+`core/i18n.js`): wygenerowany prospekt ma realne imię (nie „30 30"), komplet pól odczytywanych przez `acceptProspect()` i `status:'pending'` — a więc pojawi się na liście Akademia → Przegląd i da się go zaakceptować/odrzucić normalną ścieżką. `node --check` bez błędów.

10. Audyt „każda wzmianka o drużynie/zawodniku ma być linkiem do jej karty" — wdrożona łatwa część
- Życzenie gracza: wszędzie, gdzie w tekście UI pojawia się nazwa drużyny lub zawodnika, ma być klikalny link do odpowiedniej karty (`openClubModal()`/`showById()`). Zlecony szeroki audyt całego `js/` (ui/engine/systems/core) pod kątem miejsc bez takiego linku.
- Audyt potwierdził: część gry już to robi poprawnie i posłużyła za wzorzec — `plrLink()` (`traits-history.js`), `showMatchDetail()` (oś czasu meczu), generyczny post-processor `_clubLink()`/`_linkifyClubNames()` (`world-board-render.js`), `clubCell()` w tabeli kariery (`tactics-playercard.js`, link przez `h.clubId` ze `stopPropagation`, żeby nie kolidować z innym onclick na wierszu). Znalezione miejsca podzielone na „łatwe" (id/clubId już dostępne w scope, tylko brak opakowania w link) i „trudne" (system newsów/Kronika — wymaga zmiany struktury danych, patrz punkt niżej, TODO na jutro).
- **Wdrożone (łatwa część, wzorzec wszędzie ten sam: cały wiersz linkuje już do jednej encji przez `onclick`, więc druga wzmianka w tym samym wierszu dostaje własny link z `event.stopPropagation()`, żeby oba działały niezależnie)**:
  1. `ui/season-summary.js` — rankingi ligowe Strzelcy/Asysty/Oceny (zakładka Tabela → drawer ligi): nazwa klubu pod nazwiskiem zawodnika była martwym tekstem (cały wiersz linkował tylko do zawodnika). Dodany lokalny helper `clubTagHtml(club)`, użyty w 3 miejscach.
  2. `ui/world-board-render.js` — lista „Top 100 transferów świata" (`renderWorldTransfers()`, zarówno podium jak i pozycje #4-100): kluby źródłowy/docelowy (`fromClub`/`toClub`) były skróconym martwym tekstem, mimo że cały wiersz już linkuje do zawodnika. Dodany lokalny helper `clubLinkShort(fullName,shortLabel)` (szuka klubu po nazwie w `ALL_CLUBS`). Reszta plików audytu w tym pliku (lista klubów, newsy świata) już poprawnie linkowała — `_linkifyClubNames()` jest tam stosowane centralnie w `_worldNewsItemHtml()`, więc dodatkowa zmiana nie była potrzebna.
  3. `ui/club-modal.js` — zakładka Wyniki (`_renderClubMatches()`): nazwa przeciwnika w każdym meczu nie linkowała do jego karty klubu (cały wiersz linkował tylko do szczegółów meczu). Dodany link po znalezieniu klubu po nazwie (`ALL_CLUBS.find`). Zakładka Newsy już poprawna (reużywa `_worldNewsItemHtml`).
  4. `systems/academy.js` — karta zawodnika, zakładka Akademia → Historia (`renderAcadHistoryTab()`, oś czasu sezonów wychowanka): nazwa klubu z `p.history` była martwym tekstem mimo dostępnego `h.clubId` (to samo pole, które `tactics-playercard.js` już wykorzystuje do linkowania w tabeli kariery) — teraz link, dokładnie tym samym wzorcem co `clubCell()`.
- **Sprawdzone i świadomie NIE ruszone**: `academy.js`, `renderAcadWychowankowie()` podzakładka „Historia" — oś czasu sezonów wychowanka tam czyta pola `h.ovr`/`h.m`/`h.g`/`h.a`/`h.club` z wpisów `G.academy.hist`, ale żadne miejsce, które tam faktycznie pushuje (`academy.js`, `transfers.js`, `season-summary.js`), nigdy nie ustawia tych pól (tylko `startOvr`/`releaseOvr`/`peakOvr`, `action`, itd.) — te pola są zawsze `undefined`, więc ta oś czasu już dziś nie pokazuje żadnych realnych danych, niezależnie od linkowania. To osobny, wcześniej istniejący bug (błędny model danych), nie coś złamane dzisiejszą zmianą — poza zakresem tego zlecenia, zgłaszam osobno, nie naprawiałem. Podzakładka „Odrzuceni" (rejected) świadomie zostawiona bez linku — odrzucony junior nigdy nie wchodzi do `G.players`/`G.fa` (zgodnie z zasadą zamkniętego świata z `CLAUDE.md`), więc link i tak byłby zawsze martwy.
- Zweryfikowane: `node --check` bez błędów na wszystkich czterech zmienionych plikach.
- **TODO na jutro — trudna część (system newsów klubu gracza + Kronika)**: `addNews(msg)` (`news-bootstrap.js`) przechowuje już złożony, płaski string bez `playerId`/`clubId` — sam news w `innerHTML` obsłużyłby link, ale ten sam string trafia też do `notif()`, który renderuje przez `textContent` (link w środku pokazałby surowe tagi HTML w popupie zamiast linku) — realny konflikt, nie kosmetyka. Wolumen: ~193 wywołania `addNews()` i 112+ użyć `.replace('{name}'/{rival}...)` w samej `engine/kronika.js`, plus dziesiątki więcej w `systems/transfers.js`, `engine/week-progress.js`, `ui/tactics-playercard.js`. Rekomendowane rozwiązanie (do wdrożenia): uogólnić już istniejący `_linkifyClubNames()` do wspólnej pary helperów w `core/data.js` — `linkifyNames(msg)` (jak `_linkifyClubNames`, ale też dla zawodników, wołany raz przed zapisaniem newsa/outcome'u zamiast edycji 200 miejsc osobno) + `stripLinks(msg)` (czyści HTML z powrotem do czystego tekstu, wyłącznie dla ścieżki `notif()`).

11. Cele Zarządu — termin przyjęcia ograniczony do 4. tygodnia sezonu
- Życzenie gracza: cel główny i opcjonalny Zarządu (`systems/board-goals.js`) dało się dotąd przyjąć w dowolnym tygodniu sezonu, bez żadnego ograniczenia czasowego. Wprowadzone ograniczenie: wybór możliwy tylko do 4. tygodnia sezonu włącznie.
- Sprawdzone przed zmianą: `checkBoardGoals()` (koniec sezonu) już wcześniej poprawnie no-opuje, gdy `b.mainGoal` jest `null` (`if(!b.mainGoal)return;`) — brak wybranego celu nie powodował i nadal nie powoduje żadnej kary, więc nowe ograniczenie nie wymagało dodatkowej logiki „co się dzieje, gdy termin minie bez wyboru" — gracz po prostu zostaje bez celu na ten sezon, tak jak dotąd w przypadku celu opcjonalnego.
- **Wdrożone**: nowa stała `BOARD_GOAL_DEADLINE_WEEK=4` (`systems/board-goals.js`). `selectMainGoal()`/`selectOptGoal()` odrzucają wybór po terminie (`(G.week||1)>BOARD_GOAL_DEADLINE_WEEK`) z komunikatem `notif()` (`board_notif_deadline`). W `ui/world-board-render.js`, `renderBoardCele()`: gdy termin minął i cel (główny lub opcjonalny, niezależnie) nie został jeszcze wybrany, panel zamiast przycisków wyboru (które i tak nic by już nie zrobiły) pokazuje komunikat o minięciu terminu (`board_deadline_passed`) — ten sam wzorzec co reszta sesji: żadnych „martwych" przycisków w UI. Nowe klucze i18n PL/EN: `board_notif_deadline`, `board_deadline_passed`.
- Zweryfikowane: `node --check` bez błędów na `board-goals.js`, `world-board-render.js`, `i18n.js`; test graniczny w Node (tydz. 1-4 → dozwolone, tydz. 5+ → zablokowane) potwierdza poprawność warunku odcięcia.

14.07.2026
1. Resztkowy spadek OVR świata (cz. 4) — szerszy bonus potencjału juniora AI
- Kontynuacja zgłoszenia o spadającym OVR świata: po poprawkach z 13.07 (tempo treningu, dopasowanie transferów do ligi) świat przestawał spadać w nieskończoność, ale stabilizował się wyraźnie PONIŻEJ poziomu sezonu 1 (symulacja 40-sezonowa: średnio -3 do -9 pkt/liga). Cel: równowaga jak najbliżej sezonu 1.
- Diagnoza: potencjał juniora (`calcPotential()`, data.js) liczy się jako `aktualny_OVR + wąski_bonus`, a junior wchodzi z OVR blisko dołu ligi — nawet przy szybkim treningu fizycznie nie ma dokąd rosnąć. Dwie testowane poprawki (poszerzenie `LEAGUE_POT` per liga + podniesienie startowego OVR juniora do środka pasma ligi) okazały się WADLIWE przy pełnej weryfikacji: poszerzenie `LEAGUE_POT` jest globalne dla `calcPotential()`, więc podbijało potencjał też już dojrzałej populacji sezonu 1, nie tylko juniorów; podniesienie startu juniora w połączeniu z tym dawało systemowy WZROST OVR świata o +4 do +9 pkt PONAD sezon 1 zamiast stabilizacji (każdy z ~150-200 juniorów/sezon startował już blisko szczytu ligi, z dodatkowym szerokim bonusem na to nałożonym). Obie wycofane.
- Wdrożone: start OVR juniora zostaje przy oryginalnym paśmie (`r(lgMin-3,lgMin+10)`, ogon ligi), `LEAGUE_POT`/`calcPotential()` bez zmian (nie dotyka reszty świata). Jedyna zmiana: potencjał juniora liczony teraz OSOBNO, z szerszym bonusem `r(10,18)` zamiast wąskiego per-ligowego (np. VIII liga: 3-10) — sufit wciąż `LEAGUE_POT[lvl].max`, ten sam co dla reszty ligi.
- Wynik (symulacja 40-sezonowa, 3 przebiegi uśrednione): VI/VII/VIII liga (6/7/8) praktycznie odzyskują poziom sezonu 1 (-1,2 / +0,1, znaczna poprawa względem -5,7/-3,0 sprzed zmiany), IV/V liga (3/4) też wyraźnie lepiej (-4,3/-4,6 z -6,9/-8,6). Premier Division i I Liga (1/2) zostają bez zmiany, ok. -8 do -9 pkt poniżej sezonu 1 — osobny efekt, patrz pkt. 2 niżej.

2. Resztkowa luka Premier Division/I Liga (cz. 5) — głębia składu, nie gwiazdy
- Diagnoza (rozkład OVR starterzy-vs-ławka w symulacji 40-sezonowej, nie tylko średnia całego składu): w L1/L2 STARTERZY (top 11/klub) trzymają się blisko poziomu sezonu 1, ale ŁAWKA/głębia składu (pozostali ~13 zawodników) spada wszędzie o 9-11,5 pkt — także w ligach, które w pkt. 1 wróciły do poziomu sezonu 1. Przyczyna: `mkLeaguePlayers()` (news-bootstrap.js) przy starcie gry przydziela OVR całym 24-osobowym składom wg klasy klubu (gradient słaby→mocny klub w tabeli), więc nawet głębia mocnego klubu startuje wysoko — ale od sezonu 2 każdy klub, niezależnie od własnej siły, rekrutuje juniora z tego samego, płaskiego pasma CAŁEJ ligi (`r(lgMin-3,lgMin+10)`). Mocne kluby tracą tę przewagę w głębi składu, gdy ich sezon-1 kohorta się starzeje i schodzi z boiska.
- Wdrożone: start juniora podniesiony TYLKO dla klubów wyraźnie mocniejszych niż typowa dla ligi (`avgOvr` klubu, już liczone w tym miejscu pętli) — `r(max(lgMin-3,avgOvr-25), max(lgMin+10,avgOvr-10))` zamiast płaskiego `r(lgMin-3,lgMin+10)` dla wszystkich. Słabe/średnie kluby (avgOvr blisko lgMin) dostają dokładnie to samo pasmo co wcześniej — to nie jest jednolite podniesienie całej ligi (które w pkt. 1 zalewało świat i podbijało OVR ponad sezon 1), tylko przywrócenie różnicy między klubami.
- Wynik (symulacja 40-sezonowa, 3 przebiegi uśrednione): L1 -8,9→-7,6, L2 -8,0→-5,7 — realna, ale częściowa poprawa. Reszta lig w granicach szumu (L3 -4,3→-3,5, L4 -4,6→-6,5, L5 -7,7→-7,3, L6 -6,4→-6,7, L7 -1,2→0,0, L8 0,1→-0,2). L1/L2 wciąż zostają ok. 6-8 pkt poniżej sezonu 1 — luka nie zamknięta w całości, prawdopodobnie dlatego, że osiągnięcie elitarnego OVR (85+) zajmuje więcej sezonów rozwoju niż niższe ligi, a emerytura (ten sam wiekowy próg dla wszystkich) zabiera zawodników zanim nowe pokolenie w pełni dorośnie — osobny mechanizm (tempo starzenia/czas kariery), nietknięty w tej poprawce.

3. Audyt stabilności OVR świata AI (100+ sezonów) — reputacja→jakość juniora, bonus mistrzowski z tłumieniem piętrzenia, doprecyzowanie archetypu `stabilny`
- Kontynuacja wątku "Resztkowy spadek OVR świata" (pkt. 1-2 wyżej) na wyraźną prośbę: pełny audyt z korytarzem stabilizującym (max -1%/10 sezonów, max -3%/50 sezonów względem sezonu 1), podwójną rolą reputacji (jakość juniora, obok istniejących ról), bonusem za mistrzostwo/Puchar na kolejny sezon, zróżnicowanymi archetypami transferowymi AI. Pełny raport diagnostyczny (lokalizacja mechanizmów, dane symulacyjne, mapa zależności, pliki do zmiany) w `js/AUDYT_STABILNOSC_OVR.md` — zostawiony w repo jako dokument referencyjny.
- **Metoda**: dwa niezależne przebiegi symulacji Node (prawdziwy, niezmodyfikowany kod gry w `vm.Context`), 100 sezonów każdy: (1) 10 klubów/liga bez Pucharu/okna zimowego, (2) 16 klubów/liga z pełnym silnikiem Pucharu i oknem zimowym — plus ablacja przyczynowa (30-sezonowe przebiegi izolujące każdą zmienną osobno: liczba klubów, okno zimowe, Puchar).
- **Odkrycie kluczowe dla kierunku wdrożenia**: te dwa przebiegi dały PRZECIWNE znaki (przebieg 1: 5/8 lig spada poniżej korytarza, głównie głębia/ławka składu; przebieg 2: wszystkie 8 lig rosną +17% do +37% do sezonu 100). Ablacja wykazała, że **to Puchar jest pojedynczą, dominującą przyczyną odwrócenia znaku** (nie liczba klubów, nie okno zimowe — oba prawie bez wpływu) — konkretnie bonus sukcesu w `clubDevMult` (`_topHalfR`/`_wonCupR`/`_finalCupR` w `aiSeasonalRefresh()`, kronika.js). Sprawdzone na wyraźne pytanie: sam bonus resetuje się poprawnie co sezon (brak literalnego buga sumowania) — piętrzenie jest efektem architekturalnym przez dwa TRWAŁE stany bez mechanizmu wygaszającego: reputację (bez limitu, bez spadku) i sam OVR (utrzymuje się z sezonu na sezon), więc sukces ułatwia kolejny sukces bez końca. Niezależnie od kierunku całego świata, we WSZYSTKICH wariantach symulacji `avgXIOvr` (czubek składu) rósł/spadał wolniej niż `avgSquadOvr` (cała kadra) — erozja głębi/ławki to osobny, stały wzorzec.
- **Wdrożone** (3 zmiany, zweryfikowane składniowo `node --check`, zgodnie z priorytetem wynikającym z ablacji — najpierw dźwignia o największym zmierzonym wpływie):
  1. **Reputacja → jakość juniora AI** (`aiTransferSeason()`, match-post.js): nowy `_repJuniorBonus` (rep≥500: +6, rep≥250: +3, rep<50: -2, inaczej 0 — ten sam próg co istniejące `_repTierR`) dodawany do losowanego OVR juniora. Nowa, dodatkowa rola reputacji — istniejące (przychody, tempo rozwoju przez `clubDevMult`, cele zarządu) bez zmian.
  2. **Bonus mistrzowski z tłumieniem piętrzenia** (`aiSeasonalRefresh()`, kronika.js): nowa, wyższa stawka `+0.20` do `_successMultR` za sam tytuł mistrzowski (osobna od `+0.10` za "górną połowę tabeli"), połączona z nowym mechanizmem tłumiącym: `ai._successStreak` (nowe pole, persystuje na `club.ai` między sezonami jak np. `_lastSeasonPos`) liczy kolejne sezony sukcesu z rzędu; bonus ponad 1.0 skalowany przez `1/(1+0.15*min(10,streak))` — jednorazowy sukces dostaje pełną stawkę, wieloletnia dominacja z rzędu dostaje coraz mniej (floor przy streak≥10, nigdy zero). Wdrożone RAZEM (nie osobno) z pkt. 1, zgodnie z rekomendacją z audytu — dodanie samego bonusu bez tłumienia pogłębiałoby właśnie odkryty mechanizm niekontrolowanego wzrostu.
  3. **Doprecyzowanie archetypu `stabilny`** (`AI_TYPES`, match-post.js) w stronę "stawia na doświadczenie" (Twoja decyzja: doprecyzować istniejący typ, nie dodawać piątego): `maxBuyAge` 30→33, `juniors` [1,1]→[0,1], `minUpgradeDelta` 1→2, `renewChance` 0,70→0,80. Nazwa/ikona/opis bez zmian.
- **Świadomie NIE wdrożone w tej sesji**: mechanizm różnicujący tempo rozwoju 23-27 lat między rdzeniem składu a głębią (erozja ławki, potwierdzona w KAŻDYM wariancie symulacji, niezależnie od kierunku Pucharu) — oznaczony w audycie jako średni priorytet, ale bez konkretnego, zmierzonego wzoru liczbowego do wdrożenia (tylko kierunek). Zgodnie z zasadą "nie zgaduj" z `CLAUDE.md`: wymaga osobnej rundy pomiaru przed kodem, nie zgadywania wartości. Też odłożona: wrażliwość L1/L8 (końce piramidy lig) na uproszczony wzór wyniku meczu w harnessie — wymaga weryfikacji przez `runDevSim()` w prawdziwej grze (prawdziwy silnik meczowy) przed jakąkolwiek zmianą punktową dla tych dwóch lig.
- Pliki zmienione: `engine/match-post.js` (`_repJuniorBonus`, `AI_TYPES.stabilny`), `engine/kronika.js` (`_isChampionR`, `ai._successStreak`, tłumienie), `ARCHITECTURE.md` (nowa dokumentacja reputacji per liga i `AI_TYPES` w sekcji `engine/match-post.js` — było nieudokumentowane). `core/data.js` świadomie nietknięty (ryzyko podbicia całej populacji, nie tylko juniorów/sukcesu — patrz pkt. 1 wyżej i ostrzeżenie w kodzie).
- Skrypty symulacyjne (Node, poza repo) i surowe wyniki zostały w scratchpadzie sesji — jak poprzednio, nie są częścią repo.

4. Weryfikacja powdrożeniowa pkt. 3 — **poprawka NIE działa zgodnie z zamierzeniem**
- Na wyraźną prośbę: ten sam wariant harnessu (16 klubów/liga, realny Puchar, okno zimowe), 100 sezonów, uruchomiony PONOWNIE po wdrożeniu pkt. 3, porównany bezpośrednio z liczbami "przed" z tego samego harnessu.
- **Wynik**: jedyna realna awaria korytarza sprzed wdrożenia (L8, sezon 10: -7,5%) **pozostała dokładnie bez zmian** — poprawka jej nie dotknęła. Jednocześnie wzrost OVR (formalnie nieograniczony w pierwotnych założeniach — brak sufitu poza cap 100 na zawodnika) jest **większy niż przed poprawką w 5 z 8 lig przy sezonie 50** (L1 +9,1%→+17,7%, L2 +17,0%→+26,9%, L3 +21,7%→+28,2%, L4 +22,9%→+29,7%, L5 nieznacznie). L6/L7 bez zmian/lepiej, L8 wyraźnie lepiej przy sezonie 50 (+5,2%→-1,3%), ale nie przy sezonie 10, gdzie problem realnie siedzi.
- Kontrola poprawności mechanizmu: `ai._successStreak` działa technicznie bezbłędnie (0 błędów na 128 klubach AI, rozkład streaków sprawdzony) — **to nie błąd implementacji, tylko błędny dobór wielkości bonusu**. Diagnoza: nowy bonus mistrzowski (+0,20) jest DWA RAZY większy niż to, co zastępował (mistrz był już w "górnej połowie tabeli" i dostawał +0,10) — przy 16 klubach/lidze na przestrzeni 100 sezonów tytuł często wędruje między różnymi klubami, więc dla większości sezonów to "pierwszy sukces" danego klubu, gdzie tłumienie (celowo, na Twoją prośbę) nie ogranicza pełnej stawki. Tłumienie chroni przed DYNASTIĄ (ten sam klub wygrywający wiele lat z rzędu), nie przed sumarycznym wzrostem światowym napędzanym przez wielu różnych zwycięzców pod rząd. Dodatkowo `_repJuniorBonus` (osobny kanał z pkt. 3.1) w ogóle nie ma tłumienia i premiuje te same, zwykle najczęściej wygrywające kluby.
- **Decyzja o korekcie czeka na Ciebie** (pełna analiza w `js/AUDYT_STABILNOSC_OVR.md`, sekcja 9.4) — dwie osobne kwestie: (1) czy większy wzrost jest w ogóle problemem do adresowania, skoro pierwotne założenia nie limitowały wzrostu; (2) L8/sezon 10 wymaga osobnej diagnozy, niezależnej od bonusu mistrzowskiego. Stan w repo zostaje bez dalszych zmian do czasu decyzji.

5. Korekta bonusu mistrzowskiego (+0,20→+0,15) i diagnoza L8, na Twoje polecenie
- Sprawdzone ponownie na wyraźne polecenie: żaden atrybut zawodnika nie może przekroczyć 100 nawet z bonusami — potwierdzone, że wszystkie miejsca modyfikujące atrybuty (`mkAttrs`, `aiSeasonalRefresh`, trening gracza, generator juniora, akademia) klamrują do 99, niezależnie od wielkości mnożnika. Zero zmian w kodzie potrzebnych, wymóg już spełniony z zapasem.
- `engine/kronika.js` (`aiSeasonalRefresh()`): bonus mistrzowski zmniejszony z `+0.20` na `+0.15` (wciąż wyraźnie wyższy niż `+0,10` za samą górną połowę tabeli, ale już nie podwojony). `node --check` przeszedł. Nowa symulacja weryfikująca tę konkretną wartość jeszcze nie uruchomiona.
- **Diagnoza L8 (VII Liga) — dedykowana instrumentacja (trajektoria sezon-po-sezonie, rozbicie juniorów wg progu reputacji, mini-ablacja)**: prawdziwa przyczyna uporczywego -7,5% w sezonie 10 to **jednorazowy "wstrząs bootstrapowy" przy przejściu sezon 1→2** — `mkPlayer()` losuje wiek startowego składu jednolicie `r(17,35)` niezależnie od ligi, więc każda liga startuje z grupą zawodników już w paśmie ryzyka emerytalnego (32+); ta grupa odchodzi na emeryturę jednorazowo i masowo przy przejściu do sezonu 2 (zmierzone w L8: 22 emerytury w jednym przejściu, potem ZERO przez sezony 2-15). Efekt dotyka wszystkich lig jednakowo w punktach absolutnych, ale L8 (brak typu `bogaty`, najluźniejsze standardy zastępstwa, najniższe pasmo juniora w całej grze) odczuwa go ~4× mocniej procentowo niż L1, bo ten sam absolutny spadek ląduje na dużo niższej bazie OVR (~24,5 vs ~76). **To efekt przejściowy, samoistnie wygasający** (L8: -9,5%→-9,2%→-9,5%→-6,8%→-7,2%→-3,7%→-1,1% w sezonach 1-7) — próg korytarza w sezonie 10 trafia w środek krzywej powrotu, nie w jej dno.
- Hipoteza o `_repJuniorBonus` jako przyczynie L8: częściowo trafna (L8 ma faktycznie więcej klubów z rep<50 niż wyższe ligi, więc więcej kar -2 do juniorów), ale NIE dominująca — mini-ablacja z wyzerowanym bonusem dała wynik sprzeczny z hipotezą (wariant bez bonusu wypadł gorzej), ale L8 ma tak niski OVR bazowy, że odczyt procentowy jest silnie zaszumiony pojedynczą próbą (ten sam nominalny wariant dał różne wyniki w różnych przebiegach tej sesji) — wkładu tego mechanizmu w problem L8 nie dało się dziś czysto wydzielić z szumu.
- **Rekomendacja, nie wdrożona**: jeśli chcesz domknąć ten punkt korytarza, kandydatem jest złagodzenie rozkładu wieku populacji startowej (`mkPlayer()`/`mkLeaguePlayers()`) proporcjonalnie do wrażliwości każdej ligi — adresowałoby przyczynę (wstrząs bootstrapowy) we wszystkich ligach, nie punktowo dla L8. Czeka na Twoją decyzję, czy efekt przejściowy w ogóle wymaga interwencji.

13.07.2026
1. Sezon 1: brak transferów AI do okna zimowego + bezpiecznik budżetowy AI
- Zgłoszenie gracza: zawodnicy FA na starcie gry, brak transferów w pierwszych kolejkach sezonu 1, brak transferów „za darmo" gdy klub nie ma funduszy. Analiza wykazała, że rozdział dodatkowej głębi składu (0-2 zawodników/klub) bezpośrednio do klubów na starcie zamiast osobnej puli FA to już wcześniej wdrożona, zamierzona zmiana (patrz pkt. 10, 12.07.2026, „zasada zamkniętego świata") — bez modyfikacji.
- Naprawione: `initGame()` (news-bootstrap.js) wołał `aiTransferSeason(false)` od razu na starcie kariery — kluby AI robiły pełną aktywność transferową (sprzedaż/kupno/juniorzy) jeszcze przed 1. kolejką sezonu 1. Usunięte; pierwsza aktywność transferowa AI w karierze pojawia się teraz dopiero przy otwarciu okna zimowego (runda 16), tak jak w każdym kolejnym sezonie (`aiTransferSeason(false)` nadal wołane przy zmianie sezonu w season-summary.js). Rynek transferowy gracza pozostaje otwarty od tygodnia 1 jak dotąd (nie ruszany).
- Naprawione: uzupełnianie składu klubu AI do preferowanego rozmiaru (krok w Fazie 3 `aiTransferSeason()`, match-post.js, wywołujący `aiSignReplacement`) nie sprawdzało w ogóle budżetu klubu — klub bez pieniędzy i tak „kupował" zawodnika, schodząc głęboko na minus. Rozdzielone na dwa kroki: prawdziwy kryzys składu (<18 zawodników) nadal może podpisać zawodnika mimo braku funduszy jako ostateczność (żeby klub AI nie został bez możliwości wystawienia drużyny), natomiast zwykłe uzupełnianie (18→24) wymaga teraz pokrycia w budżecie (`requireBudget:true`) — klub bez funduszy po prostu nie robi transferu.

2. Log transferów klubu pokazywał zbyt dużo pozycji „—" (za darmo) + błędna etykieta „→ FA"
- Zgłoszenie gracza: w historii transferów klubu (karta klubu → zakładka Karta, sekcja logu) zdecydowana większość wpisów miała cenę „—" zamiast kwoty, a część sprzedaży pokazywała „→ FA" mimo że zamknięty świat (pkt. 10, 12.07.2026) nie ma puli wolnych agentów.
- Przyczyna 1 (realny błąd zaokrąglenia): `calcValue()` (state.js) ma podłogę 500 i nigdy nie zwraca 0, ale dwa miejsca liczące cenę transferu AI (Faza 1 — wystawienie na sprzedaż, i `aiSignReplacement()` — realokacja) zaokrąglały wynik do pełnych tysięcy PO przemnożeniu przez losowy mnożnik, więc dla słabych/starszych zawodników blisko tej podłogi cena potrafiła spaść do 0. Naprawione: podłoga 500 dodana też po przemnożeniu, w obu miejscach (match-post.js).
- Przyczyna 2 (celowe 0, zmienione na decyzję gracza): transfer po wygasłym kontrakcie bez odnowienia (`aiRenewContracts()`, krok „wygasłe kontrakty", match-post.js) miał zawsze cenę sztywno 0 — to była zasada Bosmana (realny wolny transfer), ale generowała największy wolumen wpisów „—" w logu. Zmienione na symboliczną opłatę (10-20% wartości zawodnika, podłoga 500) — nowy klub musi mieć na nią pokrycie w budżecie, inaczej kandydat odpada, a przy braku jakiegokolwiek chętnego kontrakt automatycznie się przedłuża (istniejący fallback). Rzadkie wydarzenia fabularne Kroniki Klubu (pkt. C, kronika.js) zostawione bez zmian — to osobny, świadomie darmowy mechanizm narracyjny, znikomy wolumen.
- Przyczyna 3 (błąd wyświetlania, nie mechanizmu): etykieta „→ FA" zamiast „→ 🏁" przy emeryturze zawodnika AI. Log emerytury (match-post.js) zapisywał `toClub:null` i liczył na to, że karta klubu (club-modal.js) znajdzie żywy obiekt gracza po `playerId` i sprawdzi `status==='retired'` — ale zapis gry (news-bootstrap.js) przycina `G.retiredPlayers` do zawodników, którzy grali w klubie gracza, więc AI-owy emeryt po wczytaniu zapisu znikał z danych i log mylnie pokazywał „→ FA", sugerując istnienie puli wolnych agentów, choć w rzeczywistości zawodnik po prostu zakończył karierę. Naprawione jawną flagą `retired:true` zapisywaną wprost na wpisie logu — nie zależy już od tego, czy zawodnik nadal istnieje w danych.

3. Kluby AI w słabszych ligach kurczyły się poniżej minimum składu (VII liga: spadek do ~14 zawodników po 10 sezonach)
- Zgłoszenie gracza: ustalić min/max liczby zawodników na pozycję i całościowo dla klubów AI; dla gracza jedna całościowa liczba max, bez wymuszonego minimum ani rozkładu pozycyjnego.
- Wprowadzona jedna wspólna stała `SQUAD_SIZE={min:18, target:24, max:30}` (data.js) zastępująca rozrzucone po match-post.js literały (25/40/22-25 zależnie od miejsca) — `min` to suma `POS_QUOTA.min` (GK 2, OBR 6, POL 6, NAP 4).
- Przyczyna 1: krok „wygasłe kontrakty" w `aiRenewContracts()` (match-post.js) — najbardziej masowa ścieżka odpływu zawodników AI — w ogóle nie sprawdzał, czy odejście zepchnie klub źródłowy poniżej `SQUAD_SIZE.min` lub poniżej `POS_QUOTA.min` na danej pozycji (w odróżnieniu od dobrowolnej sprzedaży, gdzie `aiEvaluateSale()` już to pilnował). Naprawione: taki sam warunek dodany przed przeniesieniem; jeśli złamałby minimum, kontrakt automatycznie się przedłuża (istniejący fallback), zamiast wywozić klub poniżej progu.
- Przyczyna 2: naprawa „kryzysu składu" (<18 zawodników, wprowadzona 13.07.2026 pkt. 1) ograniczała dosyp do sztywnych 2 zawodników na sezon niezależnie od realnego niedoboru — klub na 12 zawodnikach i tak dostawał tylko +2, nigdy nie odrabiając różnicy. Naprawione: liczba dosypywanych zawodników domyka teraz faktyczny deficyt do `SQUAD_SIZE.min` (pętla w `aiSignReplacement` i tak zatrzymuje się na `targetSize`, więc to nie ryzykuje przesady).
- Dla gracza: nowy pojedynczy sufit `SQUAD_SIZE.max` (30) przy kupnie z rynku (`doBuy()`, transfers.js), podpisywaniu skautowanego talentu (`signTalent()`, transfers.js) i awaryjnym podpisaniu z panelu kryzysu kadrowego (`signFreeAgent()`, navigation-squad.js) — bez wymuszonego minimum ani wymogu pozycyjnego, gracz decyduje sam o liczbie i pozycjach w tym zakresie. Istniejący blok sprzedaży (min 22 zawodników, min 2 bramkarzy, tactics-playercard.js) zostawiony bez zmian na wyraźną prośbę.

4. Spadający OVR świata AI, cz. 3 — kluby importowały zawodników za słabych na swoją ligę + trening AI 4-9× wolniejszy niż u gracza
- Zgłoszenie gracza: czy kluby AI kupują zawodników odpowiednich do swojej ligi, czy z za niskim OVR; następnie (w toku analizy) prośba o sprawdzenie, czy wzrost przez trening w klubach AI nie jest za niski względem treningu gracza.
- Metoda weryfikacji: izolowana symulacja 16-sezonowa w Node (prawdziwy kod gry — match-post.js/kronika.js/data.js/state.js załadowane w `vm.Context` ze stubami DOM, syntetyczny świat 8 lig × 10 klubów), z opomiarowaniem każdego transferu AI (`aiTransferPlayer` — jedyne miejsce, przez które przechodzi każdy transfer) oraz z dekompozycją sumy OVR świata na sezon (trening/starzenie vs emerytury vs juniorzy).
- Przyczyna 1 (dopasowanie transferów): krok „wygasłe kontrakty" (`aiRenewContracts()`, match-post.js) — **>50% całego wolumenu transferów AI w świecie** — dobierał klub docelowy wyłącznie po zasięgu poziomu ligi (±2), wolnym miejscu i budżecie, bez żadnego sprawdzenia OVR względem ligi docelowej. W symulacji 40% transferów tą ścieżką lądowało poniżej dolnej granicy ligi docelowej (20% z całego wolumenu >5 pkt poniżej). Naprawione: dodana ta sama tolerancja co w `aiSignReplacement()` (`band[0]=lgMin-5`) — kandydat na klub docelowy odpada, jeśli zawodnik miałby tam OVR >5 pkt poniżej dolnej granicy tej ligi; brak pasującego kandydata → kontrakt automatycznie się przedłuża (istniejący fallback). Wynik: głębokie niedopasowania (>5 pkt poniżej granicy) spadły z 20% do ~0% transferów w symulacji.
- Przyczyna 2 (dominująca — tempo treningu AI): dekompozycja sumy OVR świata pokazała, że trening AI (`aiSeasonalRefresh()`, kronika.js — wzrost <22 lat i 23-27 lat) dawał netto zaledwie ~70-180 pkt/sezon na ~2000 zawodników świata — praktycznie nie licząc się przy odpływie emerytur (~-5000 pkt/sezon). Monte Carlo obu ścieżek (ta sama formuła co week-progress.js dla gracza) pokazało dlaczego: dawne `r(1,2)`/sezon (<22 lat) i `r(0,2)`/sezon (23-27 lat) dawały AI ~1-1,5 pkt/sezon, podczas gdy gracz z aktywnym fokusem na NOR dostaje ~8-9 pkt/sezon — **4-9× szybciej**, mimo że to sam mechanizm rozwoju (potencjał, `calcPotential`) miał być wspólny dla całego zamkniętego świata. Naprawione: `r(1,2)`→`r(6,10)` (<22 lat), `r(0,2)`→`r(4,8)` (23-27 lat), oba dalej skalowane przez `clubDevMult` (filozofia klubu × reputacja × sukces sezonu) — przy clubDevMult=1,0 AI dostaje teraz ~8 pkt/sezon (20 lat) / ~6 pkt/sezon (24 lata), tego samego rzędu co gracz na NOR, wciąż nieco niżej (AI rozrzuca punkty po WSZYSTKICH 6 atrybutach zamiast 2 atrybutów fokusu, więc efektywny wpływ na OVR jest i tak skromniejszy niż u aktywnie zarządzanego gracza).
- Wynik łączny (symulacja 16-sezonowa, ten sam syntetyczny świat): trend spadku OVR wyraźnie spłaszczony, szczególnie w pierwszej połowie symulacji (Liga 1: poprzednio ~74→60 w 16 sezonów, po poprawce ~74→69, ze stabilnym płaskim odcinkiem sezony 3-9). Resztkowy spadek w dalszych sezonach wynika już głównie z naturalnej przewagi odpływu emerytów (~5000 pkt OVR/sezon) nad dopływem juniorów (~3500 pkt OVR/sezon) — osobny, mniejszy efekt, zostawiony bez zmian w tym przebiegu (juniorzy i emerytura były już tuningowane w pkt. 13-14, 12.07.2026).

12.07.2026
1. Zakładka WYNIKI w karcie klubu — mecze otwierały się „z opóźnieniem"
- Przyczyna: modal klubu jest przy każdym otwarciu przenoszony na koniec <body>, więc przy tym samym z-index co overlay meczu zawsze lądował nad nim — klik na mecz otwierał overlay ukryty pod wciąż widocznym modalem klubu. Naprawione: klik na mecz najpierw zamyka modal klubu (closeClubModal()), dopiero potem otwiera szczegóły meczu.

2. Oceny zawodników w szczegółach meczu — podpis drużyny + naprawiony brakujący skład rywala
- Sekcja ocen podzielona na dwie grupy z nagłówkiem nazwy drużyny (gospodarz/gość) zamiast jednej wymieszanej listy bez podpisu — dotyczy meczów AI-AI i gracz-AI.
- Złapany przy okazji poważniejszy błąd: mecz gracz-AI zapisywał w historii (G.mHist) oceny WYŁĄCZNIE własnych zawodników (filtr myPl() w match-engine.js) — oceny rywala AI w ogóle nie trafiały do zapisu, więc w historycznym widoku meczu połowa ocen zawsze była pusta. Naprawione zapisem obu drużyn, tak jak już działo się to dla meczów AI-AI.

3. Zakładka Tabela → Wyniki — można teraz otworzyć też mecze AI-AI
- Wcześniej klikalne były wyłącznie mecze z udziałem klubu gracza (lgRefreshWyniki() w season-summary.js); mecze AI-AI nie miały żadnej akcji. Dodane wyszukiwanie wpisu w G._mHistAI i otwarcie tych samych szczegółów meczu.

4. Powrót do widoku meczu po otwarciu karty zawodnika ze strzelców/ocen
- Linki do zawodnika w szczegółach meczu zamykały overlay meczu PRZED otwarciem karty zawodnika, więc mechanizm zapamiętywania „skąd wróciłem" (_captureReturnPoint()) nie widział już overlayu jako otwartego i po zamknięciu karty gracz lądował w złym miejscu (np. w tabeli ligi). Naprawione: overlay zamyka się dopiero PO otwarciu karty (showById() w news-bootstrap.js), tym samym wzorcem co już istniał dla modalu klubu/podsumowania sezonu.

5. News o wyniku meczu — przycisk „ZOBACZ MECZ" + naprawiony brak natychmiastowego wyświetlania
- Wiadomość o wyniku Twojego meczu w aktualnościach dostała przycisk prowadzący wprost do szczegółów tego meczu.
- Złapany własny błąd po drodze: addNews() renderuje listę od razu w środku siebie, a pole action/actionLabel dopisywało się do newsa DOPIERO po tym wywołaniu — pierwszy render wychodził bez przycisku i pojawiał się dopiero po przypadkowym kolejnym odświeżeniu listy. Naprawione dodatkowym renderNews() po ustawieniu action.

6. Przebudowa „PRZEBIEG MECZU" — wydarzenia pogrupowane per drużyna
- Zamiast jednej chronologicznej listy z tagiem drużyny przy strzelcu: dwie kolumny — wydarzenia gospodarzy po lewej, gości po prawej, każda z nagłówkiem nazwy drużyny i własną chronologią (gole + kartki). Kartki (bez zapisanej wprost drużyny w danych) przypisywane po aktualnym klubie zawodnika, tak jak już robiła to sekcja ocen.
- W kartach ocen doszły ikonki ⚽/🅰️ przy nazwisku — liczba ikonek odpowiada liczbie goli/asyst danego zawodnika w tym meczu.

7. Karta zawodnika: zakładka NAGRODY nie odświeżała się od razu
- renderPlayerAwards() był wołany wyłącznie przy ręcznym kliknięciu zakładki, w przeciwieństwie do zakładki HISTORIA, która odświeża się przy każdym (re)renderze karty. Efekt: świeża nagroda (np. MVP) nie pojawiała się, dopóki gracz nie przełączył zakładki i nie wrócił. Naprawione — nagrody renderują się teraz zawsze przy otwarciu/odświeżeniu karty, tak samo jak historia.

8. Powrót z karty klubu otwartej z poziomu karty zawodnika
- Audyt całego mechanizmu powrotu (karta klubu/gracza/mecz/podsumowanie sezonu) wykrył jeszcze jedną lukę: klik na nazwę klubu w karcie zawodnika zamykał kartę zawodnika PRZED otwarciem karty klubu, więc karta klubu nie miała zapisane, skąd przyszła — zamknięcie lądowało na przypadkowym wcześniejszym panelu zamiast z powrotem na karcie zawodnika. Naprawione dopisaniem tej ścieżki do window._clubModalReturn/closeClubModal(), tym samym mechanizmem co istniejący już powrót karta klubu ↔ szczegóły meczu.

9. Diagnoza i naprawa spadku OVR klubów AI — rozwój, transfery, limity pozycji
- Diagnoza (symulacja wieloseznowa w izolowanym środowisku, nie w grze): OVR klubów AI systematycznie spadał, bo `aiSeasonalRefresh()` (kronika.js) miał starzenie 28+ i wzrost ≤22 lat, ale zupełną lukę 23-27 lat (wiek świetności zawodnika stał w miejscu), a AI w ogóle nie miało własnego treningu.
- `aiSeasonalRefresh()` przebudowany: nowy mnożnik `clubDevMult` (filozofia klubu × reputacja × sukces kończącego się sezonu — górna połowa tabeli/Puchar) przyspiesza rozwój i łagodzi starzenie; nowe pasmo wzrostu 23-27 lat.
- Mechanika transferowa AI przepisana od zera (`match-post.js`): sprzedaż/zakup przechodzi teraz przez bramkę jakości (`aiEvaluateSale`/`aiEvaluateSigning` — kupuj tylko lepszych/porównywalnych, sprzedawaj tylko z konkretnym powodem), ochronę rdzenia składu (`aiCoreProtect` — najlepsi + młodzi wychowankowie nie idą na sprzedaż bez wyjątkowej oferty) i limit podpisań na sezon skalowany typem klubu i ligą (`aiSigningCap`, `LEAGUE_AI_TUNING`). Nowy mechanizm: AI proaktywnie przedłuża kontrakty chronionym graczom przed wygaśnięciem (`aiTryRenewContracts`), zamiast tracić ich bez decyzji klubu.
- Nowe limity liczby zawodników na pozycję w składzie (`POS_QUOTA`, data.js) — bez tego kluby AI potrafiły zgromadzić np. 10 bramkarzy i 0 napastników (zmierzone: 38% klubów z ≥4 bramkarzami po 8 sezonach), co samo w sobie osłabiało wyniki i przez to tempo rozwoju.
- Usunięty reset składu dołu VIII Ligi (season-summary.js) — kasował zawodników bez śladu i tworzył nowych przez surowy mkPlayer() z pominięciem pasma OVR ligi; to też był błąd, przez który klub GRACZA (jeśli spadł na dno VIII Ligi) tracił cały skład bez ostrzeżenia.

10. Likwidacja puli wolnych agentów (G.fa) — zamknięty świat
- Nowa, formalna zasada w js/CLAUDE.md: świat zawodników to zamknięty obieg — wejście wyłącznie jako junior, wyjście wyłącznie przez emeryturę, cała reszta to bilansowanie między klubami. Pula G.fa (zawodnicy „zawieszeni" bez klubu) łamała tę zasadę i dawała nierealistyczne, oderwane od budżetu ceny.
- Silnik AI: wygasły kontrakt/sprzedaż bez kupca szuka teraz bezpośrednio realnego klubu z wolnym miejscem (`aiSignReplacement` — zastąpił dawną `aiSignFromFA`); brak takiego klubu = kontrakt zostaje automatycznie przedłużony. Zero zawodników w zawieszeniu.
- Panel kryzysu kadrowego gracza (navigation-squad.js) pokazuje teraz konkretne, nazwane oferty transferu od klubów AI z nadwyżką na danej pozycji zamiast anonimowej listy „wolnych agentów", z ceną kotwiczoną do aktualnego budżetu gracza (nigdy więcej niż połowa) zamiast pełnej wartości rynkowej.
- 8 wydarzeń Kroniki Klubu opartych o G.fa (m.in. „okazja transferowa", „licytacja z rywalem", zbuntowany zawodnik) przepisane na realnych kandydatów z nadwyżki u klubów AI, z tą samą logiką kotwiczenia ceny do budżetu.
- 150 startowych wolnych agentów rozdzielone bezpośrednio do klubów AI na starcie gry zamiast osobnej puli.

11. Naprawa nadmiernej liczby transferów AI po pkt. 9-10
- Po wdrożeniu limitu podpisań na sezon (pkt 9) okazało się, że i tak dochodziło do kilkunastu transferów na klub na sezon zamiast kilku wg AI_TYPES.maxAnnualSignings, część z kwotami sięgającymi setek milionów euro, część klubów AI na kilkudziesięciomilionowym minusie budżetowym.
- Przyczyna 1: `aiTransferPlayer()` nigdy nie zapisywał strony sprzedającego (tylko kupującego) — naprawione, teraz loguje obie strony jednego transferu w jednym miejscu.
- Przyczyna 2: cztery z pięciu miejsc uzupełniających skład AI nie miały żadnego limitu liczby zakupów na wywołanie ani odniesienia ceny do budżetu — dodane limity i cena kotwiczona do budżetu kupującego (ta sama zasada co w pkt 10).
- Przyczyna 3: trzy niezależne, redundantne ścieżki robiły dokładnie to samo zadanie („uzupełnij skład") w tej samej sekwencji zmiany sezonu — dwie usunięte jako czysta duplikacja.
- Przyczyna 4: limit podpisań na sezon zerował się w środku sekwencji zmiany sezonu, PO tym jak dwie wcześniejsze funkcje już coś podpisały — te podpisania nigdy się nie liczyły do limitu. Reset przeniesiony na sam początek sekwencji.
- Wynik: liczba transferów na klub na sezon spadła o połowę, 0 klubów AI na minusie budżetowym w symulacji 5-sezonowej (wcześniej ~55 na ~127). Nadal wyżej niż docelowe 3-6/sezon — do dalszej obserwacji.

12. Limit sprzedaży, domknięcie ostatniej dziury w limitach transferów AI i naprawa populacji świata
- Dodany symetryczny limit sprzedaży (`aiSellingCap`, `maxAnnualSells` w AI_TYPES — akademia 2, sprzedający 5, bogaty 1, stabilny 2, skalowane ligą) — do tej pory capowane było tylko kupno, sprzedaż nie miała żadnego ograniczenia.
- Znaleziona i domknięta ostatnia nielimitowana ścieżka: krok „wygasłe kontrakty" w `aiRenewContracts()` (match-post.js) przenosił zawodnika do innego klubu całkowicie z pominięciem `aiSigningCap`/`aiSellingCap` — to była najbardziej masowa, w pełni bezlimitowa ścieżka transferów AI. Naprawione: transfer odbywa się teraz tylko gdy obie strony mają jeszcze zapas w limicie sezonowym; inaczej kontrakt automatycznie się przedłuża (już istniejący fallback).
- Wynik (symulacja 5-sezonowa): wolumen transferów na klub spadł z 48-64 (suma kupna+sprzedaży po 5 sezonach) do 25-49, zgodnie z zaprojektowanymi limitami per typ klubu (np. „sprzedający" z założenia ma wyższy wolumen — to jego filozofia, nie błąd).

13. Diagnoza i naprawa spadającego OVR świata AI, cz. 2 — nieosiągalny potencjał juniorów
- Diagnoza (symulacja 16-sezonowa z podziałem na grupy wiekowe): OVR grupy ≤22 lat spadał z ~46 do ~32 i się tam stabilizował, a `headroom` (potencjał minus OVR) rósł bez końca zamiast się domykać — grupa 23-27 zaczynała łapać ten sam spadek po ok. 6 sezonach, gdy słaby rocznik zaczynał w nią wchodzić.
- Przyczyna: generator juniorów AI (`aiTransferSeason`, match-post.js) liczył potencjał osobnym wzorem kotwiczonym do szczytu ligi (`lgMax*0.9+r(0,10)`), niezależnie od faktycznego (niskiego) OVR startowego juniora — luka 26-31 pkt, nieosiągalna przy obecnym tempie wzrostu. Cała reszta świata (populacja startowa, `mkLeaguePlayers()`) już używała właściwej, powiązanej z OVR formuły `calcPotential()` (data.js) — to był jedyny niespójny punkt.
- Naprawione dwoma zmianami w tej samej funkcji: pasmo startowego OVR juniora podniesione z `r(lgMin-8,lgMin+5)` na `r(lgMin-3,lgMin+10)`, potencjał liczony teraz przez `calcPotential(junior,lvl)` zamiast osobnego wzoru — headroom po zmianie stabilny ~12 pkt (dopasowany do reszty świata) zamiast rosnącego bez końca.
- Sprawdzone: system akademii własnego klubu gracza (`systems/academy.js`) ma podobnie dużą lukę potencjału (min. 20 pkt), ale to inny, zamierzony mechanizm — napędzany aktywnym treningiem gracza (`training-stadium.js`, bez twardej granicy wieku, ze wzmocnieniami z centrum treningowego), analitycznie osiągalny w ~7 sezonach nawet bez inwestycji. Zostawiony bez zmian.

14. Naprawa kurczącej się populacji świata AI
- Diagnoza: mimo napraw z pkt. 12-13 populacja świata AI systematycznie malała (3428→2921 w 16 sezonów, ~15%, bez oznak zatrzymania), choć dobór juniorów (~140-210/sezon) formalnie przewyższał zmierzony odpływ emerytów.
- Przyczyna 1 — podwójna loteria emerytalna: `season-summary.js` losował emeryturę dla WSZYSTKICH zawodników (`G.players.forEach`, próg 10-90% wg wieku) tuż przed tym, jak `aiRenewContracts()` (match-post.js) robił dla klubów AI DRUGĄ, niezależną loterię (inny próg 5-90%) — AI miało efektywnie dwie szanse na emeryturę w tym samym sezonie zamiast jednej. Pętla w `season-summary.js` miała wyraźny ślad, że pierwotnie miała dotyczyć tylko klubu gracza (news o zapowiedzi emerytury filtrowany po `myClubId`) — zawężona do `myPl()`, tak jak było zamierzone; AI ma teraz jedną, właściwą loterię.
- Przyczyna 2 — dobór juniorów wciąż za wolny nawet bez duplikatu: każdy klub AI miał tylko 85% szans w sezonie na w ogóle podjęcie naboru juniora (`Math.random()<0.85` w `aiTransferSeason`) — usunięty ten losowy próg (+17,6% naboru), dopasowane do zmierzonego niedoboru (~18%).
- Wynik (symulacja 16-sezonowa): populacja ustabilizowała się w okolicach 3120-3260 zamiast dalej spadać (spadek złagodniał z ~15% do ~9% i się wypłaszcza od 6. sezonu). Średni OVR świata nadal spada podobnym tempem co wcześniej — to osobny, wciąż niezbadany wątek (skład populacji nowych juniorów zdaje się przesuwać w stronę słabszych lig z czasem), do dalszej analizy.

11.07.2026
1. Newsy klubowe/transferowe — osobna sekcja per liga
- Przeniesione z ogólnej zakładki Świat do kontekstu przeglądu lig (ui/season-summary.js) — każda liga ma teraz własną zakładkę Newsy z wydarzeniami klubów tej ligi.
- Diagnoza przed wdrożeniem: struktura newsa (addNews) nie miała pola clubId/leagueId — trzeba było dopisać leagueLevel do G.worldNews (osobny kanał od newsów gracza, bez ryzyka dla starych zapisów).
- Zakładka Świat → Newsy usunięta całkowicie (duplikowała nową sekcję per liga) — zakładka „Newsy" w karcie klubu pokazuje wszystkie zdarzenia tego klubu bez filtra, zakładka przy lidze — tylko istotne (priority ≥ 50).
- Limit newsów: per liga (60 dla własnej ligi, 30 dla pozostałych) zamiast jednego globalnego licznika, z priorytetowym kasowaniem (ważne zdarzenia przeżywają dłużej niż rutynowe).
- Próg „duży transfer" przeliczony z mnożnika OVR (nieosiągalny w I lidze — wymagał OVR>99) na wartość rynkową zawodnika na szczycie pasma danej ligi.
- Zakres rozszerzony na wszystkie 8 lig (wcześniej tylko własna liga ±1) — dotyczy serii wyników, kryzysu finansowego (potem usuniętego, patrz niżej), talentów akademii, celów zarządu.

2. Pełna przebudowa architektury newsów (rejestr typów, nie if/else)
- Zaprojektowany i wdrożony rejestr WORLD_NEWS_TYPES (priorytet/ikona/kolor/pula wariantów tekstu/reguła cooldownu per typ) — dodawanie nowego typu newsa to teraz jeden wpis w rejestrze, nie rozgałęziony kod.
- Usunięty news o kryzysie finansowym klubów AI (Twoja decyzja redakcyjna) — samo naliczanie budżetu zostaje, wpływa na zdolność zakupową AI.
- Rekord serii klubu — seria zwycięstw/porażek jest teraz porównywana z historycznym rekordem klubu (nie tylko „dokładnie 4 z rzędu”); pobicie rekordu dostaje mocniejszy tekst i wyższy priorytet.
- Priorytet newsów akademii obniżony pod próg zakładki ligi — widoczne tylko na karcie klubu, zgodnie z Twoją uwagą.
- Komplet humorystycznych wariantów tekstu PL/EN dla każdego typu (3 warianty na typ), w tonie już ustalonym w narracji meczowej.

3. Łączenie newsów tego samego klubu w jednym tygodniu (digest)
- Na Twoją prośbę: kilka zdarzeń jednego klubu z tego samego tygodnia scala się teraz w jeden wpis zamiast osobno zaśmiecać log.
- Wymagało to przebudowy potoku zapisu na dwuetapowy: detektory zdarzeń buforują w club.ai, a flushWeeklyNews() (wołane na końcu tygodniowego cyklu i przy starcie nowego sezonu) opróżnia bufor — pojedyncze zdarzenie zapisuje się normalnie, kilka scala się w digest.
- Złapany własny błąd po drodze: newsy generowane na starcie sezonu (zapowiedź bukmacherska) trafiały do bufora, ale nic go tam nie opróżniało — ginęły po cichu. Naprawione dopisaniem brakującego wywołania flush we właściwym miejscu.

4. Walka o tytuł/spadek i bukmacherska zapowiedź sezonu
- 3 kolejki przed końcem sezonu: news o zaostrzającej się walce o tytuł i o utrzymanie, liczony matematycznie (czy pozostałe punkty do zdobycia wciąż pozwalają dogonić lidera/uciec ze strefy spadkowej).
- Pierwszy news sezonu w każdej lidze: bukmacherski typ na tytuł i kandydatów do spadku, oparty o już istniejącą siłę składu (tStr) — bez nowej symulacji.

5. Rywalizacje/derby
- Kluby parowane w derby na starcie każdego sezonu — jawnie umownie (arbitralnie), bo projekt nie ma danych geograficznych do parowania „prawdziwego"; potwierdzone z Tobą przed wdrożeniem.
- Mecz między parą rywali generuje osobny news z wyższym priorytetem niż zwykły wynik.
- Efekt uboczny: ożywione martwe dotąd pole G.rival w Kronice Klubu (istniało w kodzie, nigdy nie było przypisywane) — bez dotykania tego pliku.

6. Retrospektywa sezonu
- Klub AI i liga: podsumowanie najważniejszych zdarzeń sezonu budowane redakcyjnie nad już zapisanymi newsami (bez nowej symulacji), widoczne w karcie klubu i w zakładce ligi.
- Klub gracza: osobna ścieżka na Twoją prośbę — nowa zakładka „Sezon" w istniejącym podsumowaniu sezonu, budowana z newsów gracza (G.news), nie z kanału AI.
- Złapany własny błąd po drodze: mechanizm łączenia newsów (pkt 3) scalał niemal wszystkie zdarzenia klubu w jeden wpis na tydzień, więc próg „co najmniej 2 zdarzenia" na retrospektywę był praktycznie nieosiągalny — poprawione na „2 zdarzenia lub 1 naprawdę ważne".
- Całość zweryfikowana dynamicznie: dwa pełne sezony rozegrane w przeglądarce przez wbudowany symulator deweloperski, zero błędów w konsoli.

7. Poprawki po Twoim zgłoszeniu z dev-mode (3. sezon) + rozszerzenie puli tekstów
- Duplikat ikony w newsach świata: każdy szablon i tak zaczynał się od własnej ikony, a osobny znacznik ikony w _worldNewsItemHtml() ją powielał — usunięty znacznik, kolorowy pasek zostaje jako wskaźnik typu.
- Chronologia przy zmianie sezonu była zaburzona: newsy końca sezonu (mistrzostwo/awans/spadek/cel, a także zwykłe zaległe newsy tygodniowe typu seria/derby z ostatniego tygodnia) znakowały się numerem NOWEGO sezonu zamiast kończącego się, bo G.season++ następował przed ich wygenerowaniem/spłukaniem bufora. Naprawione przekazaniem zapamiętanego numeru i tygodnia kończącego się sezonu do flushAllWeeklyNews()/generateSeasonRecaps() — obejmuje to każdy typ zdarzenia, nie tylko wyliczankę. Kolejność wywołań w startNewSeason() przestawiona tak, by retrospektywa lądowała w newsach POD derbami/zapowiedzią nowego sezonu, zgodnie z oczekiwanym obiegiem: derby → zapowiedź → newsy sezonu → podsumowanie → (nowy sezon) podsumowanie poprzedniego → derby → zapowiedź...
- Zakres historii recapów rozdzielony na Twoją prośbę: w zakładce ligi widoczny tylko recap ostatniego zakończonego sezonu (starsze znikają), w karcie klubu zostaje pełna kronika wszystkich sezonów wstecz.
- Każdy klub AI dostaje teraz „sezon w skrócie" w karcie klubu co sezon, również gdy sezon był spokojny (wcześniej próg „min. 2 zdarzenia" pomijał ciche kluby całkowicie) — spokojny sezon dostaje dedykowany tekst zamiast braku wpisu.
- Pula tekstów newsów rozszerzona z 3 do 10 wariantów na typ (PL+EN), dla wszystkich 19 typów zdarzeń — WORLD_NEWS_TYPES generuje teraz listę szablonów pomocniczą funkcją zamiast ręcznie wypisanych kluczy a/b/c.

8. Kolejna runda poprawek po testach w dev-mode (chronologia, karta klubu, bramkarze, nagrody AI)
- Chronologia — druga warstwa błędu: nie tylko mistrzostwo/awans/spadek/cel, ale KAŻDY zwykły zaległy news tygodnia (seria, derby) mógł dostać datę nowego sezonu, jeśli czekał w buforze klubu w momencie zamknięcia sezonu. Naprawione przeniesieniem nadpisania sezonu/tygodnia na poziom samego wywołania flusha zamiast wyliczanki typów.
- Bukmacherska zapowiedź sezonu i ogłoszenie derbów (zdarzenia z tygodnia 1, nie „co się wydarzyło") wykluczone z puli kandydatów do retrospektywy — wcześniej ich wysoki priorytet wciągał je do „sezonu w skrócie" poprzedniego sezonu.
- Złapany własny błąd: limit newsów per liga kasował retrospektywy klubów jako pierwsze (priorytet 40, poniżej progu zakładki ligi) — przy ~15 klubach w lidze same coroczne podsumowania przekraczały limit i znikały. Retrospektywy całkowicie wyłączone z tego kasowania; dopisany backfill dogenerowujący brakujące wpisy (S1 wzwyż) na istniejących zapisach.
- Zakres retrospektywy ligi ograniczony na Twoją prośbę do ostatniego zakończonego sezonu (starsze znikają z zakładki ligi); w karcie klubu zostaje pełna kronika wszystkich sezonów.
- Klub gracza dostał to samo traktowanie co AI: retrospektywa budowana z jego newsów (G.news), z filtrem wykluczającym przypomnienia-CTA (okno transferowe, cele zarządu, sponsorzy, obóz, oferty premium — mają w kodzie pole `.action`, to przyciski „zrób X", nie historia sezonu).
- Nazwy klubów wspomniane w treści newsa (nie tylko ten, do którego news jest przypięty) są teraz klikalne — prowadzą do karty tej konkretnej drużyny.
- Trofeum „Mistrz" w karcie klubu nie pokazywało której ligi — dodana nazwa ligi; dopisana też naprawa retrospektyw, które w starym kodzie zgubiły samo zdarzenie mistrzostwa (dane zawsze poprawne w G.lgHist, niezależnym źródle).
- Bezpiecznik: mkPlayer() losuje pozycję bez gwarancji rozkładu (1/8 szans na bramkarza) — przy generowaniu całego składu naraz (reset dołu VII Ligi) była realna szansa (~9%) na zero bramkarzy w klubie AI. Dodana funkcja sprawdzająca i uzupełniająca po każdej operacji, która może zmienić składy AI (zmiana sezonu, zimowe okno, wczytanie zapisu).
- Nagrody sezonowe zawodników (mistrzostwo, Puchar, awans, król strzelców, najlepsza ocena, MVP, wierny klubowi, ulubieniec kibiców) liczyły się wyłącznie dla składu gracza — zakładka Nagrody u zawodników AI zawsze była pusta. Wydzielona wspólna funkcja, wołana teraz też dla każdego klubu AI z danych uniwersalnych (allStandings/cupHistory zamiast G.trophies, systemu tylko dla klubu gracza).

9. Poprawka daty nagród AI (Twoje zgłoszenie: puchar za sezon, który dopiero się zaczął)
- Nagrody klubów AI z pkt. 8 dostały tę samą chorobę co newsy: assignSeasonAwards() dla AI było wołane już PO G.season++, więc nagroda za właśnie zakończony sezon zapisywała się z numerem NOWEGO sezonu — widoczne jako np. „Mistrzostwo ligi — SEZON 4" tuż po starcie sezonu 4, mimo że tytuł padł w sezonie 3. Karta drużyny (trofea z G.lgHist) była cały czas poprawna — błąd dotyczył tylko p.awards w karcie zawodnika.
- Naprawione: assignSeasonAwards() przyjmuje teraz jawny numer kończącego się sezonu zamiast liczyć na bieżący G.season.
- Migracja na wczytaniu zapisu: każda nagroda oznaczona numerem dopiero co rozpoczętego sezonu jest z definicji artefaktem tego błędu (nagroda mogła powstać tylko na koniec sezonu, zanim ten się zaczął) — cofa jej datę o jeden sezon.

10. Usunięty ranking prestiżu z zakładki Tabela → Historia → Legenda
- Na Twoją prośbę usunięty cały blok „KLUB — Ranking prestiżu" w renderLegenda() (season-summary.js) wraz z nieużywanymi już kluczami i18n (lg_prestige_*).

11. Zniesiony limit 3 zakupów w oknie transferowym
- Usunięta blokada w buyTransfer() (transfers.js) — zakup ograniczony teraz tylko budżetem klubu. Usunięty licznik „Kupiono: X/3" i disabled na przycisku Kup oraz powiązane nieużywane zmienne/klucze i18n. Limit sprzedaży (3/okno) zostawiony bez zmian — nie był objęty prośbą.

12. Karta klubu AI: „Forma" pokazuje realną dyspozycję zawodników zamiast sztucznego licznika
- Wiersz Forma w _renderClubCard() (club-modal.js) czytał dotąd club.ai.form — osobny licznik „passy" (start 50, ±3 za wygraną/porażkę), niezwiązany z realną kondycją zawodników i w ogóle niepokazywany dla własnego klubu, stąd wrażenie że AI startuje na 50% a gracz na 100%. Podmieniony na średnią formy (p.form) zawodników wyjściowej 11 — tę samą wartość, która realnie wpływa na moc drużyny w meczu — więc oba kluby startują teraz na ~100% i wartość reaguje na zmęczenie/kontuzje w sezonie. club.ai.form zostaje nietknięty, dalej napędza newsy o seriach wygranych/przegranych.

13. Poprawka przekierowań newsów do złej zakładki
- newsAction() (news-bootstrap.js): akcja finance_contracts celowała w .tab-btn:nth-child(3) panelu Finanse, czyli w zakładkę SEZONY zamiast KONTRAKTY — naprawione na selektor [data-tab="kontrakty"]. News o wygasłym kontrakcie (season-summary.js) prowadził tylko do ogólnego przeglądu Finansów zamiast wprost do kontraktów — przełączony na tę samą, poprawną akcję.

14. Usunięty bonus reputacji za nowy sezon
- Usunięte changeReputation(5, „Nowy sezon w roli menedżera") w season-summary.js — klub gracza nie dostaje już automatycznego +5 rep na starcie każdego sezonu. Reputacja za awanse/spadki/cele zarządu bez zmian.

15. Wyniki ligowe drużyny w karcie klubu (nowa zakładka WYNIKI)
- Diagnoza przed wdrożeniem: wynik każdego meczu bieżącego sezonu istniał w G.allSchedules, ale mecze AI-AI (simOthers() w match-post.js) nigdy nie trafiały do żadnego logu — tylko do zbiorczej tabeli; showMatchDetail() (traits-history.js) był ściśle sprzężony z perspektywą klubu gracza (czytał wyłącznie G.mHist).
- Wdrożony wariant „B-lekki" (Twoja decyzja): simOthers() dopisuje teraz lekki log meczu (minuta gola, kartki wg tych samych bazowych prawdopodobieństw co pełny silnik, oceny obu drużyn) do nowego G._mHistAI — pole runtime-only, celowo pominięte w saveGame() (SKIP), więc zero wpływu na rozmiar zapisu; resetowane co sezon tak jak G.mHist.
- Nowa zakładka WYNIKI w karcie klubu (własnego i dowolnego rywala) łączy G.mHist + G._mHistAI; klik na mecz otwiera showMatchDetail().
- showMatchDetail() dostał opcjonalny parametr src (mHist/ai) i neutralny framing „WYGRANA GOSPODARZY/GOŚCI" zamiast mylącego „WYGRANA/PRZEGRANA", gdy żaden z klubów w meczu nie jest klubem gracza. Ścieżka powrotu karta klubu ↔ karta zawodnika ↔ szczegóły meczu zsynchronizowana w club-modal.js/navigation-squad.js/tactics-playercard.js, żeby src przechodziło przez cały łańcuch.
- Zweryfikowane dynamicznie w przeglądarce (Playwright): pełna gra od nowej kariery po 1. kolejkę, zero błędów w konsoli; G._mHistAI miało poprawne 63 wpisy po kolejce, framing i ścieżka powrotu potwierdzone zrzutami ekranu dla meczu AI-AI i meczu gracza.
- Przy okazji znaleziony niezwiązany, wcześniej istniejący drobny problem: .md-overlay (position:absolute) może wyrenderować się poza widocznym obszarem po przewinięciu strony — zgłoszony, celowo nienaprawiany (poza zakresem tego zadania).

10.07.2026
1. Żywy świat klubów AI (nowy system)
- Osobny kanał newsów o klubach AI (G.worldNews) — awanse/spadki, seria wygranych/przegranych, Puchar, talenty z akademii, kryzysy finansowe, cele zarządu — widoczny w zakładce Świat i w karcie każdego klubu.
- Nowe pola klubu AI: forma, cel zarządu na sezon (losowany wg siły składu: mistrzostwo/awans/utrzymanie).
- Bilans bezpośrednich starć (H2H) z Twoim klubem w karcie rywala.

2. Ekonomia klubów AI
- Realne pensje zawodników AI (potrącane co 4 tygodnie, tak jak u Ciebie) — wcześniej p.salary istniało tylko na papierze.
- Przychód sezonowy zależny od miejsca w tabeli (0,7×–1,4× własnego funduszu płac klubu).
- Naprawiony poważny błąd: przychód liczył się przed tegorocznymi transferami, więc ekonomia AI systematycznie się rozjeżdżała (w 10-sezonowej symulacji: mediana budżetu spadała z +20k do -420k, 94% klubów na minusie) — po przeniesieniu liczenia na koniec fazy transferowej: 0 klubów poniżej zera w żadnym z 10 sezonów.
- AI kupuje zawodników tylko wtedy, gdy realnie ma na to budżet (wcześniej mogła się zadłużać bez ograniczeń, widziałem -40 mln).
- W karcie klubu i rankingu „Aktywne kluby" budżet zastąpiony wartością składu (realna, rynkowa wycena zawodników).

3. Reputacja — pełna przebudowa
- Modal historii reputacji gracza (klik na ⭐ Rep: w nagłówku) — pokazuje każdą zmianę z powodem i datą; podpięty do ~15 miejsc w grze + jeden centralny hak w Kronice (zamiast edycji ~50 osobnych zdarzeń).
- Reputacja AI działa według tych samych reguł co gracza (awans +50, spadek -30, Puchar, cele zarządu) i na tej samej skali.
- Cel zarządu w karcie klubu daje premię w reputacji zamiast pieniędzy.
- Na Twoją prośbę: limit 1000 zdjęty całkowicie — reputacja rośnie bez końca, a przychody (sponsorzy/reklamy/kontrakty/frekwencja/TV) liczone teraz krzywą nasycającą zamiast liniowego mnożnika z twardym sufitem — więcej reputacji zawsze coś daje, z malejącymi zyskami, bez ściany.

4. Drobne poprawki po drodze
- Kolory newsów o seriach wygranych/przegranych rozdzielone (zielony/czerwony zamiast jednego niebieskiego).
- Usunięte niepotrzebne pole „morale" (nic nie robiło) i „ambicja" po jego usunięciu.
- Kilka znalezionych martwych fragmentów kodu posprzątanych po drodze (m.in. duplikat logiki stadionu w dwóch plikach).

Podsumowanie od ostatniego podsumowania

Reputacja bez limitu 1000
- Usunięty twardy sufit — reputacja rośnie bez końca (gracz i AI).
- Przychody (sponsorzy/reklamy/kontrakty/frekwencja/TV) przeliczone na krzywą nasycającą repCurve() zamiast liniowego mnożnika z twardym cap — więcej reputacji zawsze coś daje, malejąco, bez ściany.
- Tabela progów sponsorskich rozszerzona o dwa otwarte poziomy powyżej 1000.

Cele zarządu za reputację
- Znalazłem, że rep500/rep1000 były zdefiniowane, ale nigdy nie trafiały do puli wyboru (martwy kod) — pokazałem Ci pełną tabelę 29 celów i drzewo doboru.
- Naprawione: rep1000 przekalibrowany, dodane nowe rep2000 ("Ikona ligi") i rep4000 ("Żywa legenda futbolu"), podpięte do 3 gałęzi selekcji.
- Złapałem własny błąd po drodze: rep4000 miał nieobsługiwaną trudność 'extreme', przez co renderowałby się na zielono zamiast czerwono — poprawione na 'hard'.

Audyt mechaniki reputacji
- Na Twoją prośbę wypisałem kompletną listę: kiedy i o ile zmienia się reputacja gracza (mecz, awans/spadek, Puchar, cele zarządu, Kronika) i AI (te same reguły, węższy zestaw wyzwalaczy, bez Kroniki i bez bonusu za pojedynczy mecz).

Naprawa jednostek w worldTab → Aktywne Kluby
- Diagnoza: to nie błąd danych ani duplikat fmtVal() (ten duplikat już wcześniej ktoś usunął — poprawiłem nieaktualną notatkę o tym w ARCHITECTURE.md) — tylko adaptacyjny formatter użyty w tabeli porównawczej.
- Dodana fmtMln() — dwa progi (mln ≥1mln, k poniżej), podmieniona w 4 miejscach (wydatki/przychody/saldo/wartość składu).
- Przy okazji znaleziony bonus-bug: fmtVal() źle formatuje liczby ujemne (np. -2500000 € zamiast -2,5 mln €) — zgłoszony, nie naprawiony (osobna decyzja, bo dotyczy funkcji używanej w wielu innych miejscach).

"Zawodnik star" klubu — usunięta duplikacja
- Zdiagnozowano: kryterium to najwyższy OVR w całej kadrze klubu (ui/club-modal.js), logika była zduplikowana w dwóch funkcjach.
- Wydzielona wspólna funkcja clubStarPlayer() — bez zmiany kryterium ani zachowania.

Trofeum za mistrzostwo ligi w karcie klubu
- Diagnoza: mechanizm już istniał, ale był zepsuty — wpis ligowy w G.trophies nigdy nie miał place:1, więc filtr w club-modal.js go nie łapał; dodatkowo G.trophies jest scoped tylko do własnego klubu, więc dla klubów AI trofeum i tak by się nie pokazało.
- Naprawione: trofea ligowe budowane teraz z entries (lgHist — uniwersalne dla każdego klubu), tak samo jak puchary z G.cupHistory.
- Przy okazji naprawiona ta sama przyczyna w liczeniu tytułów ligowych legend klubu (lgWins zawsze wychodziło 0).

Rekord transferowy i Aktywne kluby — pełna historia kariery
- Diagnoza: dane dla własnego klubu (G.fin.transfers) już liczyły się za całą karierę, ale dla klubów AI były ucięte do ostatnich 20 transferów na klub (transferLog) — starsze rekordowe transfery znikały, sprawiając wrażenie statystyk tylko z bieżącego sezonu.
- Dodane G.worldTopTransfers — globalny rekord transferowy (cała kariera, max 100, aktualizowany na bieżąco) oraz liczniki ai.totalSpent/totalEarned/totalBuys/totalSells na klubach AI.
- Stare zapisy: jednorazowa migracja z tego, co jeszcze zostało w okrojonym logu, żeby nic nie zniknęło z widoku po aktualizacji.
- Dopisane podpisy „cała historia kariery" w obu zakładkach Świata (Transfery i Aktywne kluby).





09.07.2026

Poprawa algorytmu wyboru MVP

☐ NIEZGODNOŚĆ WYNIKU MECZU (3-1 vs 4-1)
- wynik na boisku (np. 3-1) różni się od wyniku w relacji/komentarzu
  po meczu (np. 4-1)
- zdiagnozować rozjazd między silnikiem meczu a warstwą raportującą wynik
  (match-engine.js vs match-post.js / komentarz)
- ☐ KARTA ZAWODNIKA — ROZSZERZENIE
- rozważyć dodanie dodatkowych informacji, np. liczby MVP – dodano do nagród.

- ☐ DEFAULT LANGUAGE
- EN = domyślny język przy starcie nowej kariery
- PL = opcjonalny

-
☐ TIMING EVENTÓW KRONIKI - naprawione
- eventy nie powinny pojawiać się od razu po zakończeniu meczu
- wyzwalanie dopiero po wyjściu z ekranu meczu

- poprawa przejścia między meczem ligowym a pucharowym, musi być zachowana sekwencja wszystkich etapów – poprawione.
 
- poprawa żeby następowała zmiana wieku zawodników gracza, 


04.07.2026
Tłumaczenia z polski na angielski: sesja 1, sesja 2 i sesja 3, sesja 4, sesja 5 kronika 1/3,
Przywrócenie akademii 2 zakładek zamiast 3.

05.07.2026 
Dokończenie tłumaczeń na angielski:, etap 4 sesja 7 2/3 i 3/3, etap 5 sesja 8.
Są jeszcze w grze nieprzetłumaczone miejsca:…dużo cząstkowych i transfery całościowo.
Tłumaczenie całościowo przez Claude code – sprawdzenie pozostawionych braków tłumaczeń, podział na etapy i przetłumaczenie do końca.
Zakończono tłumaczenia.

06.07.2026 kolejne działania – nie można wychodzić z meczu jak się już wejdzie. Zostało to poprawione jak wejdziesz w analizę przedmeczową lub mecz nie można wyjść z meczu. Można wyjść ale wraca się do meczu.

Usunięcie przycisku skład z analizy przedmeczowej zostaje tylko taktyka. 

Poprawka nierównej liczby meczów przez puchary.

Poprawka dodanie walut euro, dol, zł. I przełącznik.

Poprawa wizualna zakładki tabela, i widoku tabeli ligowej.

poprawa asymetrii goli w meczach gracza i AI.


07.07.2026
Dalsza poprawa asymetrii goli w meczach gracza i AI. Poprawa silnika meczowego.

Poprawa tłumaczenia na angielski w transfery kupno zimowe.

Karta analizy przedmeczowej match odds poprawione.

Poprawa widoku w zakładce tabela.

Nowy wzór widoku meczu.

08.07.2026

Pracujemy nad nową wizualizacją meczu i po meczu.

Aktualizacja relacji meczowych na humorystyczne. 

Poprawa wychodzenia z karty zadania żeby wychodziło do ostatniego widoku.

Teraz wychodzenie jak się wejdzie do karty zadania do ostatniego widoku działa. 


Dodanie nowej zakładki po meczu: podsumowania meczowego.

Aktualizacja grafiki twarzy pixel art.


